/JIG_SHIT.md
# Mandatory repository-local infrastructure state.
/.cache/
/.temp/
/.logs/
/.dependencies/

# Non-authoritative study material embedded beside rule contracts.
**/cheap-reference/

# Local editor and operating-system state.
/.cursor/
/.idea/
/.vscode/
.DS_Store
Thumbs.db

# Local interpreter and tool caches.
**/__pycache__/
**/.pytest_cache/
**/.ruff_cache/
**/.mypy_cache/
**/.eslintcache
/node_modules/

# Local temporary artifacts outside the mandatory infrastructure roots.
*.log
*.tmp
*.temp

# DOOM
/doom/

# Reproducible algorithm outputs are local. User-owned interoperability input
# corpora are local too; other algorithm inputs/configuration remain versioned.
**/out/

# Canonical interoperability products are generated from user-supplied source
# and must remain local even if a tool accidentally writes them outside `out/`.
doom.c
doom.malbolge
