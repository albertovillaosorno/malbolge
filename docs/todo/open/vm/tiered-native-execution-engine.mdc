---
id: "tiered-native-execution-engine"
status: "active"
lane: "9"
area: "vm"
task: "TODO - Tiered native execution engine"
contract: "docs/technical/runtime/execution/tiered-native-execution-engine.md"
depends_on:
  - "safe-rust-malbolge-vm"
  - "self-modification-state-graph-optimizer"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/host-cpu-and-accelerator-runtime-baseline.md"
  - "docs/technical/adr/tiered-native-execution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "vm/"
  - "execution/"
  - "tests/vm/"
  - "benchmarks/interpreter/"
validation:
  - "jig validate --root ."
---
# TODO - Tiered native execution engine

## Objective

Build a tiered execution engine instead of choosing between interpretation, AOT,
and JIT. Decode Malbolge into a compact execution IR, simplify that IR through
verified state-graph mathematics, compile demonstrably stable regions to native
machine code before execution, specialize hot or mutation-sensitive regions at
runtime, and deoptimize safely to the interpreter whenever a code-version guard
or speculative assumption fails. The normative VM contract remains the semantic
baseline; native tiers are accelerators of identical observable behavior.

## Acceptance

- Interpreter, AOT, JIT, native cache, and deoptimization share one observable
  VM contract; disabling every native tier produces the same guest behavior.
- Observable state, I/O, termination, and diagnostics match the declared
  semantic profile across positive, boundary, and adversarial fixtures.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `vm/`, `execution/`, `tests/vm/`,
  `benchmarks/interpreter/`.
- Required evidence: semantic fixtures, state/I/O traces where diagnostic, and
  differential results against independent interpreter-compatible
  implementations; the original C source is compared only where its behavior is
  defined and reproducible.
- Prerequisite completion evidence: `safe-rust-malbolge-vm`,
  `self-modification-state-graph-optimizer`.
- Active foundation: `src/runtime/virtual-machine/domain/execution_ir.rs` owns
  portable effect IR v3
  (`EffectOp`, `MemoryLiveIn`, and `RegionEffectProgram`) and re-exports the
  VM-owned `TargetProfileRequirement` without creating a Rust crate boundary.
  State-graph research reprojects verifier traces into this IR,
  rejects tampered profile-ID/fingerprint/version/features/geometry,
  live-in/budget/outcome/effect fields, and preserves
  deterministic interpreter deoptimization on guard miss.
<!-- jig-ignore-next-line: canonical path or identifier is indivisible -->
- `src/runtime/virtual-machine/domain/profile.rs::preflight_runtime_requirement()`
  consumes an independently
  admitted IR requirement against an explicit runtime capability. Current IR
  produces byte-identical `MALBOLGE-PROFILE-001` text to canonical descriptor
  preflight under `safe-rust-classic`, is admitted by `safe-rust-profiled`, and
  rejects unknown feature IDs explicitly.
- `select_verified_direct_native()` now requires that explicit runtime
  capability,
  derives exact region memory from the IR, and applies program/profile capacity
  before runtime capability, host validation, or backend construction. `002`
  then
  `001` precedence is explicit and neither degrades to direct deopt.
- State-applying emitters/verifiers repeat the declared-profile capacity check,
  so bypassing selection cannot promote profile-invalid initial/register halt
  IR.
- `src/runtime/tiered-execution/adapter-outbound/cache/main.rs` now binds
  canonical byte-exact IR identity to host
  OS, ISA, backend revision, native ABI revision, and required features. Raw
  untrusted IR remains canonically transportable, but `RegionEffectIdentity` and
  `NativeArtifactKey` reject profile-capacity-inconsistent addressing through
  typed `NativeIdentityError::ProfileCapacity` before hashing or artifact
  construction. `NativeArtifactCache<Value>` adds caller-owned process-local
  lookup/insertion/replacement/removal. Derived digests are excluded from exact
  identity equality; operations prefer the indicated bucket but search all
  buckets
  when needed, so equal keys with different accelerator digests remain one
  entry.
  Digest collisions remain distinct because every operation confirms full key
  equality. Cache equality compares exact retained mappings rather than bucket
  layout, so the accelerator is absent from logical store identity. It owns no
  persistence, eviction, synchronization, or semantic admission.
- `src/runtime/tiered-execution/adapter-outbound/native/main.rs` now lowers
  structurally consistent effect IR to
  deterministic freestanding C23 under an exact `NativeArtifactKey`. All local
  entry/memory/input/output guards precede a collapsed final commit. Pinned
  Clang 22.1.8 emits real untrusted Windows COFF object candidates for both
  x86-64 and AArch64. Safe-Rust structural admission checks ISA, `.text`, entry
  identity, and relocation/symbol closure without trusting LLVM inspection.
  `src/runtime/tiered-execution/adapter-outbound/native/profile_metadata.rs` now
  solely owns the format-neutral MBPF
  encoding consumed by bootstrap/direct emitters and COFF admission. Direct
  deopt/initial-halt revision-4, halt-observation revision-5,
  halt-fetch/non-graphical/no-operation revision-2, and jump-data revision-1
  objects must carry exact read-only
  `MBPF` v3
  `.mbprof`
  metadata matching the profile ID/fingerprint, published version, semantic
  features, word trits, profile capacity, and derived `u64` region memory
  retained
  by their native key; absence or mismatch fails structurally. Bootstrap
  revision
  2 renders the same canonical payload into its C23 source and structural
  admission requires the section for that revision. Historical revision 1
  remains
  admissible without it; pinned Clang cross-ISA compilation is the object-level
  confirmation boundary.
- A direct deopt-only backend now emits canonical x86-64/AArch64 COFF without
  Clang. Complete byte fixtures plus structural validation admit only the exact
  `return guard-miss` programs; opcode mutation fails semantic admission. This
  is
  the first semantically admitted native artifact and intentionally never
  applies
  effects.
- `direct-initial-halt` is the first admitted state-applying template. It
  accepts
  only zero-state/no-I/O/no-memory one-step halt IR, preflights before commit,
  changes only termination on hit, and returns guard miss atomically otherwise.
  Complete object fixtures and x86-64 hit/miss/null execution evidence cover the
  boundary; ARM64 object linkage is also verified.
- `direct-halt-registers` revision 5 extends the same one-step halt contract
  across
  arbitrary 32-bit `A/C/D` values and full 64-bit input/output counter
  observations while memory/I/O effects remain forbidden. Independently rendered
  495/564-byte x86-64/AArch64 fixtures bind counters above `u32::MAX`; counter,
  opcode, and revision mismatch fail closed. The only commit remains
  termination.
- `direct-halt-fetch` revision 2 binds one exact graphical code-cell live-in to
  real halt termination. VM-owned positional decode must return `v`; full entry
  observation, memory pointer, exact IR footprint, exact `memory[C]`, and prior
  termination are guarded before committing only tag `1`. Independently rendered
  535/628-byte objects, x86-64 atomic execution, and AArch64 instruction
  decoding
  bind the contract.
- `direct-non-graphical` revision 2 is the first exact memory-live-in direct
  template. One VM-classified non-graphical live-in must equal `memory[C]`; full
  entry observation, memory pointer, and exact IR footprint are guarded before
  dereference, and only termination tag `2` is committed. Independently rendered
  538/631-byte objects, x86-64 hit/live-in/capacity/null execution, and AArch64
  instruction decoding bind the contract.
- `direct-no-operation` revision 2 is the first non-terminal and guest-memory
  writing direct template. VM-owned no-op classification, `XLAT2`, and modular
  pointer successor derive the exact IR exit. Full fetched-cell guards precede a
  single commit of encrypted `memory[C]` plus advanced `C/D`. Independent
  557/658-byte objects bind `77->65`, `5->6`, and `7->8`; x86-64 execution and
  AArch64 instruction decoding prove hit semantics and atomic misses.
- `direct-jump-data` revision 1 adds the first instruction-specific semantic
  data read. Two distinct entry live-ins at `C/D` are required; VM-owned decode,
  encryption, and successors derive the exact no-I/O transition. Independent
  564/699-byte objects bind `memory[5]:35->93`, `C:5->6`, and `D:7->124`;
  x86-64 execution and AArch64 decoding prove both live-in guards, exact
  125-word footprint, atomic commit, and misses. Aliasing remains rejected.
- `direct-jump-code` revision 1 adds the exact post-jump encryption order. Three
  distinct code/data/encryption live-ins bind `memory[5]=93`, `memory[7]=11`,
  and
  `memory[11]=68`; VM-owned decode, encryption, and successors derive
  `memory[11]:68->33`, `C:5->12`, and `D:7->8`. Independent 622/731-byte
  objects,
  x86-64 atomic execution, and AArch64 decoding prove the 13-word footprint,
  three guards, exact commit, and twelve common-miss branches.
- `direct-rotate` revision 1 adds the first reviewed transition with two
  guest-memory writes. VM-owned decode, `profile_rotate()`, encryption, and
  successors derive `memory[7]:10->1594326`, `memory[5]:34->122`,
  `A:0xdeadbeef->1594326`, `C:5->6`, and `D:7->8`. Independent 578/732-byte
  objects, x86-64 atomic execution, and AArch64 decoding prove the exact 9-word
  footprint, two live-in guards, two writes, three register commits, and eleven
  common-miss branches. Aliasing remains rejected.
- `direct-crazy` revision 1 adds the second reviewed two-write arithmetic
  transition. VM-owned decode and `profile_crazy()` derive
  `memory[7]:10->2391494`, `memory[5]:57->91`, `A:20->2391494`, `C:5->6`, and
  `D:7->8`. Independent 577/731-byte objects prove the exact 9-word footprint,
  two live-in guards, two writes, and three register commits. Aliasing and
  out-of-domain operands remain rejected.
- `direct-output` revision 1 adds the first reviewed direct I/O transition.
  VM-owned decode and `profile_low_byte()` derive encrypted code 57, output byte
  `0xa8`, `C/D=6/8`, and `output_len=4`. Independent 642/724-byte objects prove
  the exact 9-word footprint, non-null output pointer, strict capacity guard,
  one append, and atomic misses.
- `direct-input` revision 1 completes direct coverage of all eight instruction
  families. Byte input guards a non-null pointer, strict length, and exact byte,
  then commits encrypted code 68, `A=65`, and cursor 3. EOF guards length
  equality, accepts a null pointer, uses VM-owned `profile_eof_word()` for
  `A=4782968`, and keeps cursor 2. Independent complete objects are 659/744
  and 634/715 bytes.
- Every memory-backed direct template now compares ABI `memory_words` with the
  exact `NativeArtifactKey` IR footprint before dereference or commit.
- After program/profile/runtime admission, direct-template orchestration
  classifies IR before backend construction: exact zero-register halt selects
  the
  smallest fast path, other no-live-in halt IR selects the observation-bound
  template, graphical halt fetch and non-graphical termination select terminal
  live-in templates, exact non-aliasing jump/rotate/crazy IR plus exact
  input/output IR
  select their instruction-specific templates, exact no-op IR selects the
  memory-writing
  template, and remaining IR selects verified deopt. Unsupported direct host
  formats remain
  explicit at this narrow boundary and post-selection errors never degrade
  silently to fallback.
- `select_preflighted_execution_tier()` adds a product-neutral plan above that
  boundary. Only top-level direct format absence maps to the normative
  interpreter
  after preflight; `002`, `001`, and backend/emission/admission failures remain
  errors. It performs no cache lookup, executable memory, linking, or
  invocation.
- `select_cached_preflighted_execution_tier()` adds caller-owned direct reuse
  under
  explicit `DirectHost`. A private `PreparedDirectTarget` binds specialization
  to
  one exact key used by lookup and consumed by miss emission, avoiding a second
  IR
  canonicalization there; state-applying verifiers still reconstruct expected
  identity independently. `VerifiedDirectNativeCache` receives artifacts only
  after semantic admission, reports exact-key `Inserted`/`Hit`, matches uncached
  keys/bytes for all twelve templates, and reuses one pointer-identical
  immutable
  `Arc` allocation. Profile and host preflight occur before lookup; `002`,
  `001`,
  and interpreter selection leave cache cardinality unchanged. Exact-key
  invalidation removes one future reuse entry. Exact-program invalidation first
  constructs canonical region identity, removes every host/backend variant, and
  leaves unrelated regions unchanged; profile-invalid IR fails before mutation.
  Exact-target invalidation removes all regions sharing one complete
  OS/ISA/backend revision/native-ABI/features identity while preserving other
  targets. Outstanding `Arc` plans remain valid and reinsertion restores the
  same
  key/bytes under new allocations. `Arc` owns immutable sharing only;
  persistence,
  automatic eviction, synchronization policy, linking, executable memory,
  invocation, and performance remain outside.
- Native ABI revision 1 now has one Rust `repr(C)` authority. Its 80-byte
  call frame and every 64-bit-host field offset match the reviewed x86-64
  and AArch64 templates. Typed status/termination decoding rejects unknown
  values, and `NativeRegionCallFrame` binds pointers to borrowed slices while
  rejecting invalid capacities and cursors.
- `PreparedNativeRegionInvocation` now validates one canonical effect against
  exact caller memory, live-ins, input/EOF, output movement, and memory-write
  before-values. It snapshots complete ABI state, memory, and output, then
  admits only the exact IR-derived `Applied` result or a mutation-free
  `GuardMiss`. Unknown status, unexpected invalid arguments, topology drift,
  and partial commits fail closed. Every rejected completion restores the
  complete entry snapshot.
- `PreparedVerifiedDirectInvocation` reconstructs complete native identity from
  the program and verified artifact target, rejects canonical drift, and denies
  the deoptimization stub state-applying authority. `NativeRegionBuffers`
  groups memory/input/output loans so verified bytes, target assumptions, and
  the ABI pointer remain inseparable.
- `VerifiedDirectLoadImage` reparses verified COFF, rejects relocations,
  extracts
  immutable `.text` and its exact entry offset, retains complete key/target
  identity, and validates ISA alignment. Its only policy is RW staging followed
  by RX execution with mandatory instruction synchronization. Twelve templates
  across both ISAs pass exact-image evidence; machine drift and relocations fail
  closed.
- Staged, sealed, and ready executable typestates now admit explicit platform
  reports in exact RW-copy, same-mapping RX-seal, then full-code instruction
  synchronization order. Bytes, capacity, alignment, address arithmetic,
  mapping identity/range, permissions, and sync range fail closed. The ready
  state retains the exact release request; image-equal call binding is required
  before entry address and ABI state are exposed together. Twenty-four direct
  images pass the lifecycle and cross-ISA binding fails closed. Reports are
  adapter evidence only; no page allocation, permission transition, cache flush,
  cleanup operation, or foreign invocation is added.
- `NativeExecutableMemoryAdapter` now exposes exact allocate/copy/protect/sync/
  release operations to safe transactional orchestration. Allocation evidence is
  admitted before copy; returned bytes and copy identity are checked afterward.
  Every post-allocation failure attempts exact release and preserves primary
  plus cleanup diagnostics. Failed explicit release retains the ready executable
  for
  retry. A deterministic fake adapter covers 24 images, four operation failures,
  ten report drifts, cleanup failure, and exact retry. No concrete OS adapter or
  machine-code call is added.
- `NativeExecutableRunner` accepts only one exact bound executable invocation.
  `execute_verified_native()` composes load, bind, run, completion, and release.
  Load/runner failures abort the entry snapshot, completion rejection restores
  it, cleanup failure retains exact retry state, and final-release failure keeps
  the committed outcome. Six deterministic cases cover apply, guard miss, load
  short-circuit, runner mutation, completion drift, and release failure. No
  concrete unsafe call shim is added.
- `RegionEffectProgram::from_profile_step_trace()` now preserves complete
  fetch/data/encryption evidence as sorted one-step live-ins, while
  `select_verified_direct_sequence()` atomically publishes only a non-empty,
  profile-consistent, observation-continuous sequence whose every step selects
  a verified non-deopt direct artifact. A normative rotate/output trace
  produces exact x86-64/AArch64 plans; empty, discontinuous, profile-mixed,
  hidden-deopt, and post-termination shapes fail closed.
  `select_cached_verified_direct_sequence()` prepares the full sequence before
  lookup, preserves exact-hit `Arc` identity, stages verified unique misses, and
  inserts them only after complete success. Insert/replay, mixed hit/miss,
  preflight-before-lookup, and late-rejection rollback tests bind that
  transaction.
- Safe cached and uncached sequence runners now execute each exact one-step pair
  through load/bind/run/complete/release. Applied prefixes remain committed;
  guard miss returns the exact resume index/observation, and indexed failures
  preserve committed progress plus cleanup retry evidence. Rotate/output VM
  snapshots prove full completion, second-step guard resume, rollback, and
  applied/guard release failure.
- `NativeInterpreterContinuation` now validates native outcome/failure evidence
  against the complete cached or uncached plan and publishes an immutable
  semantic suffix independent of executable mappings. Complete/suffix artifact
  keys, remaining one-step programs, resume observation, expected exit/outcome,
  and guard/failure reason remain explicit. Constructors cover ephemeral and
  loaded failures. Forged counts, indices, observations, and progress fail
  closed; completed outcomes and terminal cleanup return no continuation.
  `advance()` rebases complete-plan authority after additional admitted work
  from
  any tier, deriving the exact new suffix or verified completion and rejecting
  overshoot/boundary drift. Eleven cases cover constructors and rebase behavior.
  The application handoff now resolves the
  exact canonical profile, admits either a full checkpoint or transferred
  memory/input/output-prefix state, and runs each remaining `step_traced()`
  transition through exact IR reprojection. Any machine, program, projection, or
  live-in drift returns the step-entry checkpoint and combined resume index;
  completion validates full-plan exit/outcome. Explicit budgets now yield
  completion or an affine suspension retaining cumulative progress, exact
  complete-plan resume index, artifact/program suffix, and normative checkpoint.
  Zero budget is a no-op, partial budget resumes without readmission, and
  oversized budget completes. Ten cases cover both transfer forms, admission,
  budgets, repeated zero after progress, and rollback before/after suspension.
- `application/scheduler.rs` now consumes one affine handoff under one explicit
  complete, positive-slice, caller-yield, or native-retry-yield decision. Every
  pause preserves exact checkpoint, complete-plan index, key/program suffix, and
  cumulative interpreter progress with `BudgetExhausted`, `CallerYield`, or
  `NativeRetry`. `resume()` consumes the same owner. Native retry is evidence
  only and never replans, loads, invokes, or masks hard failures. Five cases
  cover
  both yields, sliced/direct completion, cumulative progress, and failure
  propagation.
- `application/native_retry.rs` now binds one caller-replanned verified sequence
  to an exact `NativeRetry` suspension before buffer movement. Reason, ordered
  remaining programs, complete artifact key, and checkpoint entry must match;
  every rejection returns both owners unchanged. Five cases cover initial and
  progressed suffixes, caller-yield rejection, full-plan drift, and cross-ISA
  key drift. Admitted retries now execute through the existing uncached native
  sequence path from checkpoint-derived owned buffers. Success/failure retain
  exact plan, suspension, memory, input, output capacity, and observation; the
  transfer reconstructs a validated normative checkpoint, and cleanup ownership
  remains retryable. Five execution cases cover completion, guard miss,
  rollback,
  and committed cleanup failure. Successful retries now rebase through the
  original complete-plan continuation: applied suffixes yield the verified
  original outcome/final checkpoint, while guard miss advances absolute
  mixed-tier
  progress and returns a scheduler-ready normative handoff. Rebase rejection
  retains the complete successful execution owner. Three cases cover pure/mixed
  completion and progressed guard fallback. Failed retry executions now split
  independent semantic disposition from indexed native failure ownership. Runner
  failure before/after progress yields an exact fallback handoff; terminal
  cleanup failure may complete semantics while preserving release retry. Rebase
  rejection retains the full failed execution. Three cases cover both fallback
  positions and cleanup completion. Semantic publication now lives in
  `application/native_retry/rebase.rs`; admission, transfer, and execution
  remain
  in the parent with unchanged reexports and owner transitions.
- `application/leased_retry.rs` now binds an admitted retry to one exact
  immutable
  lease-cache acquisition. Key drift restores both owners before buffer
  movement.
  Loaded execution performs no allocation/release and retains hit/insertion
  evidence plus the lease across completion, guard miss, and runner failure.
  Success/failure rebase through complete continuation authority while lease
  return remains explicit. Five cases cover insertion completion, pointer-exact
  hit reuse without adapter work, progressed guard fallback, runner-failure
  fallback, and cross-ISA lease rejection. The following cached layers add
  automatic acquisition and bounded cycle reuse.
- `application/cached_retry.rs` now composes exact lease-cache acquisition,
  leased binding, and one loaded attempt. Hits perform no executable-memory
  work;
  insertions retain FIFO eviction/retirement evidence. Load failure restores the
  admitted retry plus exact load/cleanup ownership, while binding/runner failure
  retains the acquired lease. Five cases cover insertion, hit reuse, pre-runner
  load failure, mixed live-retirement insertion, and runner-failure fallback.
  Semantic rebase and lease return remain caller-owned.
- `application/cached_cycle.rs` now applies immutable attempt policy to cached
  execution. Successful attempts retain attempt number, committed native steps,
  and exact `Inserted`/`Hit` evidence, then drop only the external lease so
  active
  cache authority serves later retries. Unchanged guard suffixes hit the same
  resident sequence; progressed suffixes acquire distinct keys. Acquisition,
  binding, routing, or runner failure stops with exact owners and all successful
  prior attempts. Seven cases cover zero-limit fallback, `Inserted/Hit/Hit`
  reuse, progressed suffix insertion, initial/late load failure, initial routing
  failure, and runner failure with fallback.
- `application/cached_cycle/telemetry.rs` now exposes one exact source view for
  attempt slices, completion, and fallback owners. The summary counts attempts,
  native steps, hits, insertions, evictions, and retirements and fails closed on
  overflow. `telemetry_window.rs` retains a positive-capacity FIFO with
  monotonic sequences and exact totals. `telemetry_window/aggregation.rs`
  owns checked arithmetic; `telemetry_window/reconfiguration.rs` publishes
  capacity changes with oldest-first removals. `telemetry_assessment.rs`
  applies caller-owned inclusive thresholds after a positive attempt gate
  and retains every missed signal.
  `telemetry_snapshot.rs` validates complete capacity, eviction, sequence, and
  total evidence before reconstruction. `telemetry_codec.rs` adds canonical
  revision-one little-endian bytes and rejects framing, representation, and
  semantic drift before returning evidence. Caller-supplied latency histograms
  preserve inclusive buckets, exact totals, extrema, and transactional overflow
  evidence without reading a clock. Validated snapshots transfer complete
  histogram state and reject impossible count, extrema, total-range, or
  overflow-bin evidence. Identical-schema histograms merge transactionally with
  exact rollback. Canonical revision-one latency bytes preserve extrema flags,
  `u128` totals, bound/count pairs, and repeat snapshot validation after decode.
  Rebinning, distributed/durable merge, automatic recommendation/publication,
  runtime clock acquisition, async ownership, and durable storage remain open.
- `application/retry_planner.rs` now consumes one exact native-retry suspension
  with explicit runtime capability/OS/ISA. Supported Windows hosts receive an
  admitted exact native suffix; only direct `TargetFormat` absence becomes a
  normative handoff. Profile, IR shape, continuity, deoptimization, emission,
  and verification remain hard stable errors retaining the suspension. Four
  cases cover Windows route, Linux fallback before/after progress, hard profile
  rejection, and invalid scheduler reason.
- `application/retry_policy.rs` now applies one immutable caller-configured
  maximum native-attempt count before planning. Remaining budget preserves the
  exact suspension with completed/next attempt evidence; exhaustion routes to
  complete or positive-slice normative fallback, and zero limit falls back
  immediately. Caller supplies attempt counts, while invalid reasons retain the
  owner and fail. Four cases cover attempt numbering, both fallback forms, and
  reason rejection.
- `application/retry_router.rs` now composes attempt policy and exact host
  planning in one affine request. Exhaustion bypasses planning; remaining budget
  selects a numbered native route; missing format applies configured normative
  fallback without counting an attempt. Policy and hard planning failures retain
  the suspension. Five cases cover Windows route, exhaustion bypass, sliced
  format fallback, hard profile rejection, and invalid reason.
- `application/retry_turn.rs` now executes exactly one routed turn. Interpreter
  routes use their scheduler decision without native adapter calls. Native
  routes
  execute one attempt and immediately rebase success/failure; attempt evidence
  and
  semantic disposition remain independent from indexed runner/release ownership.
  Rebase rejection retains the full execution owner. Five cases cover both
  completions, guard fallback, runner failure, and cleanup completion.
- `application/retry_cycle.rs` now runs a bounded multi-turn cycle. Only
  successful native guard misses reschedule the exact handoff as `NativeRetry`;
  immutable attempt policy guarantees native completion or configured normative
  fallback. Runner/release failure exits immediately with semantic and cleanup
  owners intact. Seven cases cover zero/format fallback, repeated guards, later
  native completion, runner/cleanup failure, and hard routing rejection before
  adapters. Cached execution is owned by `cached_retry.rs` and
  `cached_cycle.rs`; adaptive telemetry, async queues, and broader product
  orchestration remain open.
- `ReadyNativeExecutableSequence` now prederives every image and loads every
  mapping before execution. It supports repeated loaded runs without new memory
  operations,
  rolls back partial loads in reverse, and releases all mappings while retaining
  failed cleanup for retry. Complete chain identity is admitted before buffers
  change. Seven cases cover reuse, cached guard miss, rollback, aggregate
  release, cross-ISA rejection, and runner failure. The adapter owns unique
  non-overlapping live mappings.
- `NativeExecutableSequenceCache` applies positive entry limits plus optional
  live-mapping and admitted mapped-byte limits to exact ordered artifact lists.
  Candidate weight comes from ready mapping reports. Hits preserve FIFO age and
  usage without adapter work. Oversized candidates clean themselves without
  changing prior entries; candidates that fit alone evict repeatedly until all
  projected limits fit and report every evicted key. Failed later insertion
  eviction removes cache authority for that victim and earlier successful
  victims, cleans the candidate, and retains failed ownership for retry.
  Invalidation and full drain update usage before cleanup.
  `reconfigure_limits()` publishes expansion or already-satisfied requests
  without adapter operations. Shrink requests release oldest entries until
  current usage fits, while retaining previous limits until all required
  releases succeed. Failure reports all removed keys and exact release
  ownership;
  retry plus the same request publishes without repeating completed releases.
  Seventeen cases cover original reuse/admission paths plus expansion,
  entry/mapping/byte shrink, and second-eviction failure. Transition results,
  diagnostics, and FIFO shrink now live in
  `executable_cache/reconfiguration.rs`; lookup/admission remain in the parent.
- `NativeExecutableSequenceLeaseCache` now shares exact ready chains through
  cloneable immutable `Arc` leases. Active lookup and retired residency are
  separate. Eviction, invalidation, and full drain retire leased entries while
  keeping their exact weighted usage charged; unleased entries release
  immediately. Lease clone/drop performs no adapter work. Explicit return or
  reconciliation releases only residents with no external owners, preserves
  still-leased FIFO entries, and returns keyed failures for exact retry. A miss
  blocked by retired weight cleans its candidate and reports exact limits,
  usage, removals, and blockers. Dynamic resident limits now expand without
  adapter work and shrink through active FIFO release/retirement. Leased victims
  keep exact weight charged, prior retired entries are never reconciled
  implicitly, and old limits survive blockage or keyed cleanup failure. Final
  lease return or cleanup retry permits the same request to publish without
  duplicate release. Fourteen cases cover the original lifecycle plus
  entry/mapping shrink, live entry/byte blockage, post-return publication, and
  release-failure retry. Retired reclamation/keyed cleanup now live in
  `executable_lease_cache/reconciliation.rs`, while resident-limit transitions
  live in `executable_lease_cache/reconfiguration.rs`; lookup/leasing remain in
  the parent. Durable/cross-process leasing, object fusion, direct
  branch chains, a concrete FFI shim, adaptive cache policy, and async/product
  scheduling remain out.
- Remaining fused-region emission plus cache-aware AOT/JIT
  policy beyond verified direct process-local reuse, durable cache
  serialization/storage/eviction, executable-memory policy, invocation, and
  end-to-end tier performance remain unimplemented.

**Migrated root planning detail.**

This detail moved from the former expanded `TODO.md` index on 2026-08-05; the
typed record is now its sole planning owner.

Build a tiered execution engine instead of choosing between interpretation, AOT,
and JIT. Decode Malbolge into a compact execution IR, simplify that IR through
verified state-graph mathematics, compile demonstrably stable regions to native
machine code before execution, specialize hot or mutation-sensitive regions at
runtime, and deoptimize safely to the interpreter whenever a code-version guard
or speculative assumption fails. The normative VM contract remains the semantic
baseline; native tiers are accelerators of identical observable behavior.
Portable effect IR v3 is now product-owned under `execution/ir/`, and the
verifier/deoptimization research boundary exercises it directly. The first
host-code boundary under `execution/native/` lowers verified-effect-shaped IR to
atomic freestanding C23 candidates and pinned Clang 22.1.8 emits real untrusted
Windows COFF objects for both x86-64 and AArch64. Safe-Rust COFF admission now
checks target ISA, executable/non-writable `.text`, exact entry identity,
self-contained relocations, and absence of undefined host dependencies. A
private
`profile_metadata.rs` owner now supplies one format-neutral `MBPF` encoding to
bootstrap/direct emitters and structural admission. Direct objects additionally
require read-only `.mbprof` metadata matching
profile ID/fingerprint, published version, ordered semantic features, word
trits,
profile capacity, and exact derived `u64` region memory retained by their native
key; absence, tampering, or object/key mismatch fails structurally. Bootstrap
revision-2 C23 source now renders the same canonical payload into a read-only
`.mbprof` declaration, and structural admission requires it for revision 2 while
preserving historical revision-1 absence. The pinned cross-ISA compile test
remains
the object-level confirmation boundary. A separate direct deopt backend now
emits canonical x86-64/AArch64 COFF stubs whose complete bytes are independently
verified: they return guard miss without touching state, providing the first
semantically admitted native artifact and deterministic fallback floor. A second
byte-canonical direct backend now admits the exact initial-halt IR subset and
performs the first native guest-state commit: after zero-state preflight it sets
only termination to halt; misses are atomic. `direct-halt-registers` revision 5
extends that exact template to every 32-bit `A/C/D` combination and full 64-bit
input/output counter observations, still with no memory/I/O effects.
Independently
rendered 495/564-byte x86-64/AArch64 fixtures bind counters above `u32::MAX` and
nontrivial registers; counter, opcode, and historical-revision mismatches fail
closed. `direct-halt-fetch` revision 2 binds real graphical halt termination to
one exact code-cell live-in at `C`; VM-owned positional decode must produce `v`.
Full entry observation, non-null memory, and the exact IR footprint precede the
`memory[C]` guard and sole termination-tag `1` commit. Independent 535/628-byte
objects, x86-64 hit/live-in/capacity/null execution, and AArch64 instruction
decoding bind the contract. `direct-non-graphical` revision 2 applies the same
memory guards to a VM-classified non-graphical live-in and commits only tag `2`.
Independent 538/631-byte objects bind that path. `direct-no-operation` revision
2
is the first non-terminal and memory-writing direct effect: VM-owned decode,
`XLAT2`, and modular-successor helpers derive one exact `memory[C]` encryption
plus
`C/D` advance. Independent 557/658-byte objects bind `memory[5]:77->65`,
`C:5->6`, and `D:7->8`; x86-64 execution and AArch64 instruction decoding
confirm
atomic hit/miss behavior. `direct-jump-data` revision 1 adds the first
instruction-specific data read for distinct `C/D` live-ins. VM-owned decode,
`XLAT2`, and successor helpers derive `memory[5]:35->93`, `C:5->6`, and
`D:7->124`; independent 564/699-byte objects and cross-ISA evidence bind the
contract while aliasing remains rejected. `direct-jump-code` revision 1 adds the
three-live-in post-jump encryption order: `memory[5]=93` decodes `i`,
`memory[7]=11` selects the new code pointer, and `memory[11]:68->33` is
encrypted
before `C:5->12` and `D:7->8`. Independent 622/731-byte objects, x86-64
execution,
and AArch64 decoding bind exact hit behavior plus code/data/encryption/capacity/
null misses; all three addresses must be distinct. `direct-rotate` revision 1 is
the first reviewed direct effect with two guest-memory writes. VM-owned decode,
`profile_rotate()`, encryption, and successor helpers derive
`memory[7]:10->1594326`, `memory[5]:34->122`, `A:0xdeadbeef->1594326`,
`C:5->6`, and `D:7->8`. Independent 578/732-byte objects, x86-64 execution, and
AArch64 decoding bind exact hit behavior plus code/data/capacity/null misses;
`C == D` remains rejected.

`direct-crazy` revision 1 adds the second reviewed two-write arithmetic
transition. VM-owned decode, `profile_crazy()`, encryption, and successor
helpers derive `memory[7]:10->2391494`, `memory[5]:57->91`, `A:20->2391494`,
`C:5->6`, and `D:7->8`. Independent 577/731-byte objects bind the exact 9-word
footprint, two live-in guards, two writes, and three register commits. Aliasing
and out-of-domain data or accumulator operands remain rejected.
`direct-output` revision 1 adds the first direct I/O transition. VM-owned
decode,
`profile_low_byte()`, encryption, and successors derive `memory[5]:94->57`,
byte `0xa8`, `C:5->6`, `D:7->8`, and `output_len:3->4`. Independent 642/724-byte
objects bind the exact 9-word footprint plus output pointer and capacity guards;
x86-64 execution proves atomic code/capacity/pointer/footprint misses.
`direct-input` revision 1 completes direct instruction-family coverage with
independent byte and EOF forms. VM-owned decode, `profile_eof_word()`,
encryption, and successors derive `memory[5]:112->68` plus `C/D=6/8`. Byte input
binds cursor 2 and byte `0x41`, commits `A=65`, and advances the cursor to 3;
EOF binds `input_len=cursor=2`, commits `A=4782968`, and leaves the cursor
unchanged without dereferencing the input pointer. Independent complete objects
are 659/744 bytes for byte input and 634/715 for EOF. Every memory-backed direct
template now guards ABI `memory_words` against the exact
key-bound IR footprint before any read or write. Direct-template selection
requires an explicit runtime capability, derives exact `u64` region memory
from C/D pointers, live-ins, and writes, and applies
`MALBOLGE-PROFILE-002` before `001`, host, or backend selection.
After admission, zero-register halt chooses the smallest specialization, other
no-live-in halts choose observation-bound code, graphical halt fetch and
non-graphical termination choose their terminal live-in templates, exact
non-aliasing jump-code, jump-data, rotate, crazy, input, output, and no-op IR
choose
reviewed state-applying templates, and remaining IR chooses verified deopt;
unsupported direct host formats remain
explicit at that narrow boundary. A product-neutral preflighted tier plan maps
only
that format absence to the normative interpreter after `002`/`001` checks; real
backend/emission/admission failures remain errors. Raw untrusted IR remains
canonically transportable for deterministic rejection,
but `RegionEffectIdentity` and `NativeArtifactKey` reject addressing beyond the
embedded profile capacity before hashing, bootstrap lowering, or direct object
construction. Direct deopt/initial-halt revision-4, halt-observation revision-5,
plus halt-fetch/non-graphical/no-operation revision-2 and
jump-code/jump-data/rotate/crazy/input/output revision-1 objects bind that exact
footprint in `MBPF` v3, and same-profile footprint mismatch fails structural
admission. Native ABI revision 1 now has one Rust `repr(C)` authority with an
80-byte layout and exact offsets matching both reviewed ISA template families.
Typed status and termination decoders reject unknown values, while a borrowed
`NativeRegionCallFrame` validates capacities and cursor bounds before exposing
a raw state pointer. `PreparedNativeRegionInvocation` now validates one
canonical
effect against complete memory, input, and output evidence, snapshots every
caller-visible surface, and admits only an exact `Applied` transition or a
byte-preserving `GuardMiss`. Unknown status, unexpected invalid arguments,
foreign topology changes, and partial memory/output/state commits fail closed.
Every rejected completion restores the complete entry snapshot before return.
`PreparedVerifiedDirectInvocation` now reconstructs the complete native key from
one program plus the verified artifact's exact target, rejects any canonical
identity mismatch, and denies the guard-miss deoptimization stub state-applying
authority. `NativeRegionBuffers` groups the three caller-owned buffer loans, so
verified COFF bytes, target assumptions, and the ABI pointer are exposed only by
one inseparable binding. `VerifiedDirectLoadImage` now reparses that verified
COFF, rejects every relocation, extracts the exact immutable `.text` bytes and
entry offset, retains the complete key/target identity, and validates ISA
instruction alignment. Its only representable policy is RW staging followed by
RX execution plus mandatory instruction synchronization. All twelve direct
templates produce exact images on x86-64 and AArch64; machine drift and added
relocations fail closed. `NativeExecutableMappingId` plus staged, sealed, and
ready typestates now admit explicit platform reports only in RW-copy, RX-seal,
full-code instruction-sync order. Exact bytes, capacity, alignment, address
arithmetic, mapping identity/range, permissions, and synchronization range fail
closed. `ReadyNativeExecutable` retains the image and exact release request;
`PreparedNativeExecutableInvocation` exposes entry address and ABI state only
after the ready image equals the prepared call image. All 24 direct images pass
the complete lifecycle, while adversarial reports and cross-ISA call binding
fail closed. These reports are adapter evidence, not OS operations: no pages are
allocated, permissions changed, instructions flushed, mappings released, or
machine code invoked yet. `NativeExecutableMemoryAdapter` now defines those five
platform operations explicitly. `load_native_executable()` derives the exact
allocation request, admits RW evidence before copy, verifies returned copy bytes
and identity, seals the same mapping RX, synchronizes the full code range, and
returns only a ready executable. Every post-allocation adapter or admission
failure attempts the exact release request; primary and cleanup failures remain
separate. Explicit release consumes the ready executable on success and retains
it for exact retry on failure. A deterministic fake adapter proves all 24 direct
images, four operation failures, ten evidence drifts, cleanup failure retention,
and release retry. `NativeExecutableRunner` now receives only an exact
`PreparedNativeExecutableInvocation`, keeping entry address, mapping identity,
and ABI pointer inseparable. `execute_verified_native()` loads, binds, runs,
admits, and releases one effect transactionally. Load or runner failure invokes
explicit abort restoration; completion rejection restores through the existing
contract; every ready mapping is released or retained for exact retry. A final
release failure preserves an already committed `Applied` or `GuardMiss` outcome.
Six deterministic runner cases prove successful apply, atomic guard miss, load
short-circuit, runner mutation rollback with cleanup retry, completion-drift
rollback, and committed outcome retention. No concrete OS adapter, unsafe call
shim, or machine-code invocation exists yet.
Complete normative `ProfileStepTrace` evidence can now be projected into exact
one-step IR with sorted, deduplicated fetch/data/encryption live-ins.
`select_verified_direct_sequence()` verifies one canonical profile, exact
adjacent observations, final-only termination, and a real non-deopt direct
artifact for every step before publishing the ordered plan. A retained
rotate/output VM trace selects exact artifacts on both ISAs; empty,
discontinuous, profile-mixed, hidden-deopt, and post-termination sequences fail
closed. `select_cached_verified_direct_sequence()` extends that boundary with
transactional process-local reuse: exact entry-cache hits preserve `Arc`
identity, unique misses remain staged until every step verifies, and only then
enter the caller-owned cache. Insert/replay evidence records `0/2` then `2/0`
hit/insertion counts; mixed reuse records `1/1`, cached runtime rejection
leaves the exact map unchanged, and a late hidden deopt preserves an unrelated
cached artifact pointer-identical. `execute_verified_native_sequence()` and
its cached counterpart now pair every retained program with its exact artifact,
run each one through the safe load/bind/run/complete/release transaction, and
preserve already committed prefixes. `Applied` advances to the next step;
`GuardMiss` returns the exact zero-based resume index and entry observation with
the current step untouched. Preparation, runner, completion, and cleanup failure
report the failing step, committed-step count, continuation observation, and
retryable release state. VM snapshots after one and two rotate/output steps
prove uncached and cached completion, second-step guard resume, second-step
mutation rollback, and both applied/guard cleanup failures.
`NativeInterpreterContinuation` now converts admitted cached, uncached, loaded,
or ephemeral sequence outcomes/failures into one immutable semantic handoff.
It validates complete-plan progress and rejects forged final counts, resume
indices,
and observations, and retains the full ordered artifact key plus the exact
remaining artifact-key suffix and cloned one-step programs. Guard misses retain
a `GuardMiss` reason; indexed failures retain `ExecutionFailure`. Completed
outcomes and terminal cleanup failures produce no continuation because no
semantic work remains. `advance()` now rebases the same complete-plan
identity after additional admitted work from any tier, preserving expected
exit/outcome while deriving an exact new key/program suffix. Completion returns
no continuation only at the verified exit; overshoot and observation drift fail
closed. Eleven deterministic cases cover constructor families, malformed public
evidence, partial rebase, completion, and rejected rebase. This object remains
independent from interpreter mutation.
`NativeInterpreterHandoff` now consumes that object in the normative safe-Rust
`ProfileMachine`. It accepts either one validated `ProfileMachineState` or owned
memory/input plus a committed output prefix, resolves exact profile identity,
fingerprint, and requirement, and admits checkpoint observation plus first-step
live-ins before execution. Every remaining step runs through `step_traced()` and
is reprojected to exact `RegionEffectProgram`; machine failure, projection
drift,
or live-in mismatch returns the current-step entry checkpoint and exact complete
plan resume index. Completion combines native-prefix and interpreter-suffix step
counts and requires the original exit observation/outcome. `execute_with_budget`
now executes at most the requested suffix steps and returns `Completed` or one
affine `NativeInterpreterHandoffSuspension`. A suspension retains the original
continuation, cumulative interpreter progress, complete-plan resume index, exact
artifact-key/program suffix, and normative checkpoint; `into_handoff()` resumes
without readmission or state inference. Zero budget is a no-op suspension,
and an
oversized budget completes normally. Ten deterministic cases cover transfer,
admission, completion, zero/partial/oversized budgets, repeated zero after
progress, and rollback before or after suspension.
`application/scheduler.rs` now consumes exactly one affine handoff plus one
explicit decision: complete in the interpreter, execute one positive slice, or
yield zero steps to the caller or for possible native retry. Every pause wraps
the exact handoff suspension with `BudgetExhausted`, `CallerYield`, or
`NativeRetry`; `resume()` consumes that owner under a later decision. Native
retry is evidence only: this boundary performs no native replanning, loading, or
invocation and converts no hard error into fallback. Five deterministic cases
cover both yield reasons, slice/completion, direct completion, cumulative
progress, and unchanged failure propagation.
`application/native_retry.rs` now binds a caller-replanned
`VerifiedDirectSequencePlan` to one `NativeRetry` suspension before any buffer
movement. Admission requires the exact scheduler reason, ordered one-step
program suffix, complete artifact key, and checkpoint entry observation. Every
rejection retains both affine owners for correction or fallback. Five cases
cover initial and progressed suffixes, caller-yield rejection, full-plan drift,
and cross-ISA key drift. An admitted retry can now execute through the existing
uncached native sequence path using buffers copied from its checkpoint. Success
and failure both retain the original suspension/plan plus exact memory, input,
output capacity, and admitted observation. The transfer converts back to a
validated `ProfileMachineState`; hard native failures and release-retry
ownership
remain unchanged. Five more cases cover initial/progressed completion, guard
miss,
runner rollback, and committed cleanup failure. Successful retry evidence now
rebases through the original complete-plan continuation: applied suffixes
produce
the verified original outcome plus final checkpoint, while guard miss produces a
new `NativeInterpreterHandoff` with absolute progress including prior
interpreter
steps and newly committed retry steps. Rebase failure retains the complete
successful execution owner. Three cases cover pure-native completion, mixed
interpreter/native completion, and progressed guard fallback through the
scheduler. Failed retry executions now use the same rebase evidence and split
into independent semantic disposition plus indexed native failure owner. Runner
failure before or after committed retry progress yields an exact normative
handoff; terminal cleanup failure can yield verified completion while preserving
the release-retry owner. Rebase rejection retains the complete failed execution.
Three cases cover zero-progress fallback, progressed fallback, and semantic
completion with later cleanup retry. Semantic publication now lives in
`application/native_retry/rebase.rs`; admission, transfer, and execution remain
in `native_retry.rs`, with the same reexported API and affine owners.
`application/leased_retry.rs` now binds an admitted retry to one exact immutable
lease-cache acquisition. Key drift returns both owners before buffer movement.
Loaded execution performs no allocation or release and retains hit/insertion
evidence plus the lease across completion, guard miss, and runner failure.
Success and failure rebase through the same complete continuation authority
while
the lease remains independently reusable or explicitly returnable. Five cases
cover insertion completion, pointer-identical hit reuse without adapter work,
progressed guard fallback, runner-failure fallback, and cross-ISA lease
rejection.
`application/cached_retry.rs` now composes exact lease-cache acquisition,
leased binding, and one loaded retry attempt. Hits perform no executable-memory
work; inserted acquisitions preserve FIFO eviction/retirement evidence. Load
failure restores the admitted retry plus exact load/cleanup ownership, while
binding or runner failure retains the acquired lease. Five cases cover inserted
completion, hit reuse, pre-runner load failure, mixed live-retirement insertion,
and runner-failure fallback. Semantic rebase and lease return remain
caller-owned.
`application/cached_cycle.rs` now applies immutable attempt policy over the
cached
path. Successful attempts record attempt number, committed native steps, and
exact `Inserted`/`Hit` evidence, then drop only the external lease so active
cache
authority can serve the next retry. Unchanged guard suffixes reuse the same
resident mappings; progressed suffixes acquire distinct exact keys. Acquisition,
binding, routing, or runner failure stops immediately with exact owners and
every
successful prior attempt. Seven cases cover zero-limit fallback,
`Inserted/Hit/Hit` guard reuse, progressed suffix insertion, initial and late
pre-runner failure, initial routing failure, and runner failure with fallback.
`application/cached_cycle/telemetry.rs` now exposes one exact source view for
attempt slices, native completion, and normative fallback owners. It summarizes
attempts, native steps, hits, insertions, evictions, and retirements and fails
closed on arithmetic overflow. `telemetry_window.rs` retains those summaries in
a positive-capacity FIFO with monotonic sequences, exact aggregate totals,
exact checked arithmetic in `telemetry_window/aggregation.rs`, transactional
capacity changes in `telemetry_window/reconfiguration.rs`, and explicit
oldest-first removal evidence. `telemetry_assessment.rs` applies
caller-owned inclusive count thresholds after a positive attempt gate and
preserves every simultaneously missed signal without selecting policy.
`telemetry_snapshot.rs` transfers complete window state and validates
capacity, eviction counts, contiguous sequence evidence, and aggregate totals
before reconstruction. `telemetry_codec.rs` adds canonical revision-one
little-endian bytes with exact magic/version/reserved/length checks and repeats
snapshot validation after decoding. Nine cases cover nonempty/empty roundtrip,
header drift, length drift, count overflow, zero capacity, total/sequence drift,
and encoder-side rejection. `telemetry_latency.rs` records caller-supplied
nanoseconds into strictly increasing inclusive buckets with exact totals,
extrema, and transactional overflow rejection; six cases cover bounds, bucket
edges, the overflow bin, and each counter failure.
`telemetry_latency_snapshot.rs` transfers complete histogram evidence and checks
bucket counts, sample sums, extrema membership, exact possible total ranges, and
an impossible overflow bin; eight cases cover roundtrip and every rejection.
`telemetry_latency_merge.rs` combines identical bucket schemas transactionally,
retains exact merged totals/extrema, and rejects schema or counter drift; seven
cases cover exact/no-op merge and every overflow.
`telemetry_latency_codec.rs` adds canonical revision-one little-endian histogram
bytes with exact extrema flags, `u128` totals, bound/count pairs, and repeated
snapshot validation; nine cases cover roundtrip and framing/semantic drift.
Rebinning, distributed merge, runtime clock acquisition, automatic policy
recommendation/publication, async ownership, durable storage, and cross-process
coordination remain open.
`application/retry_planner.rs` now consumes an exact `NativeRetry` suspension,
explicit runtime capability, OS, and ISA. It selects and admits the exact native
suffix on supported Windows hosts; only a direct `TargetFormat` absence becomes
a normative handoff. Profile, IR shape, continuity, deoptimization, emission,
and
verification failures remain hard stable categories and retain the suspension.
Four cases cover Windows native routing, Linux fallback before/after interpreter
progress, hard profile failure, and rejection of caller-yield reason. Adaptive
artifact/cache selection policy above the existing cached execution path remains
open.
`application/retry_policy.rs` now applies one immutable caller-configured
maximum
native-attempt count before host planning. Available budget returns the exact
suspension with a one-based next-attempt number. Exhaustion converts the owner
to
a complete or positive-slice interpreter route; zero maximum attempts falls back
immediately. Caller supplies the completed-attempt count, and non-`NativeRetry`
reasons fail while retaining ownership. Four cases cover attempts one/two,
complete fallback, zero-limit sliced fallback, and reason rejection.
`application/retry_router.rs` now composes the attempt policy with exact host
planning in one affine request. Exhausted limits bypass host planning; available
budget selects a one-based native attempt; missing target format applies the
configured normative fallback without counting an attempt. Policy and hard
planning failures retain the suspension. Five cases cover Windows native route,
planner bypass at exhaustion, sliced format fallback, hard profile rejection,
and caller-yield rejection.
`application/retry_turn.rs` now executes exactly one routed turn. Interpreter
routes run their configured scheduler decision without touching native adapters.
Native routes execute one attempt and immediately rebase success or failure.
Successful and failed native turns expose attempt evidence plus semantic
disposition; failed turns separately retain indexed runner/release ownership.
Rebase failures retain their complete execution owner. Five cases cover
interpreter completion, native completion, guard fallback, runner failure, and
semantic completion with cleanup retry.
`application/retry_cycle.rs` now executes a complete bounded cycle. It loops
only
after a successful native guard miss, rescheduling the exact handoff as
`NativeRetry`; immutable attempt policy guarantees termination through native
completion or configured normative fallback. Runner/release failure exits on its
first occurrence with semantic and cleanup owners intact. Seven cases cover
zero limit, missing-format fallback without native work, repeated guards to
fallback, guard then native completion, immediate runner failure, cleanup
failure
without retry, and hard routing rejection before adapters. Cached execution is
owned by `cached_retry.rs` and `cached_cycle.rs`; adaptive telemetry, async
queues,
and broader product orchestration remain open.
`ReadyNativeExecutableSequence` now owns every ready mapping for one exact plan.
All load images are derived before allocation; mappings then load
transactionally before the first call. A failed later load releases the ready
prefix in reverse
order and retains every failed cleanup for aggregate retry. Loaded execution
validates mapping count and complete image identity before caller buffers can
change, then reuses the same mappings across repeated cached or uncached runs
without new memory-adapter operations. Final release attempts every mapping in
reverse order even after one failure and retains only still-owned mappings for
retry. The adapter contract now requires unique mapping identities and
non-overlapping live ranges. Seven deterministic cases prove reuse, cached guard
resume, partial-load rollback, cleanup retention, all-mapping release, cross-ISA
prevalidation, and current-step rollback.
`NativeExecutableSequenceCache` now adds weighted caller-owned reuse across
exact plans. Its key remains the ordered list of full `NativeArtifactKey`
values.
`new()` preserves the positive whole-entry limit, while `with_limits()` adds
optional positive live-mapping and admitted mapped-byte limits. Every candidate
loads before accounting so weight comes from exact admitted `mapped_len`
evidence. A candidate that alone exceeds a mapping or byte limit is fully
released without changing prior cache authority. Otherwise the cache releases
as many oldest FIFO entries as required for projected entry, mapping, and byte
usage to fit. Exact hits preserve insertion age and usage without memory-adapter
operations. Failed candidate load leaves prior entries untouched. A failed later
insertion eviction removes that victim and earlier successful victims from cache
authority, cleans the candidate, and returns every still-owned release for exact
retry. Exact invalidation and `release_all()` decrement or reset usage before
cleanup, so failed release never leaves stale cache budgets. Overflow-safe usage
reports exact entries, mappings, and mapped bytes.
`reconfigure_limits()` now expands or publishes already-satisfied limits without
adapter work and shrinks by releasing oldest entries until current usage fits
all
requested budgets. The previous limits remain published until every required
release succeeds. A failed shrink removes authority and usage for the failed
victim plus earlier successful victims, retains the previous limits, reports all
removed keys, and returns exact release ownership for retry. Repeating the
request after cleanup publishes the new limits without repeating completed
releases. Seventeen deterministic cases cover the original hit/FIFO/cleanup and
weighted-admission behavior plus no-op expansion, entry/mapping/byte shrink, and
failure during the second reconfiguration eviction. Transactional limit result,
diagnostic, and FIFO-shrink authority now lives in
`executable_cache/reconfiguration.rs`; lookup/admission remain in the parent.
`NativeExecutableSequenceLeaseCache` now provides cloneable immutable `Arc`
leases over exact loaded sequences. Active lookup and retired residency are
separate: invalidation, eviction, or full drain removes lookup authority, while
live leases keep mappings resident and keep their exact
entry/mapping/byte weight charged. Lease clone and drop perform no adapter
operation. Explicit `return_lease()` or `reconcile_retired()` releases only
residents with no external owners, attempts every releasable entry, and returns
keyed failures for exact
retry. A miss can retire leased FIFO victims and release unleased victims; when
retired resident weight still prevents admission, the candidate cleans itself
and the failure reports limits, usage, evicted keys, and blocking retired keys.
`reconfigure_limits()` now publishes expansion or already-satisfied resident
limits without adapter work. Shrink removes active FIFO authority, releases
unleased victims, and retires leased victims while preserving their exact
charged
weight. Existing retired entries are never reclaimed implicitly. Blockage or
release failure retains the previous limits and reports exact evicted, retired,
and blocking keys plus resident usage; final lease return or keyed cleanup retry
followed by the same request publishes without duplicate releases. Fourteen
deterministic cases cover the original lease lifecycle plus expansion,
entry/mapping shrink, live entry/byte blockage, post-return publication, and
release-failure retry. Explicit reclamation/retry ownership now lives in
`executable_lease_cache/reconciliation.rs`, while resident-limit transitions
live in `executable_lease_cache/reconfiguration.rs`; lookup and leasing remain
in
the parent. This still does not fuse COFF, jump directly between mappings,
persist loaded state, or implement the unsafe machine-code call shim.
`NativeArtifactCache<Value>` now provides caller-owned process-local exact-key
reuse. Derived bucket digests do not participate in identity equality; equal
keys
remain one entry even when their accelerator digests differ, while forced digest
collisions remain independent across read, replacement, and removal. Cache
equality compares exact retained key/value mappings rather than bucket layout.
`VerifiedDirectNativeCache` narrows that store to semantically admitted
direct artifacts. Cache-aware planning prepares one specialization-bound exact
key
for lookup and consumes it during miss emission, avoiding a second IR
canonicalization while state-applying verifiers retain independent key
reconstruction. It reports `Inserted`/`Hit`, matches uncached keys/bytes for
all twelve templates, and shares the same immutable `Arc` allocation on hits.
Exact invalidation removes one full-equality key from future reuse without
revoking held
plans. Exact-program invalidation constructs canonical region identity before
mutation and removes every host/backend variant while preserving unrelated
regions; profile-invalid IR fails without changing the cache. Exact-target
invalidation removes all regions sharing one complete OS/ISA/backend
revision/native-ABI/features identity while preserving other targets. Requesting
removed variants again reinserts identical keys/bytes under new allocations.
Planning performs `002`, `001`, and host-format selection before lookup, so
rejected/interpreter outcomes do not mutate the artifact cache. Artifact-cache
persistence and automatic eviction, executable-cache synchronization policy,
fused-region emission, native-retry orchestration beyond bounded process-local
cached cycles, async scheduling, the unsafe foreign-call boundary, and broader
AOT/JIT performance policy remain open.

Current implementation foundation: portable effect IR v3, verifier admission,
deterministic deoptimization, capacity-consistent canonical native cache
identity,
process-local collision-safe reuse storage, cache-aware verified direct
planning, transactional trace-derived multistep plans with exact resume,
validated semantic interpreter continuations, budgeted normative checkpoint
handoff, explicit affine continuation scheduling, relocation-free load images,
persistent exact-plan executable ownership,
entry/mapping/mapped-byte weighted exact-sequence FIFO reuse with transactional
limit reconfiguration, shared immutable executable leases with explicit retired
reconciliation, and an untrusted cross-ISA bootstrap object boundary are
implemented. Combined-region native fusion, concrete foreign invocation, durable
cache serialization/storage, adaptive retry policy, and wider automated tier
orchestration remain open.
