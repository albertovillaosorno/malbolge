---
id: "tiered-native-execution-engine"
status: "active"
lane: "9"
area: "vm"
task: "TODO - Tiered native execution engine"
contract: "docs/technical/runtime/execution/tiered-native-execution-engine.md"
depends_on:
  - "safe-rust-malbolge-vm"
  - "self-modification-state-graph-optimizer"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/host-cpu-and-accelerator-runtime-baseline.md"
  - "docs/technical/adr/tiered-native-execution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "vm/"
  - "execution/"
  - "tests/vm/"
  - "benchmarks/interpreter/"
validation:
  - "jig validate --root ."
---
# TODO - Tiered native execution engine

## Objective

Build a tiered execution engine instead of choosing between interpretation, AOT,
and JIT. Decode Malbolge into a compact execution IR, simplify that IR through
verified state-graph mathematics, compile demonstrably stable regions to native
machine code before execution, specialize hot or mutation-sensitive regions at
runtime, and deoptimize safely to the interpreter whenever a code-version guard
or speculative assumption fails. The normative VM contract remains the semantic
baseline; native tiers are accelerators of identical observable behavior.

## Acceptance

- Interpreter, AOT, JIT, native cache, and deoptimization share one observable
  VM contract; disabling every native tier produces the same guest behavior.
- Observable state, I/O, termination, and diagnostics match the declared
  semantic profile across positive, boundary, and adversarial fixtures.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `vm/`, `execution/`, `tests/vm/`,
  `benchmarks/interpreter/`.
- Required evidence: semantic fixtures, state/I/O traces where diagnostic, and
  differential results against independent specification-conformant
  implementations; the historical interpreter is compared only on its documented
  agreement domain.
- Prerequisite completion evidence: `safe-rust-malbolge-vm`,
  `self-modification-state-graph-optimizer`.
- Active foundation: `src/runtime/tiered-execution/domain/ir/main.rs` owns
  portable effect IR v3
  (`EffectOp`, `MemoryLiveIn`, and `RegionEffectProgram`) and re-exports the
  VM-owned `TargetProfileRequirement` without creating a Rust crate boundary.
  State-graph research reprojects verifier traces into this IR,
  rejects tampered profile-ID/fingerprint/version/features/geometry,
  live-in/budget/outcome/effect fields, and preserves
  deterministic interpreter deoptimization on guard miss.
<!-- jig-ignore-next-line: canonical path or identifier is indivisible -->
- `src/runtime/virtual-machine/domain/profile.rs::preflight_runtime_requirement()`
  consumes an independently
  admitted IR requirement against an explicit runtime capability. Current IR
  produces byte-identical `MALBOLGE-PROFILE-001` text to canonical descriptor
  preflight under `safe-rust-classic`, is admitted by `safe-rust-profiled`, and
  rejects unknown feature IDs explicitly.
- `select_verified_direct_native()` now requires that explicit runtime
  capability,
  derives exact region memory from the IR, and applies program/profile capacity
  before runtime capability, host validation, or backend construction. `002`
  then
  `001` precedence is explicit and neither degrades to direct deopt.
- State-applying emitters/verifiers repeat the declared-profile capacity check,
  so bypassing selection cannot promote profile-invalid initial/register halt
  IR.
- `src/runtime/tiered-execution/adapter-outbound/cache/main.rs` now binds
  canonical byte-exact IR identity to host
  OS, ISA, backend revision, native ABI revision, and required features. Raw
  untrusted IR remains canonically transportable, but `RegionEffectIdentity` and
  `NativeArtifactKey` reject profile-capacity-inconsistent addressing through
  typed `NativeIdentityError::ProfileCapacity` before hashing or artifact
  construction. `NativeArtifactCache<Value>` adds caller-owned process-local
  lookup/insertion/replacement/removal. Derived digests are excluded from exact
  identity equality; operations prefer the indicated bucket but search all
  buckets
  when needed, so equal keys with different accelerator digests remain one
  entry.
  Digest collisions remain distinct because every operation confirms full key
  equality. Cache equality compares exact retained mappings rather than bucket
  layout, so the accelerator is absent from logical store identity. It owns no
  persistence, eviction, synchronization, or semantic admission.
- `src/runtime/tiered-execution/adapter-outbound/native/main.rs` now lowers
  structurally consistent effect IR to
  deterministic freestanding C23 under an exact `NativeArtifactKey`. All local
  entry/memory/input/output guards precede a collapsed final commit. Pinned
  Clang 22.1.8 emits real untrusted Windows COFF object candidates for both
  x86-64 and AArch64. Safe-Rust structural admission checks ISA, `.text`, entry
  identity, and relocation/symbol closure without trusting LLVM inspection.
  `src/runtime/tiered-execution/adapter-outbound/native/profile_metadata.rs` now
  solely owns the format-neutral MBPF
  encoding consumed by bootstrap/direct emitters and COFF admission. Direct
  deopt/initial-halt revision-4, halt-observation revision-5,
  halt-fetch/non-graphical/no-operation revision-2, and jump-data revision-1
  objects must carry exact read-only
  `MBPF` v3
  `.mbprof`
  metadata matching the profile ID/fingerprint, published version, semantic
  features, word trits, profile capacity, and derived `u64` region memory
  retained
  by their native key; absence or mismatch fails structurally. Bootstrap
  revision
  2 renders the same canonical payload into its C23 source and structural
  admission requires the section for that revision. Historical revision 1
  remains
  admissible without it; pinned Clang cross-ISA compilation is the object-level
  confirmation boundary.
- A direct deopt-only backend now emits canonical x86-64/AArch64 COFF without
  Clang. Complete byte fixtures plus structural validation admit only the exact
  `return guard-miss` programs; opcode mutation fails semantic admission. This
  is
  the first semantically admitted native artifact and intentionally never
  applies
  effects.
- `direct-initial-halt` is the first admitted state-applying template. It
  accepts
  only zero-state/no-I/O/no-memory one-step halt IR, preflights before commit,
  changes only termination on hit, and returns guard miss atomically otherwise.
  Complete object fixtures and x86-64 hit/miss/null execution evidence cover the
  boundary; ARM64 object linkage is also verified.
- `direct-halt-registers` revision 5 extends the same one-step halt contract
  across
  arbitrary 32-bit `A/C/D` values and full 64-bit input/output counter
  observations while memory/I/O effects remain forbidden. Independently rendered
  495/564-byte x86-64/AArch64 fixtures bind counters above `u32::MAX`; counter,
  opcode, and revision mismatch fail closed. The only commit remains
  termination.
- `direct-halt-fetch` revision 2 binds one exact graphical code-cell live-in to
  real halt termination. VM-owned positional decode must return `v`; full entry
  observation, memory pointer, exact IR footprint, exact `memory[C]`, and prior
  termination are guarded before committing only tag `1`. Independently rendered
  535/628-byte objects, x86-64 atomic execution, and AArch64 instruction
  decoding
  bind the contract.
- `direct-non-graphical` revision 2 is the first exact memory-live-in direct
  template. One VM-classified non-graphical live-in must equal `memory[C]`; full
  entry observation, memory pointer, and exact IR footprint are guarded before
  dereference, and only termination tag `2` is committed. Independently rendered
  538/631-byte objects, x86-64 hit/live-in/capacity/null execution, and AArch64
  instruction decoding bind the contract.
- `direct-no-operation` revision 2 is the first non-terminal and guest-memory
  writing direct template. VM-owned no-op classification, `XLAT2`, and modular
  pointer successor derive the exact IR exit. Full fetched-cell guards precede a
  single commit of encrypted `memory[C]` plus advanced `C/D`. Independent
  557/658-byte objects bind `77->65`, `5->6`, and `7->8`; x86-64 execution and
  AArch64 instruction decoding prove hit semantics and atomic misses.
- `direct-jump-data` revision 1 adds the first instruction-specific semantic
  data read. Two distinct entry live-ins at `C/D` are required; VM-owned decode,
  encryption, and successors derive the exact no-I/O transition. Independent
  564/699-byte objects bind `memory[5]:35->93`, `C:5->6`, and `D:7->124`;
  x86-64 execution and AArch64 decoding prove both live-in guards, exact
  125-word footprint, atomic commit, and misses. Aliasing remains rejected.
- `direct-jump-code` revision 1 adds the exact post-jump encryption order. Three
  distinct code/data/encryption live-ins bind `memory[5]=93`, `memory[7]=11`,
  and
  `memory[11]=68`; VM-owned decode, encryption, and successors derive
  `memory[11]:68->33`, `C:5->12`, and `D:7->8`. Independent 622/731-byte
  objects,
  x86-64 atomic execution, and AArch64 decoding prove the 13-word footprint,
  three guards, exact commit, and twelve common-miss branches.
- `direct-rotate` revision 1 adds the first reviewed transition with two
  guest-memory writes. VM-owned decode, `profile_rotate()`, encryption, and
  successors derive `memory[7]:10->1594326`, `memory[5]:34->122`,
  `A:0xdeadbeef->1594326`, `C:5->6`, and `D:7->8`. Independent 578/732-byte
  objects, x86-64 atomic execution, and AArch64 decoding prove the exact 9-word
  footprint, two live-in guards, two writes, three register commits, and eleven
  common-miss branches. Aliasing remains rejected.
- `direct-crazy` revision 1 adds the second reviewed two-write arithmetic
  transition. VM-owned decode and `profile_crazy()` derive
  `memory[7]:10->2391494`, `memory[5]:57->91`, `A:20->2391494`, `C:5->6`, and
  `D:7->8`. Independent 577/731-byte objects prove the exact 9-word footprint,
  two live-in guards, two writes, and three register commits. Aliasing and
  out-of-domain operands remain rejected.
- `direct-output` revision 1 adds the first reviewed direct I/O transition.
  VM-owned decode and `profile_low_byte()` derive encrypted code 68, output byte
  `0xa8`, `C/D=6/8`, and `output_len=4`. Independent 642/724-byte objects prove
  the exact 9-word footprint, non-null output pointer, strict capacity guard,
  one append, and atomic misses.
- `direct-input` revision 1 completes direct coverage of all eight instruction
  families. Byte input guards a non-null pointer, strict length, and exact byte,
  then commits `A=65` and cursor 3. EOF guards length equality, accepts a null
  pointer, uses VM-owned `profile_eof_word()` for `A=4782968`, and keeps cursor
  2. Independent complete objects are 659/744 and 634/715 bytes.
- Every memory-backed direct template now compares ABI `memory_words` with the
  exact `NativeArtifactKey` IR footprint before dereference or commit.
- After program/profile/runtime admission, direct-template orchestration
  classifies IR before backend construction: exact zero-register halt selects
  the
  smallest fast path, other no-live-in halt IR selects the observation-bound
  template, graphical halt fetch and non-graphical termination select terminal
  live-in templates, exact non-aliasing jump/rotate/crazy IR plus exact
  input/output IR
  select their instruction-specific templates, exact no-op IR selects the
  memory-writing
  template, and remaining IR selects verified deopt. Unsupported direct host
  formats remain
  explicit at this narrow boundary and post-selection errors never degrade
  silently to fallback.
- `select_preflighted_execution_tier()` adds a product-neutral plan above that
  boundary. Only top-level direct format absence maps to the normative
  interpreter
  after preflight; `002`, `001`, and backend/emission/admission failures remain
  errors. It performs no cache lookup, executable memory, linking, or
  invocation.
- `select_cached_preflighted_execution_tier()` adds caller-owned direct reuse
  under
  explicit `DirectHost`. A private `PreparedDirectTarget` binds specialization
  to
  one exact key used by lookup and consumed by miss emission, avoiding a second
  IR
  canonicalization there; state-applying verifiers still reconstruct expected
  identity independently. `VerifiedDirectNativeCache` receives artifacts only
  after semantic admission, reports exact-key `Inserted`/`Hit`, matches uncached
  keys/bytes for all twelve templates, and reuses one pointer-identical
  immutable
  `Arc` allocation. Profile and host preflight occur before lookup; `002`,
  `001`,
  and interpreter selection leave cache cardinality unchanged. Exact-key
  invalidation removes one future reuse entry. Exact-program invalidation first
  constructs canonical region identity, removes every host/backend variant, and
  leaves unrelated regions unchanged; profile-invalid IR fails before mutation.
  Exact-target invalidation removes all regions sharing one complete
  OS/ISA/backend revision/native-ABI/features identity while preserving other
  targets. Outstanding `Arc` plans remain valid and reinsertion restores the
  same
  key/bytes under new allocations. `Arc` owns immutable sharing only;
  persistence,
  automatic eviction, synchronization policy, linking, executable memory,
  invocation, and performance remain outside.
- Native ABI revision 1 now has one Rust `repr(C)` authority. Its 80-byte
  call frame and every 64-bit-host field offset match the reviewed x86-64
  and AArch64 templates. Typed status/termination decoding rejects unknown
  values, and `NativeRegionCallFrame` binds pointers to borrowed slices while
  rejecting invalid capacities and cursors.
- `PreparedNativeRegionInvocation` now validates one canonical effect against
  exact caller memory, live-ins, input/EOF, output movement, and memory-write
  before-values. It snapshots complete ABI state, memory, and output, then
  admits only the exact IR-derived `Applied` result or a mutation-free
  `GuardMiss`. Unknown status, unexpected invalid arguments, topology drift,
  and partial commits fail closed. Every rejected completion restores the
  complete entry snapshot.
- `PreparedVerifiedDirectInvocation` reconstructs complete native identity from
  the program and verified artifact target, rejects canonical drift, and denies
  the deoptimization stub state-applying authority. `NativeRegionBuffers`
  groups memory/input/output loans so verified bytes, target assumptions, and
  the ABI pointer remain inseparable.
- `VerifiedDirectLoadImage` reparses verified COFF, rejects relocations, extracts
  immutable `.text` and its exact entry offset, retains complete key/target
  identity, and validates ISA alignment. Its only policy is RW staging followed
  by RX execution with mandatory instruction synchronization. Twelve templates
  across both ISAs pass exact-image evidence; machine drift and relocations fail
  closed.
- Staged, sealed, and ready executable typestates now admit explicit platform
  reports in exact RW-copy, same-mapping RX-seal, then full-code instruction
  synchronization order. Bytes, capacity, alignment, address arithmetic,
  mapping identity/range, permissions, and sync range fail closed. The ready
  state retains the exact release request; image-equal call binding is required
  before entry address and ABI state are exposed together. Twenty-four direct
  images pass the lifecycle and cross-ISA binding fails closed. Reports are
  adapter evidence only; no page allocation, permission transition, cache flush,
  cleanup operation, or foreign invocation is added.
- `NativeExecutableMemoryAdapter` now exposes exact allocate/copy/protect/sync/
  release operations to safe transactional orchestration. Allocation evidence is
  admitted before copy; returned bytes and copy identity are checked afterward.
  Every post-allocation failure attempts exact release and preserves primary
  plus cleanup diagnostics. Failed explicit release retains the ready executable
  for
  retry. A deterministic fake adapter covers 24 images, four operation failures,
  ten report drifts, cleanup failure, and exact retry. No concrete OS adapter or
  machine-code call is added.
- `NativeExecutableRunner` accepts only one exact bound executable invocation.
  `execute_verified_native()` composes load, bind, run, completion, and release.
  Load/runner failures abort the entry snapshot, completion rejection restores
  it, cleanup failure retains exact retry state, and final-release failure keeps
  the committed outcome. Six deterministic cases cover apply, guard miss, load
  short-circuit, runner mutation, completion drift, and release failure. No
  concrete unsafe call shim is added.
- `RegionEffectProgram::from_profile_step_trace()` now preserves complete
  fetch/data/encryption evidence as sorted one-step live-ins, while
  `select_verified_direct_sequence()` atomically publishes only a non-empty,
  profile-consistent, observation-continuous sequence whose every step selects
  a verified non-deopt direct artifact. A normative rotate/output trace
  produces exact x86-64/AArch64 plans; empty, discontinuous, profile-mixed,
  hidden-deopt, and post-termination shapes fail closed.
  `select_cached_verified_direct_sequence()` prepares the full sequence before
  lookup, preserves exact-hit `Arc` identity, stages verified unique misses, and
  inserts them only after complete success. Insert/replay, mixed hit/miss,
  preflight-before-lookup, and late-rejection rollback tests bind that
  transaction.
- Safe cached and uncached sequence runners now execute each exact one-step pair
  through load/bind/run/complete/release. Applied prefixes remain committed;
  guard miss returns the exact resume index/observation, and indexed failures
  preserve committed progress plus cleanup retry evidence. Rotate/output VM
  snapshots prove full completion, second-step guard resume, rollback, and
  applied/guard release failure.
- `NativeInterpreterContinuation` now validates native outcome/failure evidence
  against the complete cached or uncached plan and publishes an immutable
  semantic suffix independent of executable mappings. Complete/suffix artifact
  keys, remaining one-step programs, resume observation, expected exit/outcome,
  and guard/failure reason remain explicit. Constructors cover ephemeral and
  loaded failures. Forged counts, indices, observations, and progress fail
  closed; completed outcomes and terminal cleanup return no continuation. Eight
  cases cover every constructor family. The application handoff now resolves the
  exact canonical profile, admits either a full checkpoint or transferred
  memory/input/output-prefix state, and runs each remaining `step_traced()`
  transition through exact IR reprojection. Any machine, program, projection, or
  live-in drift returns the step-entry checkpoint and combined resume index;
  completion validates full-plan exit/outcome. Explicit budgets now yield
  completion or an affine suspension retaining cumulative progress, exact
  complete-plan resume index, artifact/program suffix, and normative checkpoint.
  Zero budget is a no-op, partial budget resumes without readmission, and
  oversized budget completes. Ten cases cover both transfer forms, admission,
  budgets, repeated zero after progress, and rollback before/after suspension.
- `application/scheduler.rs` now consumes one affine handoff under one explicit
  complete, positive-slice, caller-yield, or native-retry-yield decision. Every
  pause preserves exact checkpoint, complete-plan index, key/program suffix, and
  cumulative interpreter progress with `BudgetExhausted`, `CallerYield`, or
  `NativeRetry`. `resume()` consumes the same owner. Native retry is evidence
  only and never replans, loads, invokes, or masks hard failures. Five cases
  cover
  both yields, sliced/direct completion, cumulative progress, and failure
  propagation. Automatic tier policy, executable retry, async queues, and
  product
  orchestration remain open.
- `ReadyNativeExecutableSequence` now prederives every image and loads every
  mapping before execution. It supports repeated loaded runs without new memory
  operations,
  rolls back partial loads in reverse, and releases all mappings while retaining
  failed cleanup for retry. Complete chain identity is admitted before buffers
  change. Seven cases cover reuse, cached guard miss, rollback, aggregate
  release, cross-ISA rejection, and runner failure. The adapter owns unique
  non-overlapping live mappings.
- `NativeExecutableSequenceCache` applies positive entry limits plus optional
  live-mapping and admitted mapped-byte limits to exact ordered artifact lists.
  Candidate weight comes from ready mapping reports. Hits preserve FIFO age and
  usage without adapter work. Oversized candidates clean themselves without
  changing prior entries; candidates that fit alone evict repeatedly until all
  projected limits fit and report every evicted key. Failed later insertion
  eviction removes cache authority for that victim and earlier successful
  victims, cleans the candidate, and retains failed ownership for retry.
  Invalidation and full drain update usage before cleanup.
  `reconfigure_limits()` publishes expansion or already-satisfied requests
  without adapter operations. Shrink requests release oldest entries until
  current usage fits, while retaining previous limits until all required
  releases succeed. Failure reports all removed keys and exact release
  ownership;
  retry plus the same request publishes without repeating completed releases.
  Seventeen cases cover original reuse/admission paths plus expansion,
  entry/mapping/byte shrink, and second-eviction failure.
- `NativeExecutableSequenceLeaseCache` now shares exact ready chains through
  cloneable immutable `Arc` leases. Active lookup and retired residency are
  separate. Eviction, invalidation, and full drain retire leased entries while
  keeping their exact weighted usage charged; unleased entries release
  immediately. Lease clone/drop performs no adapter work. Explicit return or
  reconciliation releases only residents with no external owners, preserves
  still-leased FIFO entries, and returns keyed failures for exact retry. A miss
  blocked by retired weight cleans its candidate and reports exact limits,
  usage, removals, and blockers. Seven cases cover cross-thread sharing,
  weighted blockage, mixed retire/release, final-owner cleanup, lease-aware
  drain, and both retry paths. Durable/cross-process leasing, object fusion,
  direct branch chains, a concrete FFI shim, executable native retry, and
  automatic/async scheduling remain out.
- Remaining fused-region emission plus cache-aware AOT/JIT
  policy beyond verified direct process-local reuse, durable cache
  serialization/storage/eviction, executable-memory policy, invocation, and
  end-to-end tier performance remain unimplemented.
