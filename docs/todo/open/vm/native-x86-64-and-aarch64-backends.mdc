---
id: "native-x86-64-and-aarch64-backends"
status: "active"
lane: "10"
area: "vm"
task: "TODO - Native x86-64 and AArch64 backends"
# jig-ignore-next-line: canonical front matter value is indivisible
contract: "docs/technical/runtime/execution/native-x86-64-and-aarch64-backends.md"
depends_on:
  - "tiered-native-execution-engine"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/host-cpu-and-accelerator-runtime-baseline.md"
  - "docs/technical/adr/tiered-native-execution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "vm/"
  - "execution/"
  - "tests/vm/"
  - "benchmarks/interpreter/"
validation:
  - "jig validate --root ."
---
# TODO - Native x86-64 and AArch64 backends

## Objective

Implement native-code emitters for x86-64 and AArch64 behind one execution-IR
backend contract. Architecture-specific register allocation, instruction
selection, calling conventions, executable-memory handling, instruction-cache
synchronization, and hardening remain adapters rather than VM semantics.

## Acceptance

- x86-64 and AArch64 consume the same portable execution IR and independently
  pass cross-backend differential suites including executable-memory and
  instruction-cache edge cases.
- Observable state, I/O, termination, and diagnostics match the declared
  semantic profile across positive, boundary, and adversarial fixtures.
- Performance conclusions use equivalent workloads and report raw-sample
  provenance, resource budgets, dispersion/uncertainty, and failure/success
  behavior rather than only a best-case number.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `vm/`, `execution/`, `tests/vm/`,
  `benchmarks/interpreter/`.
- Required evidence: semantic fixtures, state/I/O traces where diagnostic, and
  differential results against independent specification-conformant
  implementations; the historical interpreter is compared only on its documented
  agreement domain.
- Prerequisite completion evidence: `tiered-native-execution-engine`.
- Shared prerequisite evidence includes portable IR v1 and cache identity that
  distinguishes OS, ISA, backend/native ABI revisions, required features, and
  exact canonical IR even under forced bucket-digest collisions.
- A Clang C23 bootstrap backend now emits real untrusted x86-64/AArch64 Windows
  COFF object candidates from the same portable IR. This proves the shared
  host-code boundary and target split. Safe-Rust COFF admission now verifies
  machine/entry/section identity and rejects unresolved external dependencies;
  a minimal direct emitter/verifier owns deopt-only x86-64/AArch64 stubs with
  byte-exact semantic admission, and `direct-initial-halt` now supplies the
  first
  state-applying fast path for an exact one-step zero-state halt subset.
  `direct-halt-registers` revision 5 extends that halt-only surface across
  arbitrary 32-bit registers and full 64-bit counter observations. Independent
  495/564-byte fixtures bind counters above `u32::MAX` on both ISAs; historical
  revision, counter, and opcode mismatch fail closed. `direct-halt-fetch`
  revision 2 adds a VM-decoded graphical `v` live-in with independent
  535/628-byte
  objects, x86-64 atomic execution evidence, and AArch64 instruction decoding.
  `direct-non-graphical` revision 2 applies the same fetched-terminal guards to
  a
  VM-classified non-graphical live-in with 538/631-byte objects.
  `direct-no-operation` revision 2 adds the first non-terminal code-memory write
  and pointer advance with independent 557/658-byte objects, x86-64 atomic
  execution, and AArch64 instruction decoding. `direct-jump-data` revision 1
  adds
  two distinct code/data live-in guards, an exact 125-word footprint, and the
  first instruction-specific semantic data read with independent 564/699-byte
  objects. `direct-jump-code` revision 1 adds three distinct live-in guards,
  exact post-jump encryption, a 13-word footprint, and independent 622/731-byte
  objects. `direct-rotate` revision 1 adds two guarded live-ins, two exact
  guest-memory writes, three register commits, a 9-word footprint, and
  independent
  578/732-byte objects. `direct-crazy` revision 1 uses the same reviewed
  two-read/two-write machine shape with independent VM-owned `p`/Crazy
  admission, a 9-word footprint, and independent 577/731-byte objects.
  `direct-output` revision 1 adds output-pointer and strict capacity guards, an
  exact byte append, and independent 642/724-byte objects. `direct-input`
  revision 1 adds exact byte and EOF forms with independent 659/744-byte and
  634/715-byte objects. All eight instruction families now have reviewed direct
  x86-64/AArch64 templates, and every memory-backed template guards the exact
  key-bound IR footprint.
  Native ABI revision 1 now has one format-neutral Rust `repr(C)` call frame.
  Its tested 80-byte size and field offsets match both reviewed encoder
  families; typed status/termination values and borrowed-buffer construction
  fail closed. `PreparedNativeRegionInvocation` now validates one exact effect,
  snapshots complete state/memory/output, and admits only exact application or
  a mutation-free guard miss. Unknown status, unexpected invalid arguments,
  topology drift, and partial commits fail closed. Every rejected completion
  restores the complete entry snapshot.
  `PreparedVerifiedDirectInvocation` reconstructs complete native key identity,
  rejects canonical program drift, and denies the deoptimization stub
  state-applying authority. `NativeRegionBuffers` keeps caller loans bound to
  the same verified bytes, target assumptions, and ABI pointer.
  `VerifiedDirectLoadImage` reparses verified COFF, rejects relocations, extracts
  exact immutable code and entry offset, retains full key/target identity, and
  validates ISA alignment. Its fixed policy requires RW-to-RX transition plus
  instruction synchronization. This does not link, allocate pages, change real
  permissions, flush instructions, clean up mappings, or invoke code.
  Staged, sealed, and ready typestates now admit platform reports only in exact
  RW-copy, same-mapping RX-seal, and full-code instruction-sync order. Byte,
  capacity, alignment, address, identity, permission, range, and sync drift fail
  closed. Ready mappings retain exact release requests, and only image-equal
  prepared calls expose entry address plus ABI state. All 24 direct images pass
  this protocol. Reports remain adapter evidence rather than performed OS work.
  A caller-owned platform port now orchestrates allocate, copy, RX protect,
  full-range sync, and release transactionally. It prevalidates allocation,
  verifies copy evidence, releases after every later failure, preserves cleanup
  failure alongside the primary cause, and retains ready state for release
  retry. Deterministic evidence covers all 24 images and adversarial
  operation/report
  paths. Concrete Windows/POSIX executable-memory adapters remain pending.
  `NativeExecutableRunner` now receives only the exact bound call. Safe
  orchestration composes load, bind, run, admission, and release; runner or
  completion failure restores entry state, cleanup failure retains retry state,
  and final-release failure preserves the committed outcome. Six deterministic
  runner cases pass. Concrete architecture call shims remain pending.
  Complete normative traces can now project to exact one-step IR and form one
  profile/observation-continuous `VerifiedDirectSequencePlan`; a retained
  rotate/output case selects reviewed artifacts on both ISAs, while hidden
  deopt and partial sequences fail closed. Cache-aware sequence planning
  preserves exact-hit `Arc` identity, stages verified misses, and inserts only
  after full success. Safe sequence execution now runs each exact artifact in
  order, preserves applied prefixes, and returns exact second-step guard resume
  evidence. Indexed failures retain continuation and release-retry state; VM
  snapshots cover completion, rollback, and applied/guard cleanup failure. An
  immutable semantic continuation now validates cached/uncached and
  ephemeral/loaded evidence, retaining complete/suffix artifact keys, remaining
  one-step IR, exact observation, expected exit/outcome, and reason. Forged
  outcomes fail closed, while completed work yields no continuation. `advance()`
  rebases complete-plan authority after additional admitted tier work, deriving
  an exact suffix or verified completion and rejecting drift. Eleven cases cover
  constructors and rebase behavior. The application bridge now admits checkpoint
  or transferred native buffers, restores the normative profile machine, and
  executes/reprojects every remaining traced step. Drift rolls back the current
  interpreted step and reports exact combined progress; success validates the
  original exit/outcome. Explicit budgets now suspend with cumulative progress,
  exact key/program suffix, complete-plan resume index, and normative
  checkpoint;
  zero/partial/oversized cases remain deterministic and resumable without
  readmission. Ten cases cover both ISAs, state/live-in rejection, budget
  boundaries, and rollback after resume. An explicit application scheduler now
  consumes that affine owner with complete, positive-slice, caller-yield, or
  native-retry-yield decisions. Pauses retain exact checkpoint/suffix/progress
  and
  stable stop reason; `resume()` consumes the same owner. Native retry remains
  evidence only. Five cases cover both yields, completion paths, cumulative
  progress, and hard-failure propagation. Retry admission now binds one
  caller-replanned verified suffix to the exact `NativeRetry` owner before
  buffer movement. Reason, ordered programs, artifact key, and checkpoint entry
  must match; rejection restores both owners. Five cases include progressed and
  cross-ISA suffixes. Admitted retries now execute through the uncached native
  sequence path from checkpoint-derived owned buffers. Success/failure retain
  exact transfer state, reconstruct normative checkpoints, and preserve cleanup
  ownership. Five cases cover completion, guard miss, rollback, and committed
  cleanup failure. Successful retry results now rebase through complete-plan
  continuation authority: applied suffixes yield the original outcome/final
  checkpoint, while guard miss advances absolute mixed-tier progress and returns
  a scheduler-ready normative handoff. Three cases cover pure/mixed completion
  and progressed guard fallback. Failed retry execution now splits semantic
  disposition from indexed native failure ownership. Runner failure yields exact
  fallback handoff; terminal cleanup failure may complete semantics while
  preserving release retry. Three cases cover zero/progressed fallback and
  cleanup completion. Exact retry host planning now selects the remaining
  verified sequence on supported Windows hosts and converts only missing target
  format into a normative handoff. Profile, IR, continuity, deoptimization,
  emission, and verification errors remain hard with suspension ownership. Four
  cases cover Windows routing, Linux fallback before/after progress, hard profile
  rejection, and invalid reason. An immutable attempt policy now preserves
  native retry while caller-supplied budget remains and routes exhaustion to
  complete or positive-slice normative fallback; zero limit falls back
  immediately. Four cases cover numbering, both fallback forms, and invalid
  reason. A bounded router now composes attempt policy with host planning:
  exhaustion bypasses planning, remaining budget produces a numbered native
  route, and missing format applies configured fallback without consuming an
  attempt. Five cases cover both routes and owned hard failures. One-turn
  execution now runs the selected route, rebases native success/failure, and
  separates semantic disposition from indexed runner/release ownership. Five
  cases cover both completions, guard fallback, runner failure, and cleanup
  completion. A bounded cycle now repeats only successfully rebased guard
  misses; fixed attempt policy terminates in native completion or normative
  fallback, while runner/release failures stop immediately with owners intact.
  Seven cases cover zero/format fallback, repeated guards, later native
  completion, runner/cleanup failure, and hard rejection before adapters. An
  admitted retry can now execute through an exact immutable lease without
  allocation/release, retaining hit/insertion evidence and the lease across
  semantic rebase. Five cases cover insertion, pointer-identical hit reuse,
  guard/failure fallback, and key rejection. Exact cache acquisition can now
  precede one loaded retry attempt: hits perform no adapter work, insertions
  retain FIFO retirement/release evidence, and load/runner failures preserve
  retry or lease ownership. Five cases cover insertion, hit reuse, load failure,
  mixed retirement, and runner fallback. Bounded-cycle lease reuse, adaptive
  telemetry, async queues, and broader production orchestration remain pending.
  Persistent sequence ownership now preloads all exact mappings, reuses them
  across calls, rolls back partial loads, and releases every mapping with retry
  retention. Full chain identity is checked before buffer mutation; adapters own
  unique non-overlapping live allocations. A weighted exact-key FIFO reuses
  complete loaded chains without refreshing hits. Positive limits cover entries
  and can also cover live mappings plus admitted mapped bytes. Candidate weight
  comes from ready mapping reports; oversized candidates clean themselves, while
  fitting candidates evict repeatedly until every projected limit fits. Failed
  later insertion eviction returns failed victim/candidate cleanup ownership.
  Invalidation and full drain update usage before release. Weighted limits now
  reconfigure in place: expansion or already-fitting requests publish without
  adapter work, while shrink requests release oldest entries before publishing.
  Previous limits remain active through failure; removed keys and failed release
  ownership remain explicit for retry, and a repeated request does not release
  completed victims again. Seventeen cases cover original reuse/admission plus
  expansion, entry/mapping/byte shrink, and second-eviction failure. A separate
  immutable `Arc` lease cache now lets exact ready chains remain usable across
  threads after active lookup retirement. Retired entries keep exact weighted
  usage charged until explicit return/reconciliation observes no external
  owner. Mixed eviction releases unleased entries, retires leased entries, and
  rejects candidates blocked by resident weight. Invalidation and full drain
  use the same ownership rule; keyed cleanup failures remain retryable.
  Resident limits now expand without adapter work and shrink through active FIFO
  release/retirement. Leased victims keep exact weight charged, prior retired
  entries are not reconciled implicitly, and old limits survive blockage or
  cleanup failure. Final lease return or keyed retry permits idempotent repeated
  publication. Fourteen cases cover the original lifecycle plus entry/mapping
  shrink, live entry/byte blockage, post-return publication, and release-failure
  retry. Durable or cross-process leasing, fused objects, direct branches, and a
  concrete foreign-call shim remain open.
  Remaining fused-region emission, concrete executable-memory integration, and
  instruction-cache implementation remain pending for this TODO.
- Performance evidence pending: raw measurements plus a reproducible
  scaling/statistical summary tied to exact workload and hardware/software
  identity.
