---
id: "ahead-of-execution-native-translation"
status: "pending"
lane: "11"
priority: "p2"
area: "vm"
task: "TODO - Ahead-of-execution native translation"
contract: "docs/technical/runtime/execution/ahead-of-execution-native-translation.md"
depends_on:
  - "tiered-native-execution-engine"
  - "native-x86-64-and-aarch64-backends"
  - "self-modification-state-graph-optimizer"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/host-cpu-and-accelerator-runtime-baseline.md"
  - "docs/technical/adr/tiered-native-execution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "vm/"
  - "execution/"
  - "tests/vm/"
  - "benchmarks/interpreter/"
validation:
  - "jig validate --root ."
---
# TODO - Ahead-of-execution native translation

## Objective

Make verified ahead-of-execution translation the primary native path. Use
the portable execution IR between Malbolge decode and architecture-specific code
generation, then build or load exact native artifacts before guest execution
from reachable code-state evidence. When verification proves a finite closed
self-modification graph, compile its reachable code-state variants ahead of time
and lower runtime self-modification to guarded transitions among those already
compiled variants.

Offline CPU, GPU, or superoptimization search may propose stronger reductions,
but independent deterministic verification remains the only admission authority.
Regions that cannot be proven safe fall back to ordinary VM execution.

- Process-local preparation transactionally compiles requested direct fast
  paths,
  deduplicates exact artifact keys, rejects empty/deoptimization-only or failed
  variants, and publishes only a complete sealed
  VerifiedAheadOfExecutionNativeSet.
- Runtime AOT lookup never emits on a miss and distinguishes exact hit,
  uncovered identity, and unsupported host format after profile preflight.
- Ordered direct sequences require every exact step to exist in the sealed set;
  partial AOT coverage publishes no sequence plan and mutates nothing.
- Finite exact-state graph claims are replayed through ProfileMachine before
  admission. Portable IR must reproject exactly, continued edges must land on
  the complete claimed successor checkpoint, and closed/reachable topology is
  required before all graph-node fast paths are prepared into one sealed set.
- Register-masked v6 portable IR now has transactional object-only AOT
  preparation and read-only lookup. Exact mask-aware keys are deduplicated and
  every object is independently verified before the sealed set is published.
- Verifier-admitted research regions now expose their product-owned v6 effect
  IR for an explicit handoff into ordinary native admission. Integration
  evidence proves a reviewed one-step region may prepare AOT while a verified
  multi-step
  reduction remains fail-closed until a matching native shape exists. Product
  multi-step reprojection now derives dependency masks from normative traces and
  rejects continuity/profile/outcome drift plus unrepresented derived geometry.
- Product-owned register-masked v6 graph claims now carry exact entry
  checkpoints, bounded v6 programs, and claimed successors. Runtime admission
  replays every node for its declared step budget, reprojects v6 IR, requires an
  exact successor checkpoint after budget exhaustion, and proves closed,
  reachable topology before transactional native preparation. Verified
  multi-step nodes
  remain fail-closed at native preparation until a reviewed collapsed shape
  exists.
- Product-owned dependency-reduced v6 graph evidence now has an import and
  admission boundary. Each node carries an exact witness, claimed v6 program,
  claimed reduced identity, and successor; admission normatively replays the
  witness, reprojects v6, and independently derives geometry, termination,
  memory/register live-ins, masked register values, and relative input
  observations. Duplicate reduced identities, false successor guards, open or
  unreachable topology, and any claimed-field mismatch fail closed.
- Dependency-reduced v6 graphs now have bounded AOT-first runtime dispatch.
  Each turn rechecks the current node guard, performs read-only exact AOT
  selection, delegates invocation through a caller-owned native executor port,
  and rechecks the admitted successor guard against the complete actual runtime
  exit before another precompiled node may run. Native guard miss, uncovered
  objects, unsupported reviewed shapes, host-format fallback, and transition
  budget exhaustion return an explicit lower-tier boundary without guessing;
  mutated guard misses and incompatible terminal completion fail hard.
- Dependency-reduced graph provenance now has a canonical durable codec.
  Encoding first admits the complete claim and persists only exact profile
  identity/fingerprint, witness checkpoints, step budgets, and topology. Decode
  treats every byte as untrusted, validates canonical checkpoints, normatively
  replays under a caller-owned per-node step limit, reprojects v6, derives fresh
  reduced guards, and reruns graph admission before publishing authority.
  Framing/profile/checkpoint/replay drift or replay-budget excess fails closed,
  and untrusted node counts do not drive eager allocation.
- Typed reduced-graph persistence now uses the bounded single-blob application
  service. Missing state is explicit, publication encodes before replacement,
  and restore loads under a caller-owned byte bound before decode/replay. The
  existing filesystem blob adapter supplies locked same-directory staging,
  atomic replacement, and explicit durability confirmation; storage paths stay
  caller-owned rather than entering graph semantics.
- Verified register-masked AOT objects now have single-object bounded durable
  persistence. Storage contains only canonical COFF bytes; restore derives the
  exact key from caller-supplied expected IR/runtime/host assumptions and reruns
  structural plus canonical-byte verification. Missing or corrupted objects fail
  closed, and restored artifacts may be sealed into an exact-key AOT set without
  re-emission.
- Complete ordered register-masked object sets now have atomic bounded bundle
  persistence through one canonical blob. Framing stores only version/count,
  per-object lengths, and COFF bytes; publication resolves every expected exact
  artifact before replacement, so incomplete sets cannot publish prefixes.
  Restore checks stored count against the caller-owned program sequence and
  reverifies every object under current IR/runtime/host assumptions before a set
  is returned; reordered or malformed bundles fail closed.
- Whole reduced graphs now have explicit executable residency ownership. Every
  node resolves against the sealed exact AOT set before publication; duplicate
  native keys share one mapping, late load failure rolls prior unique mappings
  back in reverse order, and failed cleanup retains retryable exact ownership.
- The first collapsed multi-step semantic shape now admits exactly a verified
  v6 no-operation followed by halt. It rechecks masks, both code-cell live-ins,
  encryption, pointer successors, effect continuity, terminal completion, and
  complete canonical v6 identity without granting host-code authority.
- A second reviewed semantic shape now admits exactly two consecutive v6
  no-operations. It independently rechecks both write masks, both code-cell
  live-ins and encryptions, exact continuity, both pointer-successor steps,
  budget-exhausted completion at two steps, and canonical v6 identity. This
  pair still has no backend/object/load/invocation authority.
- That collapsed shape now has a dedicated backend identity plus canonical
  Windows COFF for x86-64/AArch64. Independent verification reconstructs the
  semantic admission, exact v6 key/target, COFF structure, and canonical bytes.
  Its dedicated load image proves relocation closure, ISA alignment, and strict
  W^X. Typed lifecycle admission now covers exact copy, RW-to-RX, instruction
  synchronization, retryable release ownership, ABI preparation, dead-state
  rebasing, and exact synchronized-executable binding.
- A caller-owned runner port now executes only an exactly bound collapsed
  mapping, restores the complete prepared snapshot on runner failure, and
  admits completion through the existing semantic verifier.
- The persistent native process host now projects that exact bound call through
  the existing `MBNPC1` protocol, validates returned mapping/state/buffer
  evidence before semantic completion, and delegates the foreign call to the
  isolated worker. Real x86-64 POSIX-worker integration executes and releases
  the collapsed Windows-ABI object without poisoning the session.
- The no-operation pair still requires a dedicated backend/object/lifecycle/
  invocation proof before collapsed native execution; all other multi-step
  shapes stay fail-closed.

## Acceptance

- Only regions whose code-state assumptions are explicit may be compiled before
  execution, and cache keys include every assumption required for safe native
  reuse.
- A verifier-proven finite closed state graph may be materialized as precompiled
  native code-state variants with explicit transitions; no runtime mutation may
  silently select a variant absent from the admitted graph.
- A native transition may collapse multiple guest steps only when
  deterministic verification proves the complete net effect on registers,
  memory, I/O, termination, and every retained code-state dependency.
- Offline CPU/GPU/superoptimization output is untrusted proposal material until
  deterministic verification proves the claimed state effect and cache identity.
- No fixed speedup is an acceptance criterion. Benchmarks must separate offline
  preparation cost, artifact load cost, transition overhead, and steady-state
  execution from interpreter and JIT baselines.
- Observable state, I/O, termination, and diagnostics match the declared
  semantic profile across positive, boundary, and adversarial fixtures.
- Performance conclusions use equivalent workloads and report raw-sample
  provenance, resource budgets, dispersion/uncertainty, and failure/success
  behavior rather than only a best-case number.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `vm/`, `execution/`, `tests/vm/`,
  `benchmarks/interpreter/`.
- Required evidence: semantic fixtures, state/I/O traces where diagnostic, and
  differential results against independent interpreter-compatible
  implementations; the original C source is compared only where its behavior is
  defined and reproducible.
- Prerequisite completion evidence: `tiered-native-execution-engine`,
  `native-x86-64-and-aarch64-backends`,
  `self-modification-state-graph-optimizer`.
- Performance evidence pending: raw measurements plus a reproducible
  scaling/statistical summary tied to exact workload and hardware/software
  identity.
