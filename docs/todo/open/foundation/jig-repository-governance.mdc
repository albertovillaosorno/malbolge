---
id: "jig-repository-governance"
status: "active"
lane: "2"
area: "foundation"
task: "TODO - Jig repository governance"
contract: "docs/technical/contracts/jig-repository-governance.md"
depends_on:
  - "repository-responsibility-scaffold"
adr_paths:
  - "docs/technical/adr/documentation-authority-taxonomy.md"
scope:
  - "jig.toml"
  - ".dependencies/jig/"
  - "docs/todo/"
  - "TODO.md"
validation:
  - ".dependencies/jig/bin/jig.cmd validate --root ."
---
# TODO - Jig repository governance

## Objective

Integrate the evolving Jig validator as repository-local tooling, preserve its
fail-closed rules, and configure only genuine project-specific differences
without weakening unrelated linter contracts.

## Acceptance

- The repository-local Jig installation and `jig.toml` describe the actual
  polyglot repository without fake language/tool requirements or weakened
  reviewed lint policy.
- The authoritative rule/specification is deterministic, versionable, and does
  not depend on undocumented host behavior.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Repository-local Jig 26.3.1 remains installed through the source-linked
  launcher under `.dependencies/jig/bin/`; the active development authority now
  requires configuration schema 8.
- `jig.toml` schema 8 declares the repository-owned typed TODO workflow: task
  document, open/completed roots, record extension, exact sections, acceptance
  evidence, validation commands, and strict dependency-lane ordering. It also
  declares closed change-class path policy for architecture, implementation,
  bugfix, tests, documentation, and dependency admission.
- Schema 8 Git governance additionally declares `JIG_SHIT.md` as a required
  local-only QA ledger. The tracked root `.gitignore` owns `/JIG_SHIT.md`, so
  agents may record Jig integration defects without accidentally promoting that
  scratch evidence into repository history.
- The installed commit-message hook parses the schema-8 configuration and
  accepts new compliant commits fail-closed.
- `.dependencies/jig/bin/jig.cmd validate --root .` is the canonical command.
  It builds and runs the source-linked authority instead of the stale versioned
  release executable.
- The source-linked validator now parses schema 8 and reaches exhaustive policy
  evaluation. Full validation remains fail-closed on inherited history-wide
  commit-policy findings, including obsolete scopes and missing commit bodies.
- Completion remains blocked until those history findings have an explicit
  governance resolution and the repository validates under the same authority.
- Prerequisite completion evidence: `repository-responsibility-scaffold`.
