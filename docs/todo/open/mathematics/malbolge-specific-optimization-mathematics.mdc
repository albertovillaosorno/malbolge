---
id: "malbolge-specific-optimization-mathematics"
status: "active"
lane: "6"
priority: "p1"
area: "mathematics"
task: "TODO - Malbolge-specific optimization mathematics"
contract: "docs/research/algorithms/malbolge-specific-optimization-mathematics/README.md"
depends_on:
  - "latex-mathematical-specification-framework"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/verification-trust-boundary.md"
  - "docs/research/adr/research-evidence-and-algorithm-mirror.md"
scope:
  - "src/specification/formal-model/math/specification/"
  - "docs/research/algorithms/"
  - "algorithms/"
  - "tests/mathematics/"
validation:
  - "jig validate --root ."
---
# TODO - Malbolge-specific optimization mathematics

## Objective

Derive algebraic decompositions, lookup-table factorizations, state reductions,
canonical forms, and lower bounds that reduce synthesis search before brute
force or stochastic optimization begins.

**Current implementation.**

- The proved slices now formalize one hundred ninety-one exact reductions:
  classic five-trit
  crazy factorization, profile-width crazy chunking, exact zero-padded 15-trit
  crazy projection and uniform three-lookup factorization for semantic widths
  10 through 14, exact checked profile-width projection laws and a finite
  output-compatible and rotate-compatible word classifications, monotone
  checked-width source admission and exact initial-memory projection, exact
  checked-width decode compatibility and self-encryption invariance, exact
  input projection and graphical-fetch mismatch counts, finite bisimulation
  certificate
  theorem, and minimum-certified-width selector,
  exact decode sum-class
  canonicalization,
  exact classic rotate lookup, graphical self-encryption orbit
  canonicalization, classic rotate-history canonicalization,
  exact classic rotate minimal-period partition, exact
  crazy-target preimage cardinality, constructive mixed-radix preimage
  rank/unrank, exact one-trit Gray traversal of each preimage hypercube,
  exact `2^k-1` minimum aggregate trit-edit cost for bijective traversal,
  exact `Q_k` one-trit neighborhood structure with degree `k` and
  `k*2^(k-1)` edges for `k>0`, exact coordinate-permutation quotient into
  `k+1` Hamming-weight classes, exact ordered cube-word pair quotient into
  `C(k+3,3)` joint-count classes, exact ordered cube-word triple quotient into
  `C(k+7,7)` joint-count classes, exact endpoint-unordered triple quotient under
  `S_3` endpoint permutation with dense constructive rank/unrank, exact ordered
  cube-word quadruple quotient
  into `C(k+15,15)` joint-count classes, exact endpoint-unordered quadruple
  quotient under `S_4` endpoint permutation with dense constructive
  rank/unrank, exact ordered cube-word quintuple
  quotient into `C(k+31,31)` joint-count classes, exact endpoint-unordered
  quintuple quotient under `S_5` endpoint permutation with an exact
  vertex-pair/K5-edge-pair stabilizer decomposition and dense residual
  order-two, order-four, order-six, and order-twelve residual edge-orbit
  rank/unrank plus an exact order-twenty-four spoke/K4-edge stabilizer
  decomposition with complete dense order-twenty-four residual rank/unrank
  across all five spoke stabilizer shapes plus an exact order-one-hundred-twenty
  full-S5 edge cycle index/count, exact subgroup-lattice automorphism
  stabilizer spectrum, exact rooted-view multiplicity/canonical-root selector,
  exact nontrivial-stabilizer normalizer quotients, complete dense
  rank/unrank across all eight nontrivial exact-stabilizer strata, and dense
  unique-minimum-root rank/unrank for the trivial-stabilizer stratum, and
  complete dense S_5 rank/unrank across all nine stabilizer strata,
  exact
  ordered sextuple
  quotient into `C(k+63,63)` joint-count classes, exact endpoint-unordered
  sextuple quotient under `S_6` endpoint permutation with an exact mathematical
  mass-fifteen Burnside/Young-stratum extension and stabilizer-type stability
  theorem plus an exact six-vertex-pair/eleven-Young-stabilizer residual
  factorization, dense distinct-vertex
  trivial-stabilizer rank/unrank, dense order-two Young-stabilizer
  rank/unrank, dense order-six Young-stabilizer rank/unrank, dense V4 Young-
  stabilizer rank/unrank, dense S3-times-S2 Young-stabilizer rank/unrank,
  an exact nested S4 vertex-bundle/K4-edge factorization for the `(4,1,1)`
  stratum with complete dense rank/unrank composed across all five second-layer
  stabilizers, an exact `(5,1)` nested S5 factorization with dense trivial,
  S2, S3, S4, V4, and S3-times-S2 second-layer rank/unrank plus an exact
  widened full-S5 edge hard-core count and stabilizer-order spectrum plus
  dense rank/unrank for five bounded symmetric strata plus scalable
  disjoint-V4 quotient/exact rank, single-transposition S3
  quotient/free/exact rank,
  and
  exact double-transposition V4 quotient count plus quotient/exact rank/unrank,
  ordered-pattern/strict-color trivial-stabilizer rank/unrank, low-mass
  D10/full-S5 ranks, complete eleven-stratum full-S5 rank/unrank, repeated-five
  bundle composition, and complete seven-branch `(5,1)` rank/unrank, where the
  S4 branch has
  complete
  dense spoke rank/unrank across all five stabilizers and their combined
  residual interval, an exact nested S2-cubed quotient/fixed-set
  decomposition, dense S2-cubed stabilizer-chain rank/unrank, and dense
  S3-times-S3 Young-stratum rank/unrank, plus an exact S4-first descended-swap
  decomposition plus dense hard-block-first rank/unrank for the top-level
  `(4,2)` S4-times-S2 stratum, plus an exact rooted-S5/full-S6 root-deficit
  decomposition plus an exact 56-conjugacy-class S6 subgroup-lattice
  stabilizer spectrum plus dense exact full-S6-stabilizer rank/unrank for the
  remaining `(6)` stratum plus dense exact point-stabilizer S5 rank/unrank and
  dense normalizer-free single-transposition S4-quotient rank/unrank plus dense
  compact-frontier exact-transposition rank/unrank plus dense exact double-
  transposition rank/unrank with S3(3,3)/D10 sparse exclusions plus dense exact
  S2-times-S2 rank/unrank with S3-times-S2/S3-times-S3 sparse exclusions plus
  dense exact triple-transposition rank/unrank with 217 transitive-S3
  exclusions plus dense exact three-pair-V4 rank/unrank with no external
  exceptions plus dense rooted-S5 trivial-stabilizer rank/unrank plus an exact
  unique-minimum rooted selector for full-S6 trivial classes plus an exact
  pair-trivial K6-edge factor with dense rank/unrank covering over three
  quarters of the free stratum plus dense exact-transposition and
  S2-times-S2, double-transposition, and three-pair-V4 pair-graph/
  triple-breaking branches,
  an exact
  S3-extension/S4-exception count decomposition plus an
  exact S4-
  extension/point-
  S5-exception count decomposition, dense exact-S4 and exact-S3 rank/unrank,
  and an exact
  repeated-six exact-transposition outer composition,
  one generic
  checked-arity
  ordered tuple quotient with `C(k+2^m-1,2^m-1)` classes for `1<=m<=8`, dense
  stars-and-bars/combinadic rank-unrank for those quotient classes, one generic
  endpoint-unordered tuple quotient under `S_m` for `1<=m<=8`, exact combined
  coordinate/endpoint orbit masses, endpoint-minimized canonical integer keys,
  exact ordered and endpoint-unordered canonical-search budget exceedance and
  inverse minimum-coverage laws, an exact finite binary-verifier call lower
  bound, checked tuple-quotient specialization, exact all-reachable-pair
  aggregation, an exact binary-decision-tree identification lower bound, and its
  checked tuple-quotient specialization,
  exact global checked-arity ordered and endpoint-unordered tuple counts, exact
  global
  ordered-triple and endpoint-unordered triple quotient counts with dense
  global S_3 rank/unrank, exact global ordered-quadruple and endpoint-unordered
  quadruple quotient counts with dense global S_4 rank/unrank, one generic
  ambiguity-indexed ragged-domain dense-rank lift, exact ordered-quintuple and
  endpoint-unordered quintuple quotient counts with dense global S_5
  rank/unrank, exact ordered and endpoint-
  unordered sextuple quotient counts across all reachable fixed pairs, exact
  endpoint-symmetric pair quotient with
  `(C(k+3,3)+floor((k+2)^2/4))/2` classes and dense constructive rank/unrank,
  exact global endpoint-symmetric pair quotient counts and dense global
  rank/unrank across reachable fixed pairs, exact binomial distance
  shells/balls
  and
  diameter `k`, exact `j` one-trit mutation-step lower bounds with `j!`
  shortest paths, exact fixed-radius covering lower bound
  `ceil(2^k / sum_{j=0}^r C(k,j))`, exact perfect radius-one Hamming
  covers with minimum center counts `1,2,16` for `k=1,3,7`, exact perfect-
  partition dimensions `{1,3,7}` through `k=14`, exact nontrivial perfect-
  radius partition parameters through `k=14`, the tight global
  1,024-preimage bound,
  the exact zero-or-power-of-two preimage spectrum, the accumulator-specific
  `2^(10-n2(a))` worst-target bound, and the exact
  `2^(10-n2(a))*3^n2(a)` reachable-target count, the exact checked-width
  `C(N-m,k)*3^m` fixed-accumulator target distribution for preimage class
  `2^k`, and the exact `C(10,k)*2^(10-k)` accumulator-class cardinality, the
  exact global
  `7^10` reachable accumulator/target-pair count, and the exact
  `C(10,k)*2^k*5^(10-k)` nonzero preimage-pair distribution, the exact
  `C(N,k)*2^k*5^(N-k)` distribution across checked widths `1<=N<=14`, the exact
  base-seven rank/unrank bijection over all `7^N` reachable pairs, the exact
  reachable-pair mean preimage size `(9/7)^N`, the exact second moment
  `(13/7)^N` and variance `(13/7)^N-(9/7)^(2N)`, the exact
  Binomial(N, 2/7) log-preimage exponent law with mean `2N/7` and variance
  `10N/49`, the exact `9^10-7^10`
  unreachable-pair count and `9^N-7^N` generalization, the exact classic
  preimage-budget exceedance sum, its width-indexed generalization, and the
  exact minimum complete-preimage budget needed to cover any integer target
  from 0 through `7^N` reachable pairs for checked widths `1<=N<=14`. Four
  reductions are consumed by runtime paths; the one hundred one search
  reductions, two padded-geometry equivalences, and eleven width-certification
  results carry no inherited runtime speedup claim.
- One hundred ninety-one optimization research equations are registered in
  `src/specification/formal-model/math/specification/correspondence.toml`;
  exhaustive classic table tests and
  current-profile scalar/runtime fixtures provide the declared correspondence.
- Existing benchmark evidence retains 15 raw samples per scalar/table path with
  matching checksums. On the recorded host, crazy is 10.43x and rotate 1.50x
  faster by median for the admitted workload.
- This result promotes only those reductions. Broader canonical forms,
  synthesis lower bounds outside the proved finite binary-question models, and
  search-space reductions remain open and
  receive no inherited
  correctness or performance claim.

## Acceptance

- Derived canonical forms, factorizations, bounds, and algebraic reductions
  state applicability conditions and are benchmarked only after equivalence
  evidence is established.
- Definitions state domains and assumptions precisely; executable code cannot
  claim a mathematical reduction outside those stated preconditions.
- Every correctness-relevant equation or equivalence used by implementation has
  explicit domain assumptions and a traceable executable correspondence check.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `math/`, `docs/research/algorithms/`,
  `algorithms/`, `tests/mathematics/`.
- Required evidence: `.tex` definitions/derivations plus exhaustive,
  property-based, or machine-checked correspondence for the admitted domain.
- Prerequisite completion evidence:
  `latex-mathematical-specification-framework`.
- Current mathematical evidence includes one hundred eighty-seven labelled
  optimization equations,
  executable correspondence, exact zero-padding projection and uniform
  three-lookup evidence for semantic widths 10 through 14, exact checked
  profile-width projection laws, output/rotate-compatible word counts,
  source-admission monotonicity, exact initial-memory projection, exact
  decode compatibility, self-encryption invariance, input projection,
  graphical-fetch mismatch counts, finite lockstep-certificate and minimum-width
  selection evidence, raw CPU
  benchmark samples for the table paths,
  identical checksums, the exact 94-class decode quotient across all 8,836
  graphical cell/phase pairs, an exhaustive six-orbit encryption partition,
  exhaustive two-cycle classic rotate-history reduction, the exact
  `3/6/240/58,800`
  minimal-period partition, exact full-domain crazy-target
  preimage counts including impossible targets, constructive mixed-radix
  preimage rank/unrank through width 14, exact Gray traversal across all
  16,384 width-14 ambiguity masks, exact `2^k-1` minimum aggregate trit-edit
  cost for bijective traversal through dimension 14, exact `Q_k` adjacency,
  degree, edge counts, exact coordinate-permutation orbits and `k+1` canonical
  weight classes through dimension 14, exact ordered cube-word pair joint-count
  orbits with `C(k+3,3)` classes through dimension 14, exact ordered triple
  joint-count classes with `C(k+7,7)` classes through dimension 14, exact
  endpoint-unordered triple classes and dense rank/unrank under `S_3` through
  dimension 14, exact ordered quadruple joint-count classes with `C(k+15,15)`
  classes through
  dimension 14, exact endpoint-unordered quadruple classes with dense
  rank/unrank under `S_4` through dimension 14, exact ordered quintuple
  joint-count classes with `C(k+31,31)`
  classes through dimension 14, exact endpoint-unordered quintuple classes under
  `S_5` through dimension 14 with a vertex/edge-pair stabilizer factorization
  and dense order-two/order-four/order-six/order-twelve residual edge ranking
  through mass 14 plus an exact order-twenty-four spoke/K4-edge stabilizer
  decomposition with complete dense order-twenty-four residual rank/unrank
  across all five spoke stabilizer shapes plus an exact order-one-hundred-twenty
  full-S5 edge cycle index/count, exact subgroup-lattice automorphism
  stabilizer spectrum, exact rooted-view multiplicity/canonical-root selector,
  exact nontrivial-stabilizer normalizer quotients, complete dense
  rank/unrank across all eight nontrivial exact-stabilizer strata, and dense
  unique-minimum-root rank/unrank for the trivial-stabilizer stratum, and
  complete dense S_5 rank/unrank across all nine stabilizer strata,
  exact
  ordered and endpoint-unordered sextuple classes through mathematical
  dimension 15 with an exact six-vertex-pair/eleven-Young-stabilizer residual
  factorization plus
  dense distinct-vertex trivial-stabilizer, order-two, order-six, and V4
  Young-stabilizer rank/unrank, dense S3-times-S2 Young-stabilizer rank/unrank,
  plus an exact S2-cubed nested quotient/fixed-set decomposition, dense
  S2-cubed stabilizer-chain rank/unrank, and dense S3-times-S3 Young-stratum
  rank/unrank, the generic
  ordered
  tuple quotient for checked
  arities one through eight with exact weak-composition class and multinomial
  mass accounting, dense combinadic quotient-class rank/unrank through dimension
  14, generic endpoint-unordered Burnside classes for arities one through
  eight, exact combined raw orbit-mass reconstruction and endpoint-canonical
  integer keys, exact complete ordered and endpoint-unordered canonical-search
  budget exceedance and inverse minimum-coverage budgets, exact candidate-local
  binary-verifier worst-case/expected call lower bounds with checked tuple-
  quotient substitution and exact all-reachable-pair aggregation, exact binary-
  decision-tree worst/mean information lower bounds with checked tuple
  substitution, exact global checked-arity ordered and endpoint-
  unordered tuple counts, exact
  global ordered-triple and endpoint-unordered triple quotient counts with
  dense global rank/unrank through width 14, exact global ordered-quadruple and
  endpoint-unordered quadruple quotient counts with dense global rank/unrank
  through width 14, a generic ambiguity-indexed ragged-domain lift specialized
  by the proved S_2, S_3, S_4, and S_5 dense local ranks,
  exact global ordered-quintuple and endpoint-unordered quintuple quotient
  counts with dense global S_5 rank/unrank,
  exact global ordered and endpoint-unordered sextuple quotient counts through
  width 14, exact endpoint-symmetric pair classes and dense rank/unrank through
  dimension 14, their exact global counts, and dense global rank/unrank
  through width 14, binomial distance
  shells/balls, and
  diameter through
  dimension 14, exact mutation distance `j` and `j!` shortest-path counts
  through dimension 14, exact fixed-radius volume covering lower bounds through
  dimension 14 plus perfect radius-one Hamming covers for `k=1,3,7`, the
  exact radius-one dimension converse, and the complete nontrivial perfect-
  radius partition classification through `k=14`, the exhaustive tight global
  1,024-preimage ceiling, the exact `{0,1,2,4,...,1024}` cardinality spectrum,
  the accumulator-specific `2^(10-n2(a))` maximum, the exact
  `2^(10-n2(a))*3^n2(a)` reachable-target count, the exact checked-width
  `C(N-m,k)*3^m` fixed-accumulator target distribution, the complete eleven-
  class accumulator partition, the exact `7^10` reachable accumulator/target-
  pair
  count, the exact classic nonzero preimage-pair distribution, its exact
  width-1-through-14 `C(N,k)*2^k*5^(N-k)` generalization, the constructive
  base-seven canonical rank/unrank over the same `7^N` reachable domain, the
  exact width-indexed reachable-pair mean preimage size `(9/7)^N`, exact second
  moment `(13/7)^N` and variance `(13/7)^N-(9/7)^(2N)`, the exact
  Binomial(N, 2/7) log-preimage exponent law with mean `2N/7` and variance
  `10N/49`, the exact
  `9^10-7^10 = 3,204,309,152` unreachable-pair count, its exact
  width-1-through-14 `9^N-7^N` complement, and exact classic/width-indexed
  counts of reachable pairs whose full preimage set exceeds any
  nonnegative enumeration budget, the exact inverse minimum budget for every
  reachable-pair coverage target, and explicit bounded conclusions. Broader
  lower-bound/search mathematics outside the proved finite binary-question
  models remains open.
