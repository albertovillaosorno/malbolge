---
id: "malbolge-specific-optimization-mathematics"
status: "active"
lane: "6"
priority: "p1"
area: "mathematics"
task: "TODO - Malbolge-specific optimization mathematics"
contract: "docs/research/algorithms/malbolge-specific-optimization-mathematics/README.md"
depends_on:
  - "latex-mathematical-specification-framework"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/verification-trust-boundary.md"
  - "docs/research/adr/research-evidence-and-algorithm-mirror.md"
scope:
  - "src/specification/formal-model/math/specification/"
  - "docs/research/algorithms/"
  - "algorithms/"
  - "tests/mathematics/"
validation:
  - "jig validate --root ."
---
# TODO - Malbolge-specific optimization mathematics

## Objective

Derive algebraic decompositions, lookup-table factorizations, state reductions,
canonical forms, and lower bounds that reduce synthesis search before brute
force or stochastic optimization begins.

**Current implementation.**

- The proved slices now formalize fifty-eight exact reductions: classic
  five-trit
  crazy factorization, profile-width crazy chunking, exact zero-padded 15-trit
  crazy projection for semantic widths 10 through 14, exact decode sum-class
  canonicalization,
  exact classic rotate lookup, graphical self-encryption orbit
  canonicalization, classic rotate-history canonicalization,
  exact classic rotate minimal-period partition, exact
  crazy-target preimage cardinality, constructive mixed-radix preimage
  rank/unrank, exact one-trit Gray traversal of each preimage hypercube,
  exact `2^k-1` minimum aggregate trit-edit cost for bijective traversal,
  exact `Q_k` one-trit neighborhood structure with degree `k` and
  `k*2^(k-1)` edges for `k>0`, exact coordinate-permutation quotient into
  `k+1` Hamming-weight classes, exact ordered cube-word pair quotient into
  `C(k+3,3)` joint-count classes, exact ordered cube-word triple quotient into
  `C(k+7,7)` joint-count classes, exact endpoint-unordered triple quotient under
  `S_3` endpoint permutation, exact ordered cube-word quadruple quotient
  into `C(k+15,15)` joint-count classes, exact endpoint-unordered quadruple
  quotient under `S_4` endpoint permutation, exact ordered cube-word quintuple
  quotient into `C(k+31,31)` joint-count classes, exact endpoint-unordered
  quintuple quotient under `S_5` endpoint permutation, exact ordered sextuple
  quotient into `C(k+63,63)` joint-count classes, exact endpoint-unordered
  sextuple quotient under `S_6` endpoint permutation, exact global ordered-triple
  and endpoint-unordered triple quotient counts, exact global ordered-quadruple
  and endpoint-unordered quadruple quotient counts, exact ordered-quintuple and
  endpoint-unordered quintuple quotient counts, exact ordered and endpoint-
  unordered sextuple quotient counts across all reachable fixed pairs, exact
  endpoint-symmetric pair quotient with
  `(C(k+3,3)+floor((k+2)^2/4))/2` classes, exact global endpoint-symmetric pair
  quotient counts across reachable fixed pairs, exact binomial distance
  shells/balls
  and
  diameter `k`, exact `j` one-trit mutation-step lower bounds with `j!`
  shortest paths, exact fixed-radius covering lower bound
  `ceil(2^k / sum_{j=0}^r C(k,j))`, exact perfect radius-one Hamming
  covers with minimum center counts `1,2,16` for `k=1,3,7`, exact perfect-
  partition dimensions `{1,3,7}` through `k=14`, exact nontrivial perfect-
  radius partition parameters through `k=14`, the tight global
  1,024-preimage bound,
  the exact zero-or-power-of-two preimage spectrum, the accumulator-specific
  `2^(10-n2(a))` worst-target bound, and the exact
  `2^(10-n2(a))*3^n2(a)` reachable-target count, the exact checked-width
  `C(N-m,k)*3^m` fixed-accumulator target distribution for preimage class
  `2^k`, and the exact `C(10,k)*2^(10-k)` accumulator-class cardinality, the
  exact global
  `7^10` reachable accumulator/target-pair count, and the exact
  `C(10,k)*2^k*5^(10-k)` nonzero preimage-pair distribution, the exact
  `C(N,k)*2^k*5^(N-k)` distribution across checked widths `1<=N<=14`, the exact
  base-seven rank/unrank bijection over all `7^N` reachable pairs, the exact
  reachable-pair mean preimage size `(9/7)^N`, the exact second moment
  `(13/7)^N` and variance `(13/7)^N-(9/7)^(2N)`, the exact
  Binomial(N, 2/7) log-preimage exponent law with mean `2N/7` and variance
  `10N/49`, the exact `9^10-7^10`
  unreachable-pair count and `9^N-7^N` generalization, the exact classic
  preimage-budget exceedance sum, its width-indexed generalization, and the
  exact minimum complete-preimage budget needed to cover any integer target
  from 0 through `7^N` reachable pairs for checked widths `1<=N<=14`. The first
  four are consumed by runtime paths; the fifty-three search reductions and
  the zero-padding equivalence carry no inherited runtime speedup claim.
- Fifty-eight optimization research equations are registered in
  `src/specification/formal-model/math/specification/correspondence.toml`;
  exhaustive classic table tests and
  current-profile scalar/runtime fixtures provide the declared correspondence.
- Existing benchmark evidence retains 15 raw samples per scalar/table path with
  matching checksums. On the recorded host, crazy is 10.43x and rotate 1.50x
  faster by median for the admitted workload.
- This result promotes only those reductions. Broader canonical forms,
  universal synthesis lower bounds, and search-space reductions remain open and
  receive no inherited
  correctness or performance claim.

## Acceptance

- Derived canonical forms, factorizations, bounds, and algebraic reductions
  state applicability conditions and are benchmarked only after equivalence
  evidence is established.
- Definitions state domains and assumptions precisely; executable code cannot
  claim a mathematical reduction outside those stated preconditions.
- Every correctness-relevant equation or equivalence used by implementation has
  explicit domain assumptions and a traceable executable correspondence check.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `math/`, `docs/research/algorithms/`,
  `algorithms/`, `tests/mathematics/`.
- Required evidence: `.tex` definitions/derivations plus exhaustive,
  property-based, or machine-checked correspondence for the admitted domain.
- Prerequisite completion evidence:
  `latex-mathematical-specification-framework`.
- Current mathematical evidence includes fifty-eight labelled optimization
  equations,
  executable correspondence, exact zero-padding projection evidence for
  semantic widths 10 through 14, raw CPU benchmark samples for the table paths,
  identical checksums, the exact 94-class decode quotient across all 8,836
  graphical cell/phase pairs, an exhaustive six-orbit encryption partition,
  exhaustive two-cycle classic rotate-history reduction, the exact
  `3/6/240/58,800`
  minimal-period partition, exact full-domain crazy-target
  preimage counts including impossible targets, constructive mixed-radix
  preimage rank/unrank through width 14, exact Gray traversal across all
  16,384 width-14 ambiguity masks, exact `2^k-1` minimum aggregate trit-edit
  cost for bijective traversal through dimension 14, exact `Q_k` adjacency,
  degree, edge counts, exact coordinate-permutation orbits and `k+1` canonical
  weight classes through dimension 14, exact ordered cube-word pair joint-count
  orbits with `C(k+3,3)` classes through dimension 14, exact ordered triple
  joint-count classes with `C(k+7,7)` classes through dimension 14, exact
  endpoint-unordered triple classes under `S_3` through dimension 14, exact
  ordered quadruple joint-count classes with `C(k+15,15)` classes through
  dimension 14, exact endpoint-unordered quadruple classes under `S_4` through
  dimension 14, exact ordered quintuple joint-count classes with `C(k+31,31)`
  classes through dimension 14, exact endpoint-unordered quintuple classes under
  `S_5` through dimension 14, exact ordered and endpoint-unordered sextuple
  classes through dimension 14, exact global ordered-triple and endpoint-
  unordered triple quotient counts through width 14, exact global ordered-
  quadruple and endpoint-unordered quadruple quotient counts through width 14,
  exact global ordered-quintuple and endpoint-unordered quintuple quotient counts,
  exact global ordered and endpoint-unordered sextuple quotient counts through
  width 14, exact endpoint-
  symmetric pair classes through dimension 14 and their exact global counts
  through width 14, binomial distance
  shells/balls, and
  diameter through
  dimension 14, exact mutation distance `j` and `j!` shortest-path counts
  through dimension 14, exact fixed-radius volume covering lower bounds through
  dimension 14 plus perfect radius-one Hamming covers for `k=1,3,7`, the
  exact radius-one dimension converse, and the complete nontrivial perfect-
  radius partition classification through `k=14`, the exhaustive tight global
  1,024-preimage ceiling, the exact `{0,1,2,4,...,1024}` cardinality spectrum,
  the accumulator-specific `2^(10-n2(a))` maximum, the exact
  `2^(10-n2(a))*3^n2(a)` reachable-target count, the exact checked-width
  `C(N-m,k)*3^m` fixed-accumulator target distribution, the complete eleven-
  class accumulator partition, the exact `7^10` reachable accumulator/target-
  pair
  count, the exact classic nonzero preimage-pair distribution, its exact
  width-1-through-14 `C(N,k)*2^k*5^(N-k)` generalization, the constructive
  base-seven canonical rank/unrank over the same `7^N` reachable domain, the
  exact width-indexed reachable-pair mean preimage size `(9/7)^N`, exact second
  moment `(13/7)^N` and variance `(13/7)^N-(9/7)^(2N)`, the exact
  Binomial(N, 2/7) log-preimage exponent law with mean `2N/7` and variance
  `10N/49`, the exact
  `9^10-7^10 = 3,204,309,152` unreachable-pair count, its exact
  width-1-through-14 `9^N-7^N` complement, and exact classic/width-indexed
  counts of reachable pairs whose full preimage set exceeds any
  nonnegative enumeration budget, the exact inverse minimum budget for every
  reachable-pair coverage target, and explicit bounded conclusions. Broader
  universal lower-bound/search
  mathematics remains open.
