---
id: "property-fuzz-and-exhaustive-testing"
status: "active"
lane: "6"
area: "verification"
task: "TODO - Property, fuzz, and exhaustive testing"
contract: "docs/technical/verification/property-fuzz-and-exhaustive-testing.md"
depends_on:
  - "safe-rust-malbolge-vm"
  - "independent-pure-c-malbolge-vm"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "verifier/"
  - "tests/property_verification.rs"
  - "tests/differential/"
  - "tests/exhaustive/"
  - "tests/fuzz/"
  - "tests/vm/tables.rs"
validation:
  # jig-ignore-next-line: exact verification command is indivisible
  - ".dependencies/jig/source/.dependencies/rust/stable-1.97.1-x86_64-pc-windows-gnu/bin/cargo.exe test --test property_verification --all-features"
  - "jig validate --root ."
---
# TODO - Property, fuzz, and exhaustive testing

## Objective

Use property testing, fuzzing, sanitizers, regression corpora, and exhaustive
finite-domain verification for small functions and VM primitives such as rotate
and crazy operations.


**Current implementation.**

- `tests/fuzz/cases.rs` generates valid classic source by position-dependent
  decode admission from a fixed seed/ordinal; no host entropy or retry-based
  validity filter is used.
- The generator has deterministic replay and strictly progressive shrink
  candidates that reduce valid source prefixes, input, or step budget. A
  one-byte input shrinks to empty, and the minimizer skips any future
  non-progressing candidate before retesting failures and reporting replay
  identity.
- `tests/differential/classic_profile.rs` executes 24 generated programs through
  classic `Machine` and explicit profiled `malbolge-1998`, comparing every
  requested step, registers, I/O, termination, normalized encryption rejection,
  and all 59,049 final memory cells.
- `tests/exhaustive/loader_boundaries.rs` exhausts non-whitespace/non-graphical
  bytes, all 94 decode phases with valid and mutated-invalid source, recurrence
  bases, and the classic source-capacity edge.
- Existing `tests/vm/tables.rs` remains the nonduplicated exhaustive arithmetic
  baseline for rotate, crazy chunks, and positional decode tables.
- Sanitizer campaigns and deliberate verifier valid/invalid mutations remain
  open until the corresponding verifier surface exists. This TODO therefore
  remains active rather than claiming full verification closure.

## Acceptance

- Generators cover valid/invalid words, instruction positions, crazy/rotate
  arithmetic, self-modification, loader boundaries, and small-state exhaustive
  domains with deterministic shrinking/replay.
- The verifier is tested against valid cases and deliberately mutated invalid
  cases so acceptance and rejection boundaries are evidenced independently.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `verifier/`, `tests/differential/`,
  `tests/exhaustive/`, `tests/fuzz/`.
- Current evidence includes exact replay/shrink identity, seeded generated
  differential cases, exhaustive loader mutations, and existing exhaustive
  arithmetic/table checks.
- Future closure still requires sanitizer evidence and known-valid plus seeded
  invalid verifier mutations/counterexamples once the verifier exists.
- Prerequisite completion evidence: `safe-rust-malbolge-vm`,
  `independent-pure-c-malbolge-vm`.

**Migrated root planning detail.**

This detail moved from the former expanded `TODO.md` index on 2026-08-05; the
typed record is now its sole planning owner.

Use deterministic property, fuzz/replay, sanitizer, regression, and exhaustive
verification. The active Rust harness now has seed+ordinal replay/shrink, 24
classic-versus-profiled-1998 generated differential cases with full final memory
comparison, exhaustive loader byte/94-phase mutation boundaries, and the
existing
exhaustive arithmetic tables. Sanitizers and verifier valid/invalid mutation
campaigns remain open.
