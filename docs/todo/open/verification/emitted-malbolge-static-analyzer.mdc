---
id: "emitted-malbolge-static-analyzer"
status: "active"
lane: "6"
priority: "p1"
area: "verification"
task: "TODO - Emitted Malbolge static analyzer"
contract: "docs/technical/verification/emitted-malbolge-static-analyzer.md"
depends_on:
  - "safe-rust-malbolge-vm"
  - "canonical-malbolge-target-profile"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "verifier/"
  - "tests/differential/"
  - "tests/exhaustive/"
  - "tests/fuzz/"
validation:
  - "jig validate --root ."
---
# TODO - Emitted Malbolge static analyzer

## Objective

Analyze generated Malbolge for lexical and address validity, self-modification,
control-flow reachability, code/data aliasing, wraparound, dataflow, invalid
executable cells, and input-dependent cycles or hangs.

## Acceptance

- The analyzer reports profile requirements, reachable invalid executable
  states, suspicious cycles/self-modification, memory needs, and source-map
  context without executing arbitrary guest work to completion.
- The verifier is tested against valid cases and deliberately mutated invalid
  cases so acceptance and rejection boundaries are evidenced independently.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- `verifier/emitted_malbolge.py` implements the first bounded historical-profile
  slice without executing guest instructions. It reports exact C-locale
  whitespace handling, graphical-source violations, recurrence-base underflow,
  historical memory-capacity overflow, and position-dependent initial decode.
- Reports bind `malbolge-1998`, its version/capacity, the exact raw-source
  SHA-256, loaded source-word count, initial decoded cells plus original byte
  offsets, stable finding codes, and explicit analysis limits. Schema v80
  keeps the exact entry-through-fifth compatibility fields, one generic
  `bounded_continuations` trace, and nullable `bounded_exact_cycle` evidence.
  Sixteen transitions remain the default,
  while library and CLI callers may request any finite total bound from 1
  through 256.
  It publishes the bounded memory requirement for the complete trace and
  records evolved fetch/data cells, C/D aliasing, planned writes, encryption
  input/output,
  input-dependent accumulator state, halt or rejection, pointer succession, and
  wrap. The bounded memory evidence reports the minimum word footprint and exact
  addresses actually touched by the prefix, excluding merely-held future C/D
  pointers. The preserved interpreter's non-graphical `continue` ordering also
  lets the verifier prove the `b"c'"` third-step fixed-fetch cycle
  (`C=2`, `D=40`, `M[2]=29503`) without executing a guest loop. The `b"('&"`
  fixture continues through three exact jumps and proves a recurrence-backed
  fourth fixed-fetch cycle at `C=3`, `D=39`, `M[3]=29487`. The prefix transfer
  now exposes one generic next-step primitive over an explicit finite accepted
  prefix. Explicit replay first requires the historical two-word recurrence
  base. The entry opcode is independently decoded from source cell zero and the
  caller-supplied entry transition is recomputed from that derived opcode before
  replay. Every continuation record is then recomputed from the current bounded
  state before its writes can influence analysis.
  Forged entries and skipped/reordered/forged prefix records fail closed. The
  four-word `b"('&%"` fixture uses it after four continued `j`
  transitions to prove a recurrence-backed fifth fixed-fetch cycle at `C=4`,
  `D=29490`, `M[4]=29489`; schema v25 publishes that exact step and includes
  address 4 in its bounded memory evidence. Schema v25 adds exact bounded
  source-address context: fetches map loaded-source addresses to their original
  loaded position/raw byte offset/initial byte and record whether the fetched
  value still equals that byte. Schema v25 also publishes exact data-read value
  lineage for semantic `j`, `i`, `*`, and `p` operands plus exact
  self-encryption
  input lineage. These use the same loaded-source, recurrence, data-write, and
  self-encryption origins as fetch lineage. Data reads are sampled before
  current-transition writes; encryption input is sampled after any aliased data
  write and before encryption commits. The `b"(&&%M"` fixture proves transition
  4 reads evolved `M[41]=49218` from transition 2's data write, and the entry
  rotate-alias fixture proves a same-transition data write becomes encryption
  input. A companion access map applies the same source coordinates to each
  exact fetch, actual data read, planned data write, and self-encryption
  address.
  Recurrence-only addresses remain explicitly unmapped. Rejected, unresolved,
  fixed-fetch-cycle, or exact
  repeated-state
  report prefixes still emit JSON but return a nonzero CLI status. Default-16,
  explicit-32, and maximum-256
  sequential-output fixtures exercise the finite report bound; transition
  17+ is reportable under an explicit request. Requests outside 1 through
  256 fail closed before analysis. The finite transfer now also canonicalizes
  exact evolved-memory overrides and can certify a repeated concrete
  `(C,D,A,memory)` state when the accumulator is known. Schema v25 publishes
  the first such certificate with transition indices, period, registers, and
  sparse memory overrides. It also publishes one pre-transition state snapshot
  per analyzed step with C/D/A plus canonical sparse evolved-memory overrides.
  Input-dependent `A` remains explicitly null. Null cycle evidence means no
  exact repeat was proved within the selected finite trace, not that longer or
  input-dependent execution is
  acyclic. Public CLI acceptance rejects any published exact-cycle certificate;
  white-box exact-state tests cover canonical memory identity and certificate
  fields. Schema v25 also adds an opt-in exact worklist under a reviewed
  1-through-4,096 unique-state cap. The transfer module owns and enforces that
  ceiling, and the public analyzer reuses it so direct calls cannot bypass the
  reviewed bound. It branches input across all 256 byte values plus EOF, keeps
  EOF sticky, deduplicates canonical
  `(C,D,A,memory,EOF-state)` nodes, and reports closed versus truncated graph
  evidence. The worklist expands immutable initial memory once per request and
  reuses it beneath sparse evolved-memory overrides, preventing recurrence reads
  from rebuilding the same initial prefix per exact state. `input_branch_points`
  counts only real byte-plus-EOF expansions; an
  EOF-sticky input transition has one successor and is not a branch point.
  `maximum_first_seen_transition_index` advances on unique-state admission, so
  truncated queued states retain their exact first-seen depth before
  exploration.
  Schema v25 detects directed cycles only from exact known-graph edges and
  publishes one deterministic `reachable_cycle_witness`. Each witness state
  carries C/D/A, canonical sparse memory overrides, and the sticky EOF flag.
  Schema v26 adds `reachable_cycle_entry_path`, a deterministic shortest path
  from the canonical entry state to the selected cycle witness using only
  admitted known edges. A path proved before truncation remains exact for those
  edges but does not promote the unexplored frontier to closed reachability.
  Schema v27 additionally publishes one deterministic terminal witness for each
  observed terminal status. The witness records the exact terminal source state
  and shortest admitted known-edge path from canonical entry; witnesses already
  observed before truncation remain exact, while missing statuses say nothing
  about the frontier. Schema v29 adds nullable `closed_terminal_status_counts`:
  closed worklists publish the complete canonical terminal counts, including an
  empty tuple when no terminal is reachable, while truncated worklists publish
  null. Schema v30 adds nullable `closed_all_paths_terminate`: a drained exact
  finite-state worklist reports true iff the reachable graph is acyclic, false
  when a reachable directed cycle proves a nonterminating path, and null while
  any frontier remains unexplored. Schema v31 adds nullable
  `closed_all_paths_halt`: a closed acyclic graph reports true only when every
  complete terminal status is `halted`, false for rejection or cycle, and null
  under truncation. Termination therefore does not masquerade as successful
  halt. Requested worklist CLI success requires that closed all-path halt proof,
  so exact rejection-only closure cannot be masked by a shallower accepted
  prefix. Schema v32 adds nullable `frontier_state_witness` and
  `frontier_entry_path`: truncation deterministically identifies the first
  pending FIFO frontier state and its exact entry path, including an unadmitted
  first successor when the state cap blocks all successor admission. Closed
  worklists publish null, and the witness does not promote unexplored frontier
  behavior to known reachability.
  The `b"u="` input-dependent crazy fixture
  now binds its
  257 rejection terminals to a concrete reachable rejection witness, and the
  closed input-halt CLI fixture publishes a reachable halt witness. An empty
  cycle witness under truncation does not prove the unexplored frontier acyclic.
  The five-word `b"u'&%$"` fixture closes in 1,286 states after one input and
  four `j` steps, binding a six-state deterministic cycle entry path through
  `(C,D)=(0,0),(1,1),(2,40),(3,37),(4,29489),(5,29489)`. This extends checked
  input-dependent cycle depth without changing the explicit worklist ceiling.
  The 15-word `b"u'&%$#\"!~}|{zyx"` input-plus-14-jump fixture closes in
  3,856 states and binds a deterministic 16-state cycle entry path, exercising
  about 94% of the reviewed 4,096-state budget without truncation. A separate
  67-word `/ j *` plus 64-`j` fixture merges the 257 input branches after
  rotate;
  it closes in only 591 states and proves a 41-state C=0..40 cycle entry path
  without raising the cap. A separate 123-byte `/jj*` plus 119-`o` fixture
  closes in 1,012 states and proves a 124-state C=0..123 path. Transition 4
  mutates recurrence M[123] from 29,486 to 49,194; the later evolved fetch reads
  that exact value before entering a fixed non-graphical self-loop. A generated
  1,665-word restoration fixture rotates M[123] ten times total before C reaches
  it, closes at exactly 4,096 unique/explored states with no frontier, and
  proves
  a 1,666-state C=0..1665 path. Its final recurrence fetch is non-graphical; the
  source-mapped witness keeps C=0..1664 loaded and only C=1665 source-null. The
  adjacent 1,666-word image hits 4,096 unique states after 4,095 explored
  states, retains a two-state loaded frontier path through C=1665, and reports
  truncation rather than closure. A generated 1,846-word `/j*i` fixture uses
  loaded M[41]=57 to jump from C=3 to C=58 after the branch-collapsing rotate,
  bypassing mutated M[40]. It closes in 4,095 states and proves a 1,793-state
  path ending at recurrence C=1846 with non-graphical value 29,441; only that
  endpoint is source-null. The adjacent 1,847-word image hits 4,096 unique
  states after 4,095 explored states with a two-state C=1847 frontier and no
  cycle/closure promotion. A generated 3,582-word late-input fixture reaches
  input at loaded C=3579, closes at all 4,096 reviewed states, and proves a
  3,583-state C=0..3582 path. C=0..3581 remain loaded/source-mapped; recurrence
  C=3582 is source-null with non-graphical value 29,421. The adjacent
  3,583-word image reaches 4,096 unique states after 4,095 explored states and
  retains two exact C=3583 frontier states instead of promoting the unfinished
  graph to cycle or closure. The
  one-word-longer `b"u'&%$#\"!~}|{zyxw"` fixture
  reaches the maximum after
  3,840 explored states, leaves 257 exact frontier states, records first-seen
  depth 17, and remains explicitly unknown for cycle/all-path closure across the
  frontier. This remains bounded evidence rather than automatic reachability.
  Repeat-heavy branch merges do not become cycle claims. Schema v25
  additionally publishes exact SCC summary counts over only the admitted known
  graph: all SCCs, cyclic SCCs, cyclic states, and the largest cyclic SCC size.
  Entry-reachable wraparound now has an exact EOF-branch witness: the admitted
  22-word `b"u'<%$#>=<;:987654321NN"` fixture writes 59,048 into M[40],
  steers D back to 40, and wraps D to zero on transition six. A 1,544-state
  public worklist observes exactly one such wrap while remaining truncated with
  257 frontier states. This supplements the older synthetic near-boundary
  snapshot without promoting unexplored input branches to closed reachability.
  Its cycle/non-cycle verdict is checked against the independent witness search
  before publication. Under truncation unexplored outgoing edges may later merge
  SCCs, so these counts are not promoted to whole-program graph evidence. Once
  the queue drains completely, schema v25 also publishes cyclic sink-SCC counts,
  recurrent-state count, largest recurrent component size, and one deterministic
  recurrent cycle witness. Schema v28 binds that selected recurrent witness to
  `closed_recurrent_entry_path`, the shortest exact path from canonical entry.
  A synthetic A↔B cycle with an escape to C↺ proves the recurrent path targets C
  rather than the first general cycle. Truncated worklists publish all closed-
  recurrence fields, including the path, as null rather than implying closure.
  Schema v25 represents each proven non-graphical fixed fetch as an exact
  self-loop edge. The three-word `b"utO"` two-input-then-halt fixture exercises
  65,536 merge edges without a cycle, while `b"ut"` reaches a fixed-fetch
  self-loop beyond a two-transition prefix. Schema v25 also records exact
  explored-graph memory addresses/highest address/minimum words; truncated
  graphs do not promote that footprint to whole-program evidence. Schema v34
  adds explored code/data-alias transition count, committed write count and
  addresses, and self-encryption transition count and addresses. Only exact
  successor transitions contribute committed writes, so rejected planned writes
  are not mislabeled as durable self-modification. Closed worklists make this
  evidence complete for their exact reachable graph; truncation keeps it
  explored-only and the analysis-limit identities say so. Schema v35 adds the
  first FIFO-explored effective data-mutation witness with exact source state,
  shortest entry path, address, previous/planned/final values, and same-address
  self-encryption alias flag. The entry-wrap fixture proves the first event on
  its byte-1 branch changes M[40] from 29,524 to 29,523; a null witness remains
  explored-only under truncation. Schema v36 maps that first mutation witness
  back to loaded source coordinates when possible, including raw byte offset,
  initial byte, and whether the pre-write value still matches the initial byte.
  Recurrence addresses remain unmapped, and the source-map analysis-limit
  identity binds the selected worklist size/status. A 41-word admitted extension
  with two leading whitespace bytes proves loaded position 40 maps to raw offset
  42. The recurrence fixture `b"('"` touches addresses 0, 1, 2, and 41 and
  requires 42 words. Schema v37 separately counts effective committed data
  mutations and their addresses after any same-address self-encryption. Rejected
  plans and no-op writes are excluded. The entry-wrap graph has 256 such
  transitions at address 40; byte input 0 is the committed no-op branch. Schema
  v38 maps every distinct effective mutation address back to loaded position,
  raw byte offset, and initial byte when applicable, retaining recurrence
  addresses with null source coordinates. A 41-word variant with a second `*`
  reaches mutation addresses 40 and 41; with two leading whitespace bytes, 40
  maps to raw offset 42 while 41 remains recurrence-derived. The source-map
  limit now names `data-mutation-evidence` rather than only the first witness.
  Schema v39 also maps every distinct committed write address, including
  self-encryption, back to loaded source coordinates when possible. With two
  leading whitespace bytes, entry-wrap committed addresses 0 through 6 map to
  raw offsets 2 through 8 while recurrence address 40 remains unmapped. The
  source-map limit now names the broader `worklist-mutation-evidence` footprint.
  Schema v40 separates committed data-write count/addresses from self-encryption
  and publishes role-specific source maps for both. Entry-wrap has 257
  committed data-write transitions at address 40 and self-encryption addresses
  0 through 6. This preserves role identity without unsafe set-subtraction
  inference when one address participates in both classes. Schema v41 records
  exact sorted observed pre-write/final value domains for each effective
  mutation address. Entry-wrap has pre-write 29,524 and 256 distinct finals at
  address 40; the 41-word multi-mutation fixture gives recurrence address 41
  the singleton domain 29,409 to 9,803. These domains remain bounded by
  explored worklist transitions and do not claim completeness across truncation.
  Schema v42 joins
  each exact mutation value domain to loaded source coordinates where possible
  and records whether the initial source byte appears in its observed pre-write
  domain. Loaded address 40 reports true; recurrence address 41 remains unmapped
  with a null match result. Schema v43 updates the `dataflow` analysis-limit
  identity to include worklist size, closed/truncated status, and explored-only
  scope whenever bounded worklist dataflow evidence is requested. Schema v44
  publishes exact sorted per-address domains for explored instruction fetches,
  semantic data reads, and encryption inputs. Closed input-crazy retains 58
  encryption-input values at address 1; truncated entry-wrap observes 257 data
  values at address 40. Truncation keeps these domains explored-only. Schema v45
  maps those read domains back to loaded source coordinates when available and
  records whether the initial source byte is among the observed values.
  Recurrence addresses stay null-mapped. The source-map suffix is now the
  broader `worklist-value-evidence`. Schema v46 adds exact sorted domains for
  committed data-write values and self-encryption outputs. Rejected input-crazy
  plans remain absent from committed data-write domains, while entry-wrap has
  257 committed data-write values at address 40. Output domains remain
  explored-only when the worklist truncates. Schema v47 adds first
  entry-reachable evolved fetch/data-read witnesses with exact state and
  shortest entry path. The closed six-state `b"(&&$^"` graph fetches
  M[95]=9,810 from initial 29,430; the closed five-state `b"(&&%M"` graph reads
  M[41]=49,218 from initial 29,558. Schema v48 replays each witness's exact
  entry path to identify its last committed writer kind and one-based path
  transition. The evolved fetch points to data-write transition 4; the evolved
  data read points to data-write transition 2. Same-address self-encryption wins
  writer classification when it is the final committed value. Schema v49 maps
  committed data-write and self-encryption output domains to loaded source
  coordinates when available. Loaded position 40 maps to raw offset 42 and its
  write outputs exclude initial byte 122; loaded position 0 self-encrypts from
  initial 117 to output 111. Recurrence-targeted writes remain null-mapped.
  Schema v50 separately records planned data-write count, addresses, and exact
  value domains before commit validation. Closed input-crazy has 257 plans at
  address 1 spanning 58 values but no committed data writes; entry-wrap has 257
  planned writes at address 40 whose domain equals its committed domain. Schema
  v51 maps planned write domains to loaded source coordinates where available.
  Input-crazy's rejected plans map to loaded/raw position 1 and initial byte 61
  while its committed map stays empty; recurrence-targeted plans remain
  unmapped. Schema v52 counts committed final no-op data writes separately from
  effective mutations. Entry-wrap has one no-op plus 256 effective mutations at
  address 40, exactly partitioning all 257 committed data writes. Final values
  include same-address self-encryption before classification. Schema v53 adds a
  first FIFO entry-reachable final no-op witness. Entry-wrap byte 0 records
  address 40, previous/planned/final value 29,524, no encryption alias, and
  shortest pointer path `(0,0),(1,1),(2,40)`. A null witness stays explored-only
  under truncation. Schema v54 adds exact evolved fetch/data-read transition
  counts and address sets. Closed `b"(&&$^"` has one evolved fetch at 95; closed
  `b"(&&%M"` has one evolved data read at 41; entry-wrap has 256 evolved
  data reads at address 40 and no evolved fetches. Schema v55 adds exact
  changed-value
  domains: 9,810 at address 95, 49,218 at address 41, and 256 entry-wrap values
  at address 40 matching its effective-mutation final domain while excluding
  initial/no-op 29,524. Schema v56 maps each evolved-read witness entry-path C
  pointer to loaded source coordinates where available. The evolved-fetch path
  maps C=0 through 4 to raw offsets 0 through 4 and keeps recurrence C=95
  unmapped; the evolved-data path maps C=0 through 3 entirely to loaded source.
  The source-map suffix becomes `worklist-value-and-control-path-evidence`.
  Schema v57 extends exact C/source entry-path mapping to effective-mutation,
  final-no-op, and wrap witnesses. With two leading whitespace bytes, mutation
  and no-op C=0..2 map to raw offsets 2..4; wrap C=0..5 maps to 2..7. Schema
  v58 source-maps reachable-cycle, closed-recurrent, truncated-frontier, and
  status-labeled terminal entry paths. Near-cap cycle/recurrent C=15 remains
  recurrence/null while the one-word-longer frontier C=0..15 is fully loaded.
  Schema v59 adds D-pointer source coordinates to each control-path state. Short
  entry-wrap D=40 remains recurrence/null; the 41-word fixture maps D=40 to
  loaded position 40/raw offset 42/initial byte 122. Schema v60 adds the exact
  committed origin value to evolved-read last-writer provenance and requires it
  to equal the later read: 9,810 for M[95] and 49,218 for M[41]. Schema v61
  source-maps each exact pre-transition writer state. With two leading
  whitespace
  bytes, M[95]'s writer C=3 maps to raw offset 5 while D=95 remains recurrence;
  M[41]'s writer C=1 maps to raw offset 3 while D=41 remains recurrence. Schema
  v62 adds exact historical initial-memory baselines to all worklist
  value/source contexts and source-links changed-only evolved domains.
  Recurrence M[95]
  distinguishes initial 29,430 from 9,810; M[41] distinguishes 29,558 from
  49,218. Schema v63 publishes exact C/D alias address sets and first shortest
  witnesses. Two-word input graphs have 258 alias transitions at addresses 0 and
  1; with two leading whitespace bytes those map to raw offsets 2 and 3 together
  with each witness path. Schema v64 splits observed wrap counts into C, D, and
  simultaneous categories. The synthetic boundary state wraps both; the
  entry-reachable fixture has one D-only wrap before its truncated frontier.
  Schema v65 separates non-cycle repeated-state merges from cycle-closing
  repeats.
  The 41-state fixture has 257 repeated edges and 255 exact merges, with both
  shortest paths source-mapped for the first merge; the near-cap jump family has
  zero merges despite 257 repeated edges. Schema v66 publishes the complementary
  cycle-closing repeat count: merged is 255+2=257; near-cap is 0+257=257.
  Schema v67 adds the first cycle-closing repeated-edge witness and source map.
  The 41-state target is loaded C=40/source position 40; near-cap C=15 remains
  recurrence/source-null. Schema v68 source-maps reachable and closed-recurrent
  cycle bodies. In the 124-byte deep variant, body C=123 maps to loaded byte 39
  while D=243 remains recurrence/source-null. Schema v69 publishes exact
  distinct explored C/D pointer address sets and source maps. Near-cap C spans
  0..15 while
  D is `(0,1,40,121,29405)`; recurrence coordinates stay null and truncation
  keeps both domains explored-only. Schema v69 also publishes exact
  source-mapped cyclic SCC components. Known-graph components remain bounded
  under truncation;
  closed-recurrent component maps stay null until queue closure. Schema v70
  partitions explored fetches and semantic data reads into initial-value-equal
  versus evolved counts/address sets, checks both sums exactly, and source-maps
  the initial-value-equal addresses. Equality is not promoted to unmodified
  provenance; recurrence source coordinates remain null. Schema v71 applies the
  same exact partition to self-encryption inputs using changed-from-initial
  terminology. Input-crazy has 258 such inputs: entry is initial-value-equal at
  address 0, while 257 post-input branches at address 1 span 58 changed values.
  All 257 changed branches reject, so zero planned data writes commit; source
  maps preserve that value-only distinction instead of claiming evolved-memory
  provenance. Schema v72 adds the minimum entry-path state count for every
  cyclic SCC without serializing duplicate full paths. The escaping synthetic
  graph reports `(1,3)` for known cycles and `(3)` for the recurrent sink;
  near-cap reports 16 for all 257 cyclic SCCs and the deep graph reports 124 for
  both. Closed-recurrent depth counts stay null under truncation. Schema v73
  adds first C-wrap, D-wrap, and simultaneous-wrap witnesses. The synthetic
  boundary event fills all three classes but remains pathless; the real EOF
  fixture fills only D-wrap and source-maps that reachable path. Missing class
  witnesses under truncation remain explored-only absence.
  Schema v74 publishes every distinct explored wrap-transition signature with
  exact source/result C/D and role flags, then source-maps source C/D
  independently. The real D-wrap maps loaded C=5 to raw offset 7 while
  recurrence D=40 stays source-null; truncation keeps the signature set
  explored-only.
  Schema v75 explicitly reports reachable non-graphical executable fetch count,
  addresses, exact value domains, and source-linked value contexts. Closed
  `b"ut"` has 257 such fetches at recurrence `M[2]=29412`; the loaded deep
  fixture has two at position 123 with evolved invalid value 13 versus initial
  source byte/value 39. Truncation keeps these observations explored-only.
  Schema v76 adds the first exact non-graphical fetch witness plus its shortest
  source-mapped entry path. Recurrence `b"ut"` reaches it in three states; the
  loaded deep fixture reaches position 123 after 124 states and maps every C
  position 0..123 to source, linking invalid executable evidence directly to
  bounded control flow without claiming exhaustive unbounded linkage.
  Schema v77 retains every exact explored input-branch state and source-maps its
  C/D pointers. Each branch context records its exact index on the selected
  cycle, closed-recurrent, or frontier path when present. The branch-merged deep
  cycle links its initial branch to index 0 of both closed paths; the
  whitespace-prefixed truncated fixture links it to frontier index 0/raw offset
  2. Missing path indices remain bounded absence, not unbounded reachability.
  Schema v78 retains every exact unexplored frontier state at truncation,
  derives the frontier count from that deduplicated set, and source-maps every
  endpoint's C/D pointers. The over-cap fixture has 257 states split between 16
  loaded C=15 endpoints and 241 recurrence C=16 endpoints; the adjacent
  restoration case has exactly loaded C=1665 and recurrence C=1666. Frontier
  states remain unexplored boundary evidence, not inferred successor behavior.
  Schema v79 retains every exact explored terminal state grouped by status and
  source-maps each endpoint's C/D pointers. Closed `b"u="` publishes all 257
  invalid-self-encryption terminal endpoints at loaded C/D=1; closed `b"uP"`
  independently publishes all 257 halted endpoints there. Truncation retains
  only terminals actually explored before its frontier. One shortest path per
  status remains the representative terminal path witness.
  Schema v80 retains every exact explored C=D alias observation with its full
  state, address, and fetched value, and source-maps both pointer endpoints. A
  closed two-word input graph publishes 258 observations across addresses 0/1;
  first-witness paths remain one per address rather than per observation. At a
  257-state pre-terminal cap only the explored C=D=0 entry alias is retained;
  frontier alternatives remain unexplored.
  Schema v25 counts exact wrap transitions in explored worklist states and binds
  wraparound analysis limits to both selected prefix and worklist scopes. Schema
  v33 additionally publishes the first FIFO-explored wrap witness with its exact
  source state, shortest known entry path, result pointers, and separate C/D
  wrap booleans. A missing witness says only that no wrap was observed in the
  explored graph. A canonical C/D=59,048 snapshot proves one wrap to zero
  without
  claiming entry reachability. The two-word `b"u="` input-then-crazy fixture
  closes in 258 states and resolves
  every byte/EOF branch concretely. A truncated requested worklist or a proven
  known-graph cycle returns nonzero from the CLI rather than implying safe graph
  closure. This does not
  close automatic or unbounded input-dependent cycle analysis.
- Automatic unbounded/worklist control-flow reachability, broader unbounded
  dataflow/evolved-memory equivalence, control-flow/source-map linkage beyond
  the selected bounded witness set, wraparound beyond selected prefix/worklist
  evidence, and input-dependent cycles deeper than the checked 3,583-state path
  remain open. Nothing after the
  requested bound is inferred from prefix or initial-image admission.

- Expected durable artifact surface: `verifier/`, `tests/differential/`,
  `tests/exhaustive/`, `tests/fuzz/`.
- Required evidence: known-valid fixtures, seeded invalid mutations,
  counterexamples for rejected candidates, and deterministic replay.
- Prerequisite completion evidence: `safe-rust-malbolge-vm`,
  `canonical-malbolge-target-profile`.
- Current executable evidence: known-valid historical fixture, all six loader
  whitespace bytes, recurrence underflow, historical capacity overflow,
  all 8,836 graphical-byte/position decode pairs against an independent table
  anchored to the preserved historical interpreter `xlat1`, load-admission
  parity anchored to its `strchr("ji*p</vo", ...)` check, historical pointer
  assignment/wrap closure, post-step encryption-target classification anchored
  to the interpreter's `i`/`v` ordering, graphical-but-forbidden positional
  decode, byte-exact CLI/library report parity, exact second-step input, halt,
  invalid self-encryption and input-dependent-crazy fixtures, a 25-case public
  CLI differential spanning recurrence-backed entry `j`, 16 seeded invalid
  positional mutations with byte-exact replay, exact third/fourth/fifth-step
  halt or fixed-fetch-cycle evidence, default-16, explicit-32 transition-17+
  and maximum-256 sequential-output continuation evidence, bounded
  recurrence-backed memory requirements, exact
  loaded-source/raw-offset bounded
  fetch/read/write/encryption context, exact bounded fetch-value lineage through
  loaded source, recurrence, and prior data-write evidence, explicit finite
  worklist closure/truncation evidence including 41-state, 124-state, and
  reviewed-ceiling 1,666-state restoration, 1,793-state jump-over, and
  3,583-state late-input cycle paths plus source-mapped
  merge and cycle-closing-edge
  witnesses, mutation
  and
  planned/committed write
  domains, source-linked worklist value maps with recurrence initial-memory
  baselines, final no-op partition/witnesses,
  evolved fetch/data-read witnesses with value-checked last-writer provenance,
  writer-state and entry-path C/D source maps, cycle-body and aggregate explored
  C/D source maps, source-mapped cyclic SCC components with compact entry depth,
  role-specific wrap witnesses, mutation/no-op/wrap and
  graph-level C/D paths, evolved read
  transition/address counts, reachable non-graphical executable fetch domains,
  initial-value read partitions, changed encryption inputs, and changed-value
  domains, explicit bounded analysis
  limits, CLI second/third/fourth rejection status, and CLI missing-source
  failure.
