---
id: "malbolge-decompiler-and-reverse-engineering"
status: "active"
lane: "6"
area: "tools"
task: "TODO - Malbolge decompiler and reverse engineering"
contract: "docs/technical/tooling/malbolge-decompiler.md"
depends_on:
  - "canonical-malbolge-target-profile"
  - "safe-rust-malbolge-vm"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/specification-authority-and-malbolge-evolution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "tools/decompile/"
  - "src/tooling/decompiler/composition/shared.rs"
  - "src/tooling/decompiler/composition/shared.rs"
  - "tests/decompiler.rs"
  - "examples/museum/"
validation:
  # jig-ignore-next-line: exact verification command is indivisible
  - ".dependencies/jig/source/.dependencies/rust/nightly-2026-07-14-x86_64-pc-windows-gnu/bin/cargo-clippy.exe clippy --test decompiler --bin malbolge_decompile --bin museum_convert --all-features -- -D warnings"
  # jig-ignore-next-line: exact verification command is indivisible
  - ".dependencies/jig/source/.dependencies/rust/stable-1.97.1-x86_64-pc-windows-gnu/bin/cargo.exe test --test decompiler --all-features"
  - "jig validate --root ."
---
# TODO - Malbolge decompiler and reverse engineering

## Objective

Build a profile-explicit professional Malbolge reverse-engineering tool that can
emit executable readable representations, including C, without claiming to
recover unavailable original source from arbitrary self-modifying programs.
Keep museum conversion as a separate local convenience over the general tool.

## Acceptance

- Every input is admitted against an explicitly selected canonical target
  profile before rendering; no historical-interpreter bug or silent profile
  fallback changes semantics.
- The C representation preserves normative decode, crazy/rotate, jumps,
  self-encryption, EOF, I/O, pointer sequencing, termination, and atomic
  rejection while keeping memory/I/O caller-owned. Output capacity is consumed
  only by actual emission and never preallocated from the step budget.
- Initial decode views use `XLAT1` translation and never confuse the raw
  modulo-94
  residue with the decoded instruction.
- The general CLI selects profile, output representation, and output path
  explicitly, rejects duplicate semantic options, accepts help only as a sole
  argument, and keeps future readable forms from redefining decompilation as
  C-only.
- `museum_convert` performs no acquisition or redistribution: it consumes only a
  local caller-supplied historical file, selects `malbolge-1998`, and writes a
  rebuildable local view.
- Tests regress deterministic/profile-specific rendering and a normative VM
  baseline; generated C has executable Clang evidence on x86-64 and
  syntax/codegen
  evidence on AArch64.
- A richer reverse-engineering IR/control-flow/state-annotation layer is
  designed
  and validated before this TODO is retired.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact root TODO heading without losing unfinished intent.

## Evidence

- Implemented surface: `src/tooling/decompiler/application/render.rs`,
  `src/tooling/decompiler/application/cli.rs`,
  `src/tooling/decompiler/application/museum.rs`, the two thin Cargo roots, and
  `tests/decompiler.rs`.
- Current Rust evidence: eight strict tests plus Clippy with warnings denied.
- Development toolchain evidence: pinned Clang 22.1.8 emitted warning-clean
  x86-64 and AArch64 COFF objects from generated `ubO` C; the x86-64 DLL
  executed
  input `0x41` to output `A`, halt, `A=65`, `C=2`, `D=2`, one consumed byte, and
  three semantic steps.
- Prerequisite completion evidence: `canonical-malbolge-target-profile`,
  `safe-rust-malbolge-vm`.

**Migrated root planning detail.**

This detail moved from the former expanded `TODO.md` index on 2026-08-05; the typed record is now its sole planning owner.

Build a profile-explicit reverse-engineering tool that turns valid Malbolge into
readable executable representations without pretending arbitrary self-modifying
programs preserve their original C source. The first active backend emits C23
with caller-owned state/I/O and exact profile metadata; profile-declared I/O
and non-graphical behavior now render explicitly, and `ubO` currently compiles
warning-clean with pinned Clang 22.1.8 for x86-64 and AArch64, and the x86-64
artifact executes with the same input/output/register/step result as the
normative
VM. The general CLI requires an explicit representation, while `museum_convert`
is a separate local-only helper pinned to `malbolge-1998`. A richer reverse-
engineering IR and control-flow/state annotations remain open.
