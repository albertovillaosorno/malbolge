---
id: "reference-interpreter-sanitizer-harness"
status: "completed"
lane: "5"
area: "foundation"
task: "TODO - Reference interpreter sanitizer harness"
# jig-ignore-next-line: canonical front matter value is indivisible
contract: "docs/technical/verification/reference-interpreter-sanitizer-harness.md"
depends_on:
  - "historical-interpreter-legal-boundary"
  - "historical-undefined-behavior-catalogue"
  - "documentation-readiness-and-implementation-gate"
adr_paths:
  - "docs/technical/adr/specification-authority-and-malbolge-evolution.md"
  - "docs/technical/adr/verification-trust-boundary.md"
scope:
  - "src/interoperability/historical-malbolge/"
  - "tests/compatibility/"
  - "benchmarks/interpreter/"
validation:
  - "jig validate --root ."
---
# TODO - Reference interpreter sanitizer harness

## Objective

Build the historical interpreter under AddressSanitizer and
UndefinedBehaviorSanitizer where supported, preserve failing fixtures, and use
the evidence to distinguish defined interpreter semantics from host-dependent
or undefined C behavior without editing Ben's source.

## Acceptance

- The untouched historical interpreter can be built and exercised under
  supported sanitizers, and sanitizer findings are captured as fixtures without
  treating undefined behavior as normative semantics.
- The authoritative rule/specification is deterministic, versionable, and does
  not depend on undocumented host behavior.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface:
  `src/interoperability/historical-malbolge/`, `tests/compatibility/`, and
  `benchmarks/interpreter/`.
- Required evidence: reviewed authority text plus deterministic
  parser/schema/governance tests for the declared boundary.
- Prerequisite completion evidence: `historical-interpreter-legal-boundary`,
  `historical-undefined-behavior-catalogue`.
- Completion evidence recorded 2026-08-05:
  - immutable source SHA-256 is verified before every build;
  - pinned Clang 22.1.8 builds the untouched source with ASan and UBSan;
  - an external `_halloc` compatibility unit is generated only in `.temp`;
  - the clean interpreter roundtrip emits `0xA8` with no finding;
  - empty and one-word sources reproduce normalized H-003
    `address-sanitizer:heap-buffer-overflow` findings;
  - manifest, reviewed evidence, Ruff, and integration tests fail closed on
    source, schema, toolchain, or result drift.
