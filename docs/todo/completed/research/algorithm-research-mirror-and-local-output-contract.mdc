---
id: "algorithm-research-mirror-and-local-output-contract"
status: "completed"
lane: "2"
priority: "p0"
area: "research"
task: "TODO - Algorithm research mirror and local output contract"
contract: "docs/research/adr/research-evidence-and-algorithm-mirror.md"
depends_on:
  - "academic-research-methodology-and-evidence-model"
adr_paths:
  - "docs/research/adr/research-evidence-and-algorithm-mirror.md"
scope:
  - "docs/research/"
  - "algorithms/"
  - "benchmarks/research/"
  - "src/automation/repository/composition/scripts/validate/research_mirror.py"
  - "tests/test_research_mirror.py"
validation:
  # jig-ignore-next-line: exact verification command is indivisible
  - ".dependencies/python/3.14.6/Scripts/pytest-jig.cmd -c .jig/lang/python/pytest.ini tests/test_research_mirror.py"
  # jig-ignore-next-line: exact verification command is indivisible
  - ".dependencies/python/3.14.6/Scripts/python-jig.cmd src/automation/repository/composition/scripts/validate/research_mirror.py"
  - "jig validate --root ."
---
# TODO - Algorithm research mirror and local output contract

## Objective

Standardize the semantic mirror between `docs/research/algorithms/<id>/` and
`algorithms/<id>/`, including stable algorithm identity, optional
`math/algorithms/<id>.tex`, implementation/test layout, experiment
configuration, and Git-ignored local `out/`. Define the explicit exception for
ordinary product algorithms such as DOOM interoperability transformations, which
require engineering evidence but not artificial academic capsules.


**Current implementation.**

- The accepted research ADR fixes the stable two-sided identity and the product
  engineering exception.
- The repository currently contains eight exact mirrored IDs (seven research
  topics plus `template`) under `docs/research/algorithms/` and `algorithms/`.
- `src/automation/repository/composition/scripts/validate/research_mirror.py`
  fails closed on one-sided IDs, missing
  document `README.md`/`research.md`, missing executable
  `README.md`/`experiment.toml`/`tests/`, an empty mirror, or an `out/` path
  that
  Git does not actually ignore.
- The local-output check uses `git check-ignore --no-index` on a virtual probe,
  so
  an absent generated `out/` directory is still checked against real Git policy.
- `tests/test_research_mirror.py` locks the current eight IDs and deliberately
  proves documentation-only/executable-only/empty failures plus the explicit
  exclusion of the non-research `diff` and `doom` suites.
- The academic record remains responsible for question/hypothesis/evidence and
  the executable mirror for implementations/config/tests. Negative/null results
  remain governed by the repository methodology committed under the prerequisite
  research-methodology task.

## Acceptance

- A stable algorithm ID resolves unambiguously between
  `docs/research/algorithms/<id>/` and `algorithms/<id>/`, while ordinary
  product algorithms are explicitly exempt from artificial research capsules.
- The research record separates observed evidence from interpretation and
  preserves negative/null outcomes that affect the conclusion.
- The work states a falsifiable question or hypothesis, an explicit baseline,
  and an observation that would reject or materially weaken the proposed
  technique before performance conclusions are accepted.
- If executable algorithm research is required, the stable ID is mirrored under
  `docs/research/algorithms/<id>/` and `algorithms/<id>/`; ordinary product
  engineering is not forced into that mirror.
- The declared scope contains no unresolved placeholder implementation or
  undocumented workaround required for this objective to function.
- Evidence is durable enough to move this TODO to `docs/todo/completed/` and
  remove the exact TODO heading without losing unfinished intent.

## Evidence

- Expected durable artifact surface: `docs/research/`, `algorithms/`,
  `benchmarks/research/`.
- Required evidence: research question, hypotheses/baselines, source trail,
  experiment manifest, raw-output provenance, results, and threats to validity.
- Prerequisite completion evidence:
  `academic-research-methodology-and-evidence-model`.
- The mirror contract and enforcement are implemented. Each individual research
  ID still owns its bibliography, experiment identity, raw evidence,
  negative/null
  results, threats to validity, and conclusion; the mirror validator does not
  fabricate those study-specific records.
