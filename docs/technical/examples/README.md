# Examples

Versioned examples pair project-owned C source with intentionally generated
`.malbolge` output. Each example records its target profile, compiler identity,
reproducible build command, expected behavior, and verification evidence.

Transient compiler output does not belong here.
