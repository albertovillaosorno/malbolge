schema_version = 1

[study]
id = "prepared-cuda-primitive-reference-phase-profile-v1"
experiment_manifest = "benchmarks/accelerator/evidence/2026-07-28-prepared-reference-primitive-phase-profile-rtx4060/experiment.toml"
kind = "deterministic"
question = "Does exact prepared CPU-reference equality reduce validation and end-to-end primitive medians versus broadword validation?"
hypothesis = "Encoding and end-to-end medians are lower while exact equality, reference count, session counters, and complete accounting pass."
baseline = "benchmarks/accelerator/evidence/2026-07-28-prepared-cuda-primitive-phase-profile-rtx4060/"
rejection_observation = "Encoding or end-to-end median is not lower, or equality/count/session/accounting proof fails."

[workload]
identity = "classic-rotate-target-full-domain-v1"
equivalent = true

[measurement]
warmup_iterations = 1
repetitions = 15
warmup_policy = "one untimed execution using the exact prepared state"
ordering = "fixed"
stopping_rule = "exactly fifteen retained samples per measured route"
outlier_policy = "retain-all"
outlier_rule = "none"

[statistics]
center = "median"
dispersion = "min-max"
uncertainty = "observed-range"

[objectives]
metrics = ["time_ns", "phase_coverage_percent"]
tradeoff_policy = "exact CPU truth, immutable bytes, and complete visible accounting precede phase comparison"

[resources]
host = "Microsoft Windows 10.0.26200 x86-64"
accelerator = "NVIDIA GeForce RTX 4060 sm_89"
memory_mib = 8188

[raw]
path = "benchmarks/accelerator/evidence/2026-07-28-prepared-reference-primitive-phase-profile-rtx4060/raw.csv"
retained = true

[stochastic]
trial_count = 0
failed_trials = 0
seeds = []
