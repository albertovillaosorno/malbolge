schema_version = 1

[study]
id = "prepared-cuda-primitive-phase-decomposition-v1"
experiment_manifest = "benchmarks/accelerator/evidence/2026-07-28-prepared-cuda-primitive-phase-profile-rtx4060/experiment.toml"
kind = "deterministic"
question = "Which exact prepared CUDA primitive and packed-validation subphases dominate the retained full-domain route?"
hypothesis = "Named phases cover at least 95 percent of every sample while exact CPU equality, validator identity, and resident proof counters pass."
baseline = "benchmarks/accelerator/evidence/2026-07-28-broadword-packed-validation-search-phase-profile-rtx4060/"
rejection_observation = "Any sample has less than 95 percent coverage, exact CPU evidence differs, validator identity drifts, or resident proof counters change."

[workload]
identity = "classic-rotate-target-full-domain-v1"
equivalent = true

[measurement]
warmup_iterations = 1
repetitions = 15
warmup_policy = "one untimed exact prepared CUDA and neutral packed-encoding profile"
ordering = "fixed"
stopping_rule = "exactly fifteen retained full-domain profiles"
outlier_policy = "retain-all"
outlier_rule = "none"

[statistics]
center = "median"
dispersion = "min-max"
uncertainty = "observed-range"

[objectives]
metrics = ["time_ns", "phase_coverage_percent"]
tradeoff_policy = "exactness, validator identity, proof counters, and coverage precede descriptive phase attribution"

[resources]
host = "Microsoft Windows 10.0.26200 x86-64"
accelerator = "NVIDIA GeForce RTX 4060 sm_89"
memory_mib = 8188

[raw]
path = "benchmarks/accelerator/evidence/2026-07-28-prepared-cuda-primitive-phase-profile-rtx4060/raw.csv"
retained = true

[stochastic]
trial_count = 0
failed_trials = 0
seeds = []
