# Algorithm Mathematics

Each `.tex` file uses the same stable research ID as its mirror under
`docs/research/algorithms/` and `algorithms/`.

These files define equations, domains, invariants, equivalence relations, cost
models, or bounds that an experiment may rely on. Promotion to trusted compiler
behavior still requires independent correspondence and verification evidence.
