% Copyright:
%   - Copyright © 2026 Alberto Villa Osorno.
% SPDX-License-Identifier:
%   - MIT
% Confidential:
%   - false
% License-File:
%   - LICENSE-MIT
%
% Boundary-Contract:
% - Owns:
%   - The repository behavior implemented by this source file.
% - Must-Not:
%   - Bypass the contracts or authority boundaries of its owning package.
% - Allows:
%   - Inputs: values admitted by the file's public or internal interface.
%   - Outputs: deterministic values or effects declared by that interface.
%   - Side effects: only those explicitly owned by the implementation.
% - Split-When:
%   - Split when one responsibility gains an independent lifecycle.
% - Merge-When:
%   - Merge when another file owns the exact same responsibility.
% - Summary:
%   - Malbolge specific optimization mathematics module.
% - Description:
%   - Implements the responsibility summarized by this module.
% - Usage:
%   - Used through the owning package, executable, or document boundary.
% - Defaults:
%   - Invalid inputs or broken invariants fail closed.
%

%! Malbolge specific optimization mathematics module.

\documentclass{article}
\usepackage{amsmath,amssymb}
\input{../malbolge-notation.tex}
\title{Malbolge-Specific Optimization Mathematics}
\author{Malbolge Project}
\date{2026-07-27}
\begin{document}
\maketitle

\section{Scope}

This document records algebraic reductions that are already consumed by the
Malbolge runtime. A reduction is admitted only on its stated finite/profile
domain and only while its correspondence evidence remains valid.

\section{Classic crazy chunk factorization}

Let $B=3^5=243$. For classic ten-trit words write
\[
  d=d_0+B d_1,\qquad a=a_0+B a_1,
\]
with $d_0,d_1,a_0,a_1\in\{0,\ldots,B-1\}$. Because crazy is digitwise,
\begin{equation}
  \CrazyN{10}{d}{a}
    = \CrazyN{5}{d_0}{a_0}
      + B\CrazyN{5}{d_1}{a_1}.
  \label{eq:optimization-crazy-chunk}
\end{equation}
Therefore one exact $243\times243=59049$ five-trit table is sufficient for both
halves of every classic crazy operation; a $59049^2$ word-pair table is neither
required nor justified.

\section{Profile-width crazy chunk factorization}

For profile width $N$, let chunk width $k=5$, chunk index $j\geq0$, and
\[
  r_j=\min(k,N-jk).
\]
For every admitted chunk with $r_j>0$, define
\[
  d_j=\left\lfloor\frac{d}{3^{jk}}\right\rfloor\bmod3^{r_j},
  \qquad
  a_j=\left\lfloor\frac{a}{3^{jk}}\right\rfloor\bmod3^{r_j}.
\]
Digit independence gives
\begin{equation}
  \CrazyN{N}{d}{a}
    = \sum_{j=0}^{\lceil N/k\rceil-1}
      3^{jk}\left(\CrazyN{5}{d_j}{a_j}\bmod3^{r_j}\right).
  \label{eq:optimization-crazy-profile-chunks}
\end{equation}
The current 14-trit runtime therefore uses chunks $5+5+4$ while reusing the same
five-trit table. The final four-trit result is reduced modulo $3^4$; no phantom
fifteenth trit is admitted.


\section{Exact crazy-target preimage cardinality}

For one ternary accumulator digit $\alpha\in\{0,1,2\}$ and target digit
$\tau\in\{0,1,2\}$, define the data-digit multiplicity
\[
  \mu(\alpha,\tau)
    = \left|\{\delta\in\{0,1,2\}:\CrazyN{1}{\delta}{\alpha}=\tau\}\right|.
\]
For classic words write the fixed accumulator and target in trits as
$a=\sum_{i=0}^{9}a_i3^i$ and $t=\sum_{i=0}^{9}t_i3^i$. Digit independence
then gives the exact full-data-domain preimage cardinality
\begin{equation}
  \left|\{d\in\WordDomain{10}:\CrazyN{10}{d}{a}=t\}\right|
    = \prod_{i=0}^{9}\mu(a_i,t_i).
  \label{eq:optimization-crazy-preimage-cardinality}
\end{equation}
This is an exact finite search bound only for the complete classic data-word
domain under one fixed accumulator. It does not claim that an arbitrary corpus
contains every preimage or contains each data word only once. In particular,
for $a=0$ and the all-one-trit target $t=\sum_{i=0}^{9}3^i$, every digit has
$\mu(0,1)=2$, so the exact full-domain cardinality is $2^{10}=1024$. If any
factor is zero, no classic data word can reach the target for that accumulator.

The same digit independence gives a constructive inverse canonical form at
checked widths $1\leq N\leq14$. For a reachable fixed pair $(a,t)$ define the
ordered local inverse set
\[
  D_i(a,t)=\{\delta\in\{0,1,2\}:\CrazyN{1}{\delta}{a_i}=t_i\},
  \qquad r_i=|D_i(a,t)|\in\{1,2\}.
\]
Let $q_i(\delta)$ be the zero-based index of $\delta$ in the increasing order
of $D_i$, and let $R_i=\prod_{h=0}^{i-1}r_h$ with $R_0=1$. Then
\begin{equation}
  \operatorname{rank}_{a,t}(d)
    =\sum_{i=0}^{N-1}q_i(d_i)R_i,
  \qquad
  \{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t\}
    \overset{\sim}{\longrightarrow}
  \left\{0,\ldots,\prod_{i=0}^{N-1}r_i-1\right\}.
  \label{eq:optimization-crazy-profile-preimage-ranking}
\end{equation}
The inverse decodes the rank in the mixed radices $r_0,\ldots,r_{N-1}$ and
selects the corresponding element of each $D_i$. If exactly $k$ local inverse
sets are doubletons, the canonical domain has exactly $2^k$ ranks. A procedure
whose contract is complete-domain preimage enumeration may therefore construct
exactly those $2^k$ data words without testing any of the other $3^N-2^k$
words. This is a finite search-space identity, not a claim about membership in
an incomplete external corpus or about wall-clock speed.

The same doubleton positions form an exact binary hypercube. Let
$J=\{j_0,\ldots,j_{k-1}\}$ contain the positions with $r_i=2$, let
$H_3(x,y)=|\{i:x_i\neq y_i\}|$ be trit Hamming distance, and define
$U_{a,t}(b)$ by selecting bit $b_h$ from the ordered doubleton $D_{j_h}$ while
holding every singleton position fixed. For the binary-reflected Gray code
$G_k(r)=\operatorname{bits}_k(r\mathbin{\oplus}\lfloor r/2\rfloor)$,
\begin{equation}
  U_{a,t}:\{0,1\}^k
    \overset{\sim}{\longrightarrow}
    \{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t\},
  \qquad
  H_3\!\left(U_{a,t}(G_k(r)),U_{a,t}(G_k(r+1))\right)=1,
  \quad 0\leq r<2^k-1.
  \label{eq:optimization-crazy-profile-preimage-gray-enumeration}
\end{equation}
Thus a complete-domain preimage traversal can visit all $2^k$ compatible data
words exactly once with one trit change between consecutive candidates and no
visits to the other $3^N-2^k$ words. This is an exact traversal identity for
reachable fixed pairs at checked widths $1\leq N\leq14$; it does not assert
external-corpus membership or a wall-clock speedup.

This Gray traversal is also edit-optimal under trit Hamming distance. For any
bijection
\[
  \pi:\{0,\ldots,2^k-1\}
    \overset{\sim}{\longrightarrow}
    \{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t\},
\]
define its aggregate trit-edit cost by
$E_3(\pi)=\sum_{r=0}^{2^k-2}H_3(\pi(r),\pi(r+1))$. Consecutive values of a
bijection are distinct, so every summand is at least one. The Gray bijection
$\pi_G(r)=U_{a,t}(G_k(r))$ attains equality term by term, hence
\begin{equation}
  \min_{\pi}E_3(\pi)=2^k-1,
  \qquad
  E_3(\pi_G)=2^k-1,
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-gray-optimality}
\end{equation}
The zero-dimensional case has one preimage and zero edits. This is an exact
minimum only for bijective complete-domain preimage traversal under trit Hamming
cost; it is not a wall-clock lower bound and does not constrain incomplete or
revisiting external-corpus searches.

The same cube coordinates identify the exact one-trit mutation graph. For a
reachable fixed pair, let $G_{a,t}$ have the compatible data words as vertices
and an edge $\{x,y\}$ exactly when $H_3(x,y)=1$. Under $U_{a,t}$, two vertices
are adjacent exactly when their binary coordinates differ in one bit, so
$G_{a,t}\cong Q_k$. Consequently every compatible word has degree $k$ and
\begin{equation}
  \deg_{G_{a,t}}(d)=k,\qquad
  |E(G_{a,t})|=
  \begin{cases}
    0, & k=0,\\
    k2^{k-1}, & 1\leq k\leq N,
  \end{cases}
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-graph}
\end{equation}
Thus a fixed-pair local search can generate every valid one-trit neighbor by
flipping one ambiguous cube coordinate rather than scanning unrelated data
words. This is an exact complete-domain neighborhood identity; it does not
assert external-corpus membership or a wall-clock speedup.

The full permutation group on the $k$ ambiguous coordinates gives an exact
canonical quotient for coordinate-symmetric analyses. Let $S_k$ act on
$z\in\{0,1\}^k$ by permuting coordinates, let $|z|_1$ be Hamming weight, and
let $c_k(j)$ be the word whose first $j$ coordinates are one and whose remaining
coordinates are zero. Coordinate permutations preserve weight, while any two
words of equal weight can be permuted to the same $c_k(j)$. Therefore
\begin{equation}
  \{0,1\}^k/S_k \overset{\sim}{\longrightarrow} \{0,\ldots,k\},
  \qquad [z]\longmapsto |z|_1,
  \qquad |[z]|=\binom{k}{|z|_1},
  \qquad c_k(j)=1^j0^{k-j},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-permutation-quotient}
\end{equation}
Transporting this action through $U_{a,t}$ gives exactly $k+1$ abstract
preimage-cube orbit representatives instead of $2^k$ cube words. This quotient
is valid only for an analysis or objective invariant under arbitrary relabeling
of the ambiguous coordinates; it is not semantic equivalence for
position-sensitive Malbolge execution, numeric word value, layout, or code.

The same coordinate action also gives an exact canonical quotient for ordered
cube-word pairs. For $x,y\in\{0,1\}^k$, define
$n_{uv}(x,y)=|\{h:(x_h,y_h)=(u,v)\}|$ for $u,v\in\{0,1\}$. Let the same
$\sigma\in S_k$ act simultaneously on both pair components, and let
\[
  \mathcal{J}_k=\left\{(n_{00},n_{01},n_{10},n_{11})\in
    \{0,\ldots,k\}^4:\sum_{u,v}n_{uv}=k\right\}.
\]
Simultaneous coordinate permutations preserve these four counts. Conversely,
grouping coordinates into the joint blocks $11,10,01,00$ maps every ordered
pair with the same count vector to one canonical representative. There are
$\binom{k+3}{3}$ nonnegative four-part count vectors summing to $k$. For fixed
counts, choosing which coordinates occupy the four blocks gives exactly
$k!/(n_{00}!n_{01}!n_{10}!n_{11}!)$ ordered pairs in that orbit. Therefore
\begin{equation}
  \left(\{0,1\}^k\times\{0,1\}^k\right)/S_k
    \overset{\sim}{\longrightarrow}\mathcal{J}_k,
  \qquad [(x,y)]\longmapsto(n_{00},n_{01},n_{10},n_{11}),
  \qquad |\mathcal{J}_k|=\binom{k+3}{3},
  \qquad |[(x,y)]|=\frac{k!}{n_{00}!n_{01}!n_{10}!n_{11}!},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-ordered-pair-quotient}
\end{equation}
Writing $w_x=|x|_1$, $w_y=|y|_1$, and
$d=|\{h:x_h\neq y_h\}|$ gives
$n_{00}=k-(w_x+w_y+d)/2$, $n_{01}=(-w_x+w_y+d)/2$,
$n_{10}=(w_x-w_y+d)/2$, and $n_{11}=(w_x+w_y-d)/2$. Thus the familiar triple
$(w_x,w_y,d)$ is an equivalent complete orbit key. The chosen block order gives
$c_x=1^{n_{11}+n_{10}}0^{n_{01}+n_{00}}$ and
$c_y=1^{n_{11}}0^{n_{10}}1^{n_{01}}0^{n_{00}}$, agreeing with $c_k$ on
fixed-zero and diagonal pair slices. Transport through $U_{a,t}$ gives exactly
$\binom{k+3}{3}$ canonical ordered cube-word pairs instead of $4^k$ abstract
ordered pairs. Endpoint order remains visible because $n_{01}$ and $n_{10}$ are
distinct, and $H_3(U_{a,t}(x),U_{a,t}(y))=n_{01}+n_{10}$. The quotient applies
only when an ordered-pair analysis is invariant under simultaneous
ambiguity-coordinate relabeling; it is not position-sensitive Malbolge semantic
equivalence or a wall-clock speedup claim.

The same coordinate action extends to ordered cube-word triples. For
$x,y,z\in\{0,1\}^k$, let $n_{uvw}$ count coordinates with joint pattern
$(x_h,y_h,z_h)=(u,v,w)$, and let $S_k$ act simultaneously on all three words.
Define
\[
  \mathcal{J}^{(3)}_k=\left\{(n_s)_{s\in\{0,1\}^3}:
    n_s\in\{0,\ldots,k\},\ \sum_s n_s=k\right\}.
\]
The eight counts are complete orbit invariants: grouping coordinates in
descending binary block order $111,110,\ldots,000$ gives one canonical triple
per count vector. Therefore
\begin{equation}
  \left(\{0,1\}^k\right)^3/S_k
    \overset{\sim}{\longrightarrow}\mathcal{J}^{(3)}_k,
  \qquad
  |\mathcal{J}^{(3)}_k|=\binom{k+7}{7},
  \qquad
  |[(x,y,z)]|=\frac{k!}{\prod_s n_s!},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-ordered-triple-quotient}
\end{equation}
At $k=14$, this gives 116280 classes instead of $8^{14}=4398046511104$ raw
ordered triples. The descending block convention reduces exactly to the
existing ordered-pair
representative when the third word is zero, and to the single-word Hamming-
weight representative when the second and third words are zero. The quotient
applies only to triple analyses invariant under simultaneous ambiguity-
coordinate relabeling; it is not position-sensitive Malbolge semantic
equivalence or a wall-clock speedup claim.

If triple endpoint order is also irrelevant, let $S_3$ permute the three words
and hence the eight labels of a joint-count vector. A transposition has four
fixed labels and two two-cycles, while a three-cycle has two fixed labels and
two three-cycles. Define
\[
  \tau_k=\sum_{b=0}^{\lfloor k/2\rfloor}
    \binom{k-2b+3}{3}(b+1),
  \qquad
  \chi_k=\sum_{b=0}^{\lfloor k/3\rfloor}(k-3b+1)(b+1).
\]
These are respectively the numbers of coordinate-quotient classes fixed by one
endpoint transposition and one endpoint three-cycle. Burnside's lemma gives
\begin{equation}
  \left|\mathcal{J}^{(3)}_k/S_3\right|
    =\frac{1}{6}\left(\binom{k+7}{7}+3\tau_k+2\chi_k\right),
  \qquad
  \left|[(x,y,z)]_{S_k\times S_3}\right|
    =\frac{k!}{\prod_s n_s!}\,\left|S_3\!\cdot(n_s)_s\right|,
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-unordered-triple-quotient}
\end{equation}
Lexicographically minimizing the joint-count vector over its six endpoint
permutations and then applying the existing descending-binary block convention
gives one canonical unordered triple per orbit. At $k=14$, the quotient has
21323 classes instead of 116280 coordinate-only ordered classes or
$8^{14}=4398046511104$ raw ordered triples. This refinement applies only to
triple analyses invariant under endpoint permutation as well as simultaneous
ambiguity-coordinate relabeling, and it makes no timing claim.

The same action extends once more to ordered cube-word quadruples. For
$w,x,y,z\in\{0,1\}^k$, let $n_s$ count coordinates whose four-bit joint
pattern is $s\in\{0,1\}^4$, and let $S_k$ act simultaneously on all four
words. Define
\[
  \mathcal{J}^{(4)}_k=\left\{(n_s)_{s\in\{0,1\}^4}:
    n_s\in\{0,\ldots,k\},\ \sum_s n_s=k\right\}.
\]
The sixteen counts are complete orbit invariants, with descending binary block
order $1111,1110,\ldots,0000$ giving one canonical quadruple per count vector.
Therefore
\begin{equation}
  \left(\{0,1\}^k\right)^4/S_k
    \overset{\sim}{\longrightarrow}\mathcal{J}^{(4)}_k,
  \qquad
  |\mathcal{J}^{(4)}_k|=\binom{k+15}{15},
  \qquad
  |[(w,x,y,z)]|=\frac{k!}{\prod_s n_s!},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-ordered-quadruple-quotient}
\end{equation}
At $k=14$, this gives 77558760 classes instead of
$16^{14}=72057594037927936$ raw ordered quadruples. The descending block
convention reduces exactly to the ordered-triple representative when the fourth
word is zero. The quotient applies only to quadruple analyses invariant under
simultaneous ambiguity-coordinate relabeling; it is not position-sensitive
Malbolge semantic equivalence or a wall-clock speedup claim.

If quadruple endpoint order is also irrelevant, let $S_4$ permute the four
words and hence the sixteen joint-pattern labels. For the endpoint cycle types
$1^4$, $1^2 2$, $2^2$, $1\,3$, and $4$, define the fixed-class counts
\[
  \begin{aligned}
    A_k&=[x^k](1-x)^{-16},&
    B_k&=[x^k](1-x)^{-8}(1-x^2)^{-4},\\
    D_k&=[x^k](1-x)^{-4}(1-x^2)^{-6},&
    C_k&=[x^k](1-x)^{-4}(1-x^3)^{-4},\\
    Q_k&=[x^k](1-x)^{-2}(1-x^2)^{-1}(1-x^4)^{-3}.
  \end{aligned}
\]
The induced label cycles are respectively $1^{16}$, $1^8 2^4$, $1^4 2^6$,
$1^4 3^4$, and $1^2 2 4^3$, while the five endpoint conjugacy classes have
sizes $1,6,3,8,6$. Burnside's lemma therefore gives
\begin{equation}
  \left|\mathcal{J}^{(4)}_k/S_4\right|
    =\frac{A_k+6B_k+3D_k+8C_k+6Q_k}{24},
  \qquad
  \left|[(w,x,y,z)]_{S_k\times S_4}\right|
    =\frac{k!}{\prod_s n_s!}\,\left|S_4\!\cdot(n_s)_s\right|,
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-unordered-quadruple-quotient}
\end{equation}
Lexicographically minimizing the joint-count vector over its twenty-four
endpoint permutations and then using the existing descending-binary block order
gives one canonical unordered quadruple per orbit. At $k=14$, the five fixed
counts are 77558760, 722696, 43472, 5256, and 308, yielding 3419552 classes
instead of 77558760 coordinate-only ordered classes or
$16^{14}=72057594037927936$ raw ordered quadruples. This refinement applies only
to quadruple analyses invariant under endpoint permutation as well as
simultaneous ambiguity-coordinate relabeling, and it makes no timing claim.

The same action extends to ordered cube-word quintuples. For
$v,w,x,y,z\in\{0,1\}^k$, let $n_s$ count coordinates whose five-bit joint
pattern is $s\in\{0,1\}^5$, with simultaneous $S_k$ action on all five words.
Define
\[
  \mathcal{J}^{(5)}_k=\left\{(n_s)_{s\in\{0,1\}^5}:
    n_s\in\{0,\ldots,k\},\ \sum_s n_s=k\right\}.
\]
The thirty-two counts are complete orbit invariants, and descending binary
blocks give one canonical quintuple per count vector. Thus
\begin{equation}
  \left(\{0,1\}^k\right)^5/S_k
    \overset{\sim}{\longrightarrow}\mathcal{J}^{(5)}_k,
  \qquad
  |\mathcal{J}^{(5)}_k|=\binom{k+31}{31},
  \qquad
  |[(v,w,x,y,z)]|=\frac{k!}{\prod_s n_s!},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-ordered-quintuple-quotient}
\end{equation}
At $k=14$, this gives 166871334960 classes instead of
$32^{14}=1180591620717411303424$ raw ordered quintuples. The descending block
convention reduces exactly to the ordered-quadruple representative when the
fifth word is zero. This quotient applies only to quintuple analyses invariant
under simultaneous ambiguity-coordinate relabeling; it is not position-
sensitive Malbolge semantic equivalence or a wall-clock speedup claim.

If quintuple endpoint order is also irrelevant, let $S_5$ permute the five
words and hence the thirty-two joint-pattern labels. For the endpoint cycle
types $1^5$, $1^3 2$, $1^2 3$, $1\,2^2$, $1\,4$, $2\,3$, and $5$, define
\[
  \begin{aligned}
    A_k&=[x^k](1-x)^{-32},\\
    B_k&=[x^k](1-x)^{-16}(1-x^2)^{-8},\\
    C_k&=[x^k](1-x)^{-8}(1-x^3)^{-8},\\
    D_k&=[x^k](1-x)^{-8}(1-x^2)^{-12},\\
    E_k&=[x^k](1-x)^{-4}(1-x^2)^{-2}(1-x^4)^{-6},\\
    F_k&=[x^k](1-x)^{-4}(1-x^2)^{-2}(1-x^3)^{-4}(1-x^6)^{-2},\\
    G_k&=[x^k](1-x)^{-2}(1-x^5)^{-6}.
  \end{aligned}
\]
The induced label cycles are respectively $1^{32}$, $1^{16}2^8$, $1^8 3^8$,
$1^8 2^{12}$, $1^4 2^2 4^6$, $1^4 2^2 3^4 6^2$, and $1^2 5^6$, while the
seven endpoint conjugacy classes have sizes $1,10,20,15,30,20,24$.
Burnside's lemma therefore gives
\begin{equation}
  \left|\mathcal{J}^{(5)}_k/S_5\right|
    =\frac{A_k+10B_k+20C_k+15D_k+30E_k+20F_k+24G_k}{120},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-unordered-quintuple-quotient}
\end{equation}
Lexicographically minimizing the joint-count vector over all endpoint
permutations gives one canonical unordered quintuple per orbit. At $k=14$, the
seven fixed counts are 166871334960, 414356400, 709452, 8841360, 14820, 19380,
and 180, yielding 1426354541 classes instead of 166871334960 coordinate-only
ordered classes or $32^{14}=1180591620717411303424$ raw ordered quintuples.
This refinement applies only to analyses invariant under endpoint permutation
and simultaneous ambiguity-coordinate relabeling, and it makes no timing claim.

The same coordinate action extends to ordered cube-word sextuples. For six
words in $\{0,1\}^k$, let $n_s$ count coordinates whose six-bit joint pattern
is $s\in\{0,1\}^6$. The sixty-four joint counts are complete orbit invariants,
so
\begin{equation}
  \left(\{0,1\}^k\right)^6/S_k
    \overset{\sim}{\longrightarrow}\mathcal{J}^{(6)}_k,
  \qquad
  |\mathcal{J}^{(6)}_k|=\binom{k+63}{63},
  \qquad
  |[(u,v,w,x,y,z)]|=\frac{k!}{\prod_s n_s!},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-ordered-sextuple-quotient}
\end{equation}
At $k=14$, this gives 839983521106400 coordinate classes instead of
$64^{14}=19342813113834066795298816$ raw ordered sextuples. This quotient
applies only to analyses invariant under simultaneous ambiguity-coordinate
relabeling and makes no timing claim.

If sextuple endpoint order is also irrelevant, $S_6$ acts on the sixty-four
joint-pattern labels. Its eleven endpoint conjugacy classes induce the following
label-cycle structures:
\[
\begin{array}{c|r|c}
\lambda&w_\lambda&\text{induced label cycles}\\ \hline
1^6&1&1^{64}\\
1^4 2&15&1^{32}2^{16}\\
1^3 3&40&1^{16}3^{16}\\
1^2 2^2&45&1^{16}2^{24}\\
1^2 4&90&1^8 2^4 4^{12}\\
1\,2\,3&120&1^8 2^4 3^8 6^4\\
1\,5&144&1^4 5^{12}\\
2^3&15&1^8 2^{28}\\
2\,4&90&1^4 2^6 4^{12}\\
3^2&40&1^4 3^{20}\\
6&120&1^2 2\,3^2 6^9
\end{array}
\]
For one row $\lambda$, let $m_{\lambda,c}$ be the multiplicity of a label cycle
of length $c$ and set
\[
  A_{\lambda,k}=[x^k]\prod_c(1-x^c)^{-m_{\lambda,c}}.
\]
Burnside's lemma gives
\begin{equation}
  \left|\mathcal{J}^{(6)}_k/S_6\right|
    =\frac{1}{720}\sum_\lambda w_\lambda A_{\lambda,k},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-unordered-sextuple-quotient}
\end{equation}
At $k=14$, the eleven fixed counts are 839983521106400, 624814559200,
281038424, 4150120800, 1846872, 2700856, 6050, 201249216, 171120, 217400,
and 1020, yielding 1179940653635 endpoint-unordered classes. The evidence
exhausts the coordinate quotient through dimension two and reconstructs the raw
$64^k$ orbit mass there; Burnside arithmetic reaches dimension fourteen. This
refinement applies only when endpoint order is irrelevant and makes no timing
claim.

For pairwise analyses that also ignore endpoint order, let
$G_k=S_k\times C_2$, where $S_k$ permutes both endpoints simultaneously and
$\tau\in C_2$ swaps them: $\tau(x,y)=(y,x)$. These actions commute. On joint
counts, $\tau$ fixes $n_{00},n_{11}$ and swaps $n_{01},n_{10}$, so choose the
canonical half with $n_{01}\leq n_{10}$. A coordinate-permutation orbit is
fixed by $\tau$ exactly when $n_{01}=n_{10}=q$. For each
$0\leq q\leq\lfloor k/2\rfloor$, the remaining counts satisfy
$n_{00}+n_{11}=k-2q$ and therefore give $k-2q+1$ fixed classes. Thus
\[
  F_k=\sum_{q=0}^{\lfloor k/2\rfloor}(k-2q+1)
    =\left\lfloor\frac{(k+2)^2}{4}\right\rfloor.
\]
Every nonfixed ordered-pair class pairs with its distinct endpoint-swapped
class. Hence the further quotient has
\begin{equation}
  U_k=\frac{1}{2}\left(
    \binom{k+3}{3}+\left\lfloor\frac{(k+2)^2}{4}\right\rfloor
  \right),
  \qquad M_k(x,y)=
  \frac{k!}{n_{00}!n_{01}!n_{10}!n_{11}!}
  \begin{cases}
    1,&n_{01}=n_{10},\\
    2,&n_{01}\neq n_{10},
  \end{cases}
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-endpoint-symmetric-pair-quotient}
\end{equation}
Here $M_k(x,y)$ is the enlarged orbit mass on the original ordered-pair domain.
The canonical orientation puts the larger endpoint Hamming weight first, so
$(\max(w_x,w_y),\min(w_x,w_y),d)$ is a complete key. At $k=14$ this gives
372 classes instead of 680 ordered coordinate-permutation classes or $4^{14}$
raw ordered pairs. Transport through $U_{a,t}$ preserves the fixed pair and
trit distance. This quotient applies only to objectives invariant under both
ambiguity-coordinate relabeling and endpoint swap; it is not direction-
sensitive Malbolge semantic equivalence or a wall-clock speedup claim.

The cube isomorphism also fixes every trit-distance shell. For any compatible
word $d$, define
$S_j(d)=\{x:\CrazyN{N}{x}{a}=t,\ H_3(d,x)=j\}$ and
$B_r(d)=\bigcup_{j=0}^{r}S_j(d)$. Choosing the $j$ differing binary coordinates
gives
\begin{equation}
  |S_j(d)|=\binom{k}{j},\qquad
  |B_r(d)|=\sum_{j=0}^{r}\binom{k}{j},\qquad
  \operatorname{diam}(G_{a,t})=k,
  \quad 0\leq j,r\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-distance}
\end{equation}
Thus a fixed-radius complete-domain neighborhood contains exactly the stated
binomial number of compatible candidates, independently of the ambient
$3^N$ data-word domain. This is a bounded search-space identity, not an
external-corpus or wall-clock performance claim.

The same coordinates give an exact mutation-step lower bound between any two
compatible words. If $x$ and $y$ differ in $j=H_3(x,y)$ trits, every path in
$G_{a,t}$ from $x$ to $y$ changes at most one differing coordinate per edge, so
it has at least $j$ edges. Flipping those $j$ coordinates once each attains the
bound, and every ordering of the $j$ flips is distinct. Therefore
\begin{equation}
  \operatorname{dist}_{G_{a,t}}(x,y)=H_3(x,y)=j,
  \qquad
  \#\operatorname{Geo}_{G_{a,t}}(x,y)=j!,
  \quad 0\leq j\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-geodesic}
\end{equation}
For $j=0$, the unique zero-edge path gives $0!=1$. This is an exact lower bound
on one-trit mutation steps inside a fixed complete-domain preimage cube and an
exact count of minimizing paths; it is not a wall-clock or external-corpus
claim.

The exact ball volume also yields a necessary lower bound on fixed-radius search
centers. Let
\[
  V_k(r)=\sum_{j=0}^{r}\binom{k}{j},
  \qquad 0\leq r\leq k.
\]
Any one radius-$r$ center covers at most $V_k(r)$ compatible words. Therefore a
family of $m$ radius-$r$ balls that covers the entire $2^k$-word preimage cube
must satisfy $mV_k(r)\geq2^k$, so
\begin{equation}
  m\geq
  \left\lceil\frac{2^k}{V_k(r)}\right\rceil,
  \qquad 0\leq r\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-covering-lower-bound}
\end{equation}
This is a necessary volume lower bound only. Overlapping balls can make the true
minimum number of centers larger, so the expression is not claimed to be an
exact covering number or generally attainable. The distinction is strict
already for $Q_5$ at radius one: $V_5(1)=6$ gives lower bound six, exhaustive
testing rejects every one of the 906,192 six-center sets, and a
seven-center witness covers all 32 vertices. Thus the exact covering number in
that checked case is seven. The general result applies only to complete
fixed-radius coverage inside one fixed preimage cube and makes no wall-clock or
external-corpus claim.

For ambiguity dimensions of Hamming form, the radius-one lower bound is attained
constructively. Let $m\in\{1,2,3\}$ and $k=2^m-1\leq N$. Label the $k$ binary
cube coordinates by the nonzero $m$-bit vectors, represented by integers
$1,\ldots,k$, and define the syndrome and zero-syndrome center set
\[
  \sigma(z)=\bigoplus_{i:z_i=1}(i+1),
  \qquad
  H_m=\{z\in\{0,1\}^k:\sigma(z)=0\}.
\]
Since the coordinate labels include every standard basis vector, $\sigma$ has
rank $m$ and $|H_m|=2^{k-m}$. For every cube word
$z$, if $\sigma(z)=s\neq0$, flipping coordinate $s-1$ changes the syndrome to
zero; if $s=0$, no flip is needed. Hence every cube word lies within radius one
of a center in $H_m$. The radius-one balls have size $k+1=2^m$, and
$|H_m|(k+1)=2^k$, so these balls partition the cube and attain the volume lower
bound. Writing $K_{Q_k}(1)$ for the minimum number of radius-one balls needed to
cover $Q_k$, the preimage-cube isomorphism $U_{a,t}$ therefore gives
\begin{equation}
  K_{Q_k}(1)=|H_m|=2^{k-m}=\frac{2^k}{k+1},
  \qquad
  m\in\{1,2,3\},\ k=2^m-1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-cube-perfect-radius-one-cover}
\end{equation}
Thus the exact minimum center counts are $1$, $2$, and $16$ for
$k=1,3,7$, respectively, and the syndrome decoder gives a canonical nearest
center for every compatible preimage. This is an exact complete-domain
radius-one covering/canonicalization result; it makes no wall-clock or
external-corpus claim.

The same checked range admits an exact converse for perfect radius-one covers.
If $c$ radius-one balls partition $Q_k$, then every ball has exactly $k+1$
vertices and disjointness gives $c(k+1)=2^k$. Hence $k+1$ divides a power of
two, so $k+1$ is itself a power of two. For $1\leq k\leq14$, this leaves only
$k\in\{1,3,7\}$; the Hamming construction above supplies a partition in each
remaining dimension. Therefore
\begin{equation}
  Q_k\text{ admits a radius-one ball partition}
    \quad\Longleftrightarrow\quad
  k\in\{1,3,7\},
  \qquad 1\leq k\leq N\leq14.
  \label{eq:optimization-crazy-preimage-cube-perfect-radius-one-dimensions}
\end{equation}
This characterizes disjoint perfect covers only. It does not claim that the
ceiling volume lower bound is unattainable by overlapping covers in every other
dimension.

The checked range also admits a complete classification of nontrivial perfect
Hamming-ball partitions. If radius-$r$ balls partition $Q_k$, disjointness
requires $V_k(r)$ to divide $2^k$. Exact integer screening for
$1\leq r<k\leq14$ leaves only
\[
  \mathcal{P}_{14}=\{(3,1),(5,2),(7,1),(7,3),(9,4),(11,5),(13,6)\}.
\]
Every odd $k=2m+1$ in the checked range has a constructive perfect radius-$m$
partition with centers $0^k$ and $1^k$: each word has either weight at most $m$
or complementary weight at most $m$, but not both. The existing radius-one
Hamming construction supplies the remaining survivor $(7,1)$. Therefore
\begin{equation}
  Q_k\text{ admits a nontrivial perfect radius-$r$ ball partition}
    \quad\Longleftrightarrow\quad (k,r)\in\mathcal{P}_{14},
  \qquad 1\leq r<k\leq14.
  \label{eq:optimization-crazy-preimage-cube-perfect-radius-classification}
\end{equation}
The antipodal cases use exactly two centers and the $(7,1)$ case uses sixteen,
so each attains the existing volume lower bound. Transport through $U_{a,t}$
gives the same perfect partitions in every compatible fixed preimage cube. This
is a bounded coding-theoretic classification only and makes no timing claim.

The complete one-trit crazy table has no accumulator/target pair with more than
two compatible data trits, so $\mu(\alpha,\tau)\leq2$. Combining that fact with
the exact product formula yields the global classic fixed-target bound
\begin{equation}
  \left|\{d\in\WordDomain{10}:\CrazyN{10}{d}{a}=t\}\right|
    \leq 2^{10}=1024
  \qquad\text{for all }a,t\in\WordDomain{10}.
  \label{eq:optimization-crazy-preimage-bound}
\end{equation}
The bound is tight because $a=0$ and the all-one-trit target have exactly two
compatible data trits at every position. This bound applies to complete
classic data-word search only; arbitrary corpora may omit those words or contain
repeated entries and therefore retain their own membership/accounting costs.

The same one-trit table sharpens the cardinality range: every factor in the
product is exactly $0$, $1$, or $2$. Therefore every fixed classic
accumulator/target pair has a preimage count in the discrete spectrum
\begin{equation}
  \left|\{d\in\WordDomain{10}:\CrazyN{10}{d}{a}=t\}\right|
    \in \{0\}\cup\{2^k:0\leq k\leq10\}.
  \label{eq:optimization-crazy-preimage-spectrum}
\end{equation}
Every listed value is attainable. With $a=0$, target trit $1$ contributes
multiplicity $2$ while target trit $2$ contributes multiplicity $1$, so choosing
exactly $k$ target-one positions realizes $2^k$; target trit $0$ in any position
realizes the zero case. This quantization is a complete-domain search-size fact,
not a statement about duplicate or incomplete external corpora.

For a fixed accumulator $a$, let
\[
  n_2(a)=\left|\{i\in\{0,\ldots,9\}:a_i=2\}\right|.
\]
Accumulator trits $0$ and $1$ each have one target trit with multiplicity $2$,
while accumulator trit $2$ has maximum multiplicity $1$. Digit independence
therefore sharpens the worst target for that particular accumulator to
\begin{equation}
  \max_{t\in\WordDomain{10}}
    \left|\{d\in\WordDomain{10}:\CrazyN{10}{d}{a}=t\}\right|
      =2^{10-n_2(a)}.
  \label{eq:optimization-crazy-preimage-accumulator-bound}
\end{equation}
The maximum is attained independently at each digit by target trit $1$ when
$a_i=0$, target trit $0$ when $a_i=1$, and any reachable target trit when
$a_i=2$. Thus accumulator state can tighten the complete-domain candidate bound
below $1024$ before enumeration, without making any claim about an incomplete or
duplicated external corpus.

For the same fixed accumulator, target reachability is also digitwise. An
accumulator trit $0$ or $1$ admits exactly two target trits, while accumulator
trit $2$ admits all three. Therefore the number of classic targets with at
least one data-word preimage is
\begin{equation}
  \left|\left\{t\in\WordDomain{10}:
    \exists d\in\WordDomain{10},\ \CrazyN{10}{d}{a}=t\right\}\right|
      =2^{10-n_2(a)}3^{n_2(a)}.
  \label{eq:optimization-crazy-reachable-target-count}
\end{equation}
This count can reject impossible target states before data-word enumeration.
It is a complete-domain reachability cardinality and does not assert that an
external candidate corpus contains any particular preimage.

The same one-trit multiplicities refine that target count by exact preimage
size. Let $m=n_2(a)$ be the number of accumulator trits equal to $2$. For every
$k\in\{0,\ldots,N-m\}$, the number of reachable targets with complete
$N$-trit data preimage cardinality exactly $2^k$ is
\begin{equation}
  \left|\left\{t\in\WordDomain{N}:
    \left|\left\{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t\right\}\right|=2^k
  \right\}\right|
    =\binom{N-m}{k}3^m,
  \qquad 0\leq m\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-fixed-accumulator-preimage-distribution}
\end{equation}
Each accumulator trit unequal to $2$ contributes one singleton-target choice
and one doubled-target choice; each accumulator trit equal to $2$ contributes
three singleton targets. Summing target classes recovers
$2^{N-m}3^m$, while preimage-weighting recovers the complete $3^N$ data-word
domain for every fixed accumulator. This is exact complete-domain target-search
planning evidence only and carries no corpus-membership or runtime-speed claim.

For a nonnegative complete-preimage enumeration budget $B$, the same fixed-
accumulator classes give an exact target exceedance count:
\begin{equation}
  T^{(N,m)}_{>B}
    =3^m\!\sum_{\substack{0\leq k\leq N-m\2^k>B}}
      \binom{N-m}{k},
  \qquad 0\leq m\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-fixed-accumulator-budget-exceedance}
\end{equation}
Thus $T^{(N,m)}_{>0}=2^{N-m}3^m$ and
$T^{(N,m)}_{>2^{N-m}}=0$. This counts targets whose complete preimage cannot
fit within $B$ data candidates for a fixed accumulator class. It does not claim
that an incomplete external corpus contains every such preimage or that every
search algorithm must enumerate them.

The same statistic $n_2(a)$ partitions the complete accumulator domain. For
$k\in\{0,\ldots,10\}$, exactly $k$ positions must be chosen for trit value 2,
and every remaining position independently admits either trit 0 or trit 1.
Hence the exact cardinality of accumulator class $k$ is
\begin{equation}
  \left|\{a\in\WordDomain{10}:n_2(a)=k\}\right|
    = \binom{10}{k}2^{10-k}.
  \label{eq:optimization-crazy-accumulator-class-cardinality}
\end{equation}
Summing these eleven classes yields $(2+1)^{10}=3^{10}=59049$, so the
partition is complete. This grouping is an exact planning classification only;
it does not imply that two accumulator values are semantically interchangeable
outside the two crazy-target cardinalities derived above.

Across accumulator and target digits together, accumulator trit $0$ admits two
target trits, accumulator trit $1$ admits two, and accumulator trit $2$ admits
three. Thus one trit position has exactly $2+2+3=7$ reachable
accumulator/target pairs. Digit independence gives the complete classic count
\begin{equation}
  \left|\left\{(a,t)\in\WordDomain{10}^{2}:
    \exists d\in\WordDomain{10},\ \CrazyN{10}{d}{a}=t\right\}\right|
      =7^{10}=282475249.
  \label{eq:optimization-crazy-reachable-pair-count}
\end{equation}
The complete accumulator/target domain has $9^{10}$ pairs, so the reachable
fraction is $(7/9)^{10}$. This is a global complete-domain pruning fact only;
it does not identify a data preimage or imply a runtime speedup.

The one-trit multiplicity table refines that total further. Among the nine
possible accumulator/target trit pairs, exactly two have multiplicity zero,
five have multiplicity one, and two have multiplicity two. Therefore, for
$k\in\{0,\ldots,10\}$, the number of reachable classic accumulator/target
pairs whose complete data-word preimage has cardinality exactly $2^k$ is
\begin{equation}
  \left|\left\{(a,t)\in\WordDomain{10}^{2}:
    \left|\left\{d\in\WordDomain{10}:\CrazyN{10}{d}{a}=t
    \right\}\right|
      =2^k
  \right\}\right|
    =\binom{10}{k}2^k5^{10-k}.
  \label{eq:optimization-crazy-preimage-pair-distribution}
\end{equation}
Choosing the $k$ doubled positions contributes $\binom{10}{k}$, each doubled
position has two accumulator/target trit pairs, and each remaining reachable
position has five singleton pairs. Summing the classes gives
$(5+2)^{10}=7^{10}$ reachable pairs, while weighting each class by its preimage
size gives $(5+4)^{10}=9^{10}=59049^2$, exactly accounting for every classic
$(d,a)$ input pair. This distribution is a complete-domain search-planning fact;
it does not imply equal semantics between different accumulator/target pairs or
a measured runtime speedup.

The same digitwise multiplicity classification extends exactly across every
bounded width $N\in\{1,\ldots,14\}$, a family that includes both canonical
profile widths 10 and 14. For $k\in\{0,\ldots,N\}$, the number of
accumulator/target pairs whose complete
$N$-trit data-word preimage has cardinality $2^k$ is
\begin{equation}
  \left|\left\{(a,t)\in\WordDomain{N}^{2}:
    \left|\left\{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t
    \right\}\right|=2^k
  \right\}\right|
    =\binom{N}{k}2^k5^{N-k},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-preimage-pair-distribution}
\end{equation}
Consequently the reachable pair count is $7^N$, weighting each class by its
preimage size accounts for all $9^N$ data/accumulator inputs, and the
unreachable accumulator/target complement is $9^N-7^N$. This is complete-domain
planning
evidence only for this bounded width family; it does not assert candidate-corpus
membership or runtime speedup.

Composing the width-indexed preimage-class distribution with the ordered
cube-triple quotient gives an exact global representative count. Let $T_N$ be
the number of canonical ordered preimage triples obtained by quotienting each
reachable fixed accumulator/target preimage cube independently. Then
\begin{equation}
  T_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}\binom{k+7}{7}
    =\sum_{j=0}^{\min(7,N)}
      \binom{7}{j}\binom{N}{j}2^j7^{N-j},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-ordered-preimage-triple-quotient}
\end{equation}
The second equality follows from
$\binom{k+7}{7}=\sum_{j=0}^{7}\binom{7}{j}\binom{k}{j}$ and the weighted
binomial moments. The unquotiented ordered-triple mass is
$\sum_k\binom{N}{k}2^k5^{N-k}8^k=21^N$. At $N=14$, the quotient has
547751638341145 representatives instead of 3243919932521508681 raw ordered
triples. This is an exact complete-domain search-space count only; it inherits
the simultaneous-coordinate symmetry requirement and makes no timing claim.

The endpoint-unordered triple quotient also composes over the same reachable
fixed-pair distribution. Let $G_N$ count canonical triples after simultaneous
coordinate permutation and the full endpoint action $S_3$. For the three
endpoint cycle types, let $I_N$, $R_N$, and $C_N$ count globally fixed
coordinate-quotient classes for the identity, one transposition, and one
three-cycle respectively. Then
\begin{equation}
  \begin{aligned}
    G_N&=\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}
      \frac{\binom{k+7}{7}+3\tau_k+2\chi_k}{6}
      =\frac{I_N+3R_N+2C_N}{6},\\
    \sum_{N\geq0}I_Ny^N&=\frac{(1-5y)^7}{(1-7y)^8},\\
    \sum_{N\geq0}R_Ny^N&=
      \frac{(1-5y)^7}{(1-7y)^6(1-3y)^2},\\
    \sum_{N\geq0}C_Ny^N&=
      \frac{(1-5y)^7}{(1-7y)^4(1-8y+19y^2)^2},
  \end{aligned}
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-unordered-preimage-triple-quotient}
\end{equation}
The generating functions follow from the binomial transform
$z=2y/(1-5y)$ applied to the fixed-class series
$(1-z)^{-8}$, $(1-z)^{-4}(1-z^2)^{-2}$, and
$(1-z)^{-2}(1-z^3)^{-2}$. At $N=14$, the three global fixed counts are
547751638341145, 61437730689609, and 6805238953045, giving
124279218052677 endpoint-unordered representatives instead of
$21^{14}=3243919932521508681$ raw ordered triples. This is an exact
complete-domain state count for analyses invariant under both stated symmetry
actions and makes no timing claim.

Composing the same preimage-class distribution with the ordered cube-quadruple
quotient gives another exact global representative count. Let $Q_N$ be the
number of canonical ordered preimage quadruples obtained by quotienting each
reachable fixed accumulator/target preimage cube independently. Then
\begin{equation}
  Q_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}\binom{k+15}{15}
    =\sum_{j=0}^{\min(15,N)}
      \binom{15}{j}\binom{N}{j}2^j7^{N-j},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-ordered-preimage-quadruple-quotient}
\end{equation}
The second equality follows from
$\binom{k+15}{15}=\sum_{j=0}^{15}\binom{15}{j}\binom{k}{j}$ and the weighted
binomial moments. The unquotiented ordered-quadruple mass is
$\sum_k\binom{N}{k}2^k5^{N-k}16^k=37^N$. At $N=14$, the quotient has
25678405217633865 representatives instead of 9012061295995008299689 raw
ordered quadruples. This is an exact complete-domain search-space count only;
it inherits the simultaneous-coordinate symmetry requirement and makes no
timing claim.

The endpoint-unordered quadruple quotient also composes over the same
reachable fixed-pair distribution. Apply the binomial transform
$z=2y/(1-5y)$ to the five fixed-class series from the local $S_4$ action and
define
\[
  \begin{aligned}
    \widehat A(y)&=\frac{(1-5y)^{15}}{(1-7y)^{16}},\\
    \widehat B(y)&=
      \frac{(1-5y)^{15}}{(1-7y)^{12}(1-3y)^4},\\
    \widehat D(y)&=
      \frac{(1-5y)^{15}}{(1-7y)^{10}(1-3y)^6},\\
    \widehat C(y)&=
      \frac{(1-5y)^{15}}{(1-7y)^8(1-8y+19y^2)^4},\\
    \widehat Q(y)&=
      \frac{(1-5y)^{15}}
      {(1-7y)^6(1-3y)^4(1-10y+29y^2)^3}.
  \end{aligned}
\]
Writing $\widehat A_N=[y^N]\widehat A(y)$ and analogously for the other
four series gives
\begin{equation}
  U^{(4)}_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}
      \left|\mathcal{J}^{(4)}_k/S_4\right|
    =\frac{\widehat A_N+6\widehat B_N+3\widehat D_N
      +8\widehat C_N+6\widehat Q_N}{24},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-unordered-preimage-quadruple-quotient}
\end{equation}
At $N=14$, the five global fixed counts are 25678405217633865,
1182834266824809, 180742210147993, 57110313884289, and 9848929136817,
yielding 1409733897288413 endpoint-unordered representatives instead of
$37^{14}=9012061295995008299689$ raw ordered quadruples. This is an exact
complete-domain state count for analyses invariant under both stated symmetry
actions and makes no timing claim.

Composing the same preimage-class distribution with the ordered cube-quintuple
quotient gives another exact global representative count. Let $P_N$ be the
number of canonical ordered preimage quintuples obtained by quotienting each
reachable fixed accumulator/target preimage cube independently. Then
\begin{equation}
  P_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}\binom{k+31}{31}
    =\sum_{j=0}^{\min(31,N)}
      \binom{31}{j}\binom{N}{j}2^j7^{N-j},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-ordered-preimage-quintuple-quotient}
\end{equation}
The second equality follows from
$\binom{k+31}{31}=\sum_{j=0}^{31}\binom{31}{j}\binom{k}{j}$ and the weighted
binomial moments. The unquotiented ordered-quintuple mass is
$\sum_k\binom{N}{k}2^k5^{N-k}32^k=69^N$. At $N=14$, the quotient has
3571359808057227945 representatives instead of 55448176762342779635202921
raw ordered quintuples. This is an exact complete-domain search-space count
only; it inherits the simultaneous-coordinate symmetry requirement and makes
no timing claim.

The endpoint-unordered quintuple quotient composes over the same reachable
fixed-pair ambiguity distribution. For one $S_5$ endpoint conjugacy class
$\lambda$, let $c_{\lambda,1},\ldots,c_{\lambda,m_\lambda}$ be its induced
cycle lengths on the thirty-two joint-pattern labels and define
\[
  A_\lambda(z)=\prod_{i=1}^{m_\lambda}
    (1-z^{c_{\lambda,i}})^{-1},\qquad
  \widehat A_\lambda(y)=\frac{1}{1-5y}
    A_\lambda\!\left(\frac{2y}{1-5y}\right).
\]
If $w_\lambda$ is the endpoint conjugacy-class size, then
\begin{equation}
  U^{(5)}_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}
      \left|\mathcal{J}^{(5)}_k/S_5\right|
    =\frac{1}{120}\sum_\lambda w_\lambda
      [y^N]\widehat A_\lambda(y),
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-unordered-preimage-quintuple-quotient}
\end{equation}
At $N=14$, the seven global fixed counts are 3571359808057227945,
54779621914531305, 955501620651033, 3768908530569225, 84481568466345,
101628460149705, and 6090369480725, yielding 34995940605821849 endpoint-
unordered representatives instead of $69^{14}=55448176762342779635202921$ raw
ordered quintuples. This is an exact complete-domain state count for analyses
invariant under both symmetry actions and makes no timing claim.

The ordered sextuple quotient composes with the reachable fixed-pair ambiguity
distribution in the same way. Let $R_N$ count canonical ordered preimage
sextuples. Then
\begin{equation}
  R_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}\binom{k+63}{63}
    =\sum_{j=0}^{\min(63,N)}
      \binom{63}{j}\binom{N}{j}2^j7^{N-j},
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-ordered-preimage-sextuple-quotient}
\end{equation}
The unquotiented ordered-sextuple mass is $133^N$. At $N=14$, the coordinate
quotient has 1584315319509725541225 representatives instead of
$133^{14}=541904769658563069794308330729$ raw ordered sextuples. This is an
exact complete-domain cardinality result only and makes no timing claim.

The endpoint-unordered sextuple quotient also composes exactly. Reuse the local
$S_6$ endpoint types $\lambda$, class sizes $w_\lambda$, and label-cycle
multiplicities $m_{\lambda,c}$. Define
\[
  A_\lambda(z)=\prod_c(1-z^c)^{-m_{\lambda,c}},\qquad
  \widehat A_\lambda(y)=\frac{1}{1-5y}
    A_\lambda\!\left(\frac{2y}{1-5y}\right).
\]
Then
\begin{equation}
  U^{(6)}_N
    =\sum_{k=0}^{N}\binom{N}{k}2^k5^{N-k}
      \left|\mathcal{J}^{(6)}_k/S_6\right|
    =\frac{1}{720}\sum_\lambda w_\lambda
      [y^N]\widehat A_\lambda(y),
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-global-unordered-preimage-sextuple-quotient}
\end{equation}
At $N=14$, the eleven global fixed counts are 1584315319509725541225,
7054158423775528425, 38878022703262281, 175942582684950825,
1460917013254953, 1806087636205929, 43767572733825, 18851333161147209,
256827625272665, 244844111940225, and 13080355599105. Burnside yields
2361488883978006005 endpoint-unordered representatives instead of $133^{14}$
raw ordered sextuples. Direct ambiguity sums, the generic transform, and all
reachable fixed pairs through width two agree. This is an exact state-count
result only and makes no timing claim.

The endpoint-symmetric pair quotient also composes exactly over the reachable
fixed-pair ambiguity distribution. Let $E_N$ count canonical ordered preimage
pairs after simultaneous coordinate permutation and endpoint swap, and let
$S_N$ count the coordinate-permutation classes fixed by endpoint swap after the
same global sum. Then
\begin{equation}
  E_N=\frac{1}{2}\left[
    \sum_{j=0}^{\min(3,N)}\binom{3}{j}\binom{N}{j}2^j7^{N-j}+S_N
  \right],\qquad
  S_N=\begin{cases}
    9,&N=1,\\
    \dfrac{7^{N+1}+3^N+20N7^{N-1}+8N(N-1)7^{N-2}}{8},&2\leq N\leq14.
  \end{cases}
  \label{eq:optimization-crazy-global-endpoint-symmetric-pair-quotient}
\end{equation}
Indeed,
$\lfloor(k+2)^2/4\rfloor=((k+2)^2-\mathbf{1}_{k\text{ odd}})/4$,
while the fixed-pair weights have generating polynomial $(5+2x)^N$ and parity
sum $(5-2)^N=3^N$. The unquotiented ordered-pair mass is $13^N$. At $N=14$,
the endpoint-symmetric quotient has 18096618233793 representatives instead of
3937376385699289 raw ordered pairs. This is an exact complete-domain
search-space count only and applies only when endpoint order is irrelevant; it
makes no timing claim.

The $7^N$ reachable-pair count also admits a constructive canonical index.
Let
\[
  L=\{(a,t)\in\{0,1,2\}^2:\exists d\in\{0,1,2\},\ \gamma(d,a)=t\}.
\]
The independent trit relation gives $|L|=7$. Let
$q:L\to\{0,\ldots,6\}$ be the lexicographic bijection. For a width-$N$
reachable accumulator/target pair with trit pairs $(a_i,t_i)$, define
\begin{equation}
  \operatorname{rank}_N(a,t)
    =\sum_{i=0}^{N-1}q(a_i,t_i)7^i,
  \qquad
  \operatorname{rank}_N:
    L^N\overset{\sim}{\longrightarrow}\{0,\ldots,7^N-1\},
  \quad 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-reachable-pair-ranking}
\end{equation}
The inverse decodes the rank in base seven and applies $q^{-1}$ digitwise.
Therefore a complete enumeration whose domain is exactly the reachable
accumulator/target pairs may enumerate $7^N$ canonical codes directly and never
visit the $9^N-7^N$ impossible pairs. The bijection preserves pair identity; it
does not equate distinct reachable pairs, infer external-corpus membership, or
claim a runtime speedup.

A uniformly chosen reachable accumulator/target pair therefore has exact mean
complete-domain preimage cardinality
\begin{equation}
  \frac{1}{7^N}
  \sum_{\substack{(a,t)\in\WordDomain{N}^{2}\
                   \exists d:\CrazyN{N}{d}{a}=t}}
    \left|\left\{d\in\WordDomain{N}:\CrazyN{N}{d}{a}=t\right\}\right|
    =\frac{9^N}{7^N}=\left(\frac{9}{7}\right)^N,
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-mean-preimage-count}
\end{equation}
This mean is over reachable pairs only and follows from exact input-mass
accounting, not from a probabilistic model of an external candidate corpus. It
is search-planning evidence and carries no runtime-speedup claim.

The same class distribution gives the exact second moment and variance of the
complete-domain preimage cardinality over uniformly chosen reachable pairs:
\begin{equation}
  \begin{aligned}
    \mathbb{E}[M_N^2]
      &= \frac{13^N}{7^N}=\left(\frac{13}{7}\right)^N,\
    \operatorname{Var}(M_N)
      &= \left(\frac{13}{7}\right)^N
       - \left(\frac{9}{7}\right)^{2N},
    \qquad 1\leq N\leq14,
  \end{aligned}
  \label{eq:optimization-crazy-profile-preimage-variance}
\end{equation}
where $M_N$ is the complete preimage cardinality of a uniformly selected
reachable accumulator/target pair. This is exact dispersion evidence for
complete-domain search planning only; it does not model an external corpus or
imply a runtime-speedup distribution.

Equivalently, for $K_N=\log_2 M_N$ over reachable pairs, the preimage exponent
is exactly binomial:
\begin{equation}
  \Pr[K_N=k]
    = \binom{N}{k}\left(\frac{2}{7}\right)^k
      \left(\frac{5}{7}\right)^{N-k},\qquad
  \mathbb{E}[K_N]=\frac{2N}{7},\qquad
  \operatorname{Var}(K_N)=\frac{10N}{49},
  \quad 0\leq k\leq N,\ 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-log-preimage-binomial}
\end{equation}
This describes exact multiplicity classes under uniform selection from the
reachable complete-domain pair set. It is not a distributional assumption about
an external candidate corpus and carries no timing claim.

For a nonnegative integer full-domain enumeration budget $B$, the same exact
preimage classes give the number of reachable accumulator/target pairs whose
complete data-word preimage cannot fit within that budget:
\begin{equation}
  N_{>B}
    = \sum_{\substack{0\leq k\leq10\2^k>B}}
      \binom{10}{k}2^k5^{10-k}.
  \label{eq:optimization-crazy-preimage-budget-exceedance}
\end{equation}
Thus $N_{>0}=7^{10}$ and $N_{>1024}=0$. This is an exact planning lower bound
only for a procedure that must enumerate every complete-domain preimage of each
pair. It does not imply that an incomplete external corpus contains those
preimages, that every search algorithm must enumerate them, or that a budgeted
search is semantically incorrect when completeness is not its contract.

The same threshold extends to the bounded width family above. For
$N\in\{1,\ldots,14\}$ and nonnegative integer $B$, define
\begin{equation}
  N^{(N)}_{>B}
    = \sum_{\substack{0\leq k\leq N\2^k>B}}
      \binom{N}{k}2^k5^{N-k}.
  \label{eq:optimization-crazy-profile-preimage-budget-exceedance}
\end{equation}
Then $N^{(N)}_{>0}=7^N$ and $N^{(N)}_{>2^N}=0$. The bound inherits the
same complete-domain assumption as the classic equation. It does not claim that
a search over an incomplete corpus must enumerate every preimage, and it carries
no timing or runtime-speed conclusion.

The same ordered classes can be inverted to answer an exact coverage-budget
question. Define
\[
  C_N(k)=\sum_{j=0}^{k}\binom{N}{j}2^j5^{N-j}.
\]
For an integer target $Q\in\{0,\ldots,7^N\}$, the least nonnegative complete-
preimage budget that covers at least $Q$ reachable accumulator/target pairs is
\begin{equation}
  B_N(Q)=\begin{cases}
    0, & Q=0,\
    \min\{2^k:C_N(k)\geq Q,\ 0\leq k\leq N\}, & 1\leq Q\leq7^N,
  \end{cases}
  \qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-minimum-coverage-budget}
\end{equation}
Because positive preimage sizes are exactly the powers of two, for $k\geq1$
no integer budget below $2^k$ covers a class beyond $k-1$; for $k=0$, budget
zero covers no positive-preimage pair and budget one reaches the first class.
Thus the selected power of two is minimal among all integer budgets, not merely
among class labels. This is exact planning evidence only for procedures that
promise complete-preimage enumeration; it does not model incomplete corpora or
imply runtime speedup.

The unreachable accumulator/target region is the exact complement of the
reachable region inside the complete $9^{10}$ pair domain:
\begin{equation}
  \left|\left\{(a,t)\in\WordDomain{10}^{2}:
    \nexists d\in\WordDomain{10},\ \CrazyN{10}{d}{a}=t\right\}\right|
    =9^{10}-7^{10}=3204309152.
  \label{eq:optimization-crazy-unreachable-pair-count}
\end{equation}
This count rejects only complete-domain impossibility. It does not imply that a
reachable pair occurs in any incomplete external candidate corpus or that
checking membership is free.

For the same bounded width family, the unreachable complement scales exactly as
\begin{equation}
  \left|\left\{(a,t)\in\WordDomain{N}^{2}:
    \nexists d\in\WordDomain{N},\ \CrazyN{N}{d}{a}=t\right\}\right|
    =9^N-7^N,\qquad 1\leq N\leq14.
  \label{eq:optimization-crazy-profile-unreachable-pair-count}
\end{equation}
This is the complement of the independently checked $7^N$ reachable class sum
inside the complete $9^N$ accumulator/target domain. It is an impossibility
count only; it does not infer corpus membership or search/runtime cost.

\section{Decode phase reduction}

For graphical cell $m$, define the exact decode sum class
\[
  s(m,c)=(m-33+c)\bmod94.
\]
The preserved historical $X_1$ table is a permutation of all 94 graphical
bytes. Hence
\begin{equation}
  \DecodeOp(m,c)=X_1[s(m,c)],
  \qquad
  \DecodeOp(m,c)=\DecodeOp(m',c')
  \Longleftrightarrow s(m,c)=s(m',c').
  \label{eq:optimization-decode-phase}
\end{equation}
Thus the $94\times94=8836$ graphical-cell/code-phase pairs form exactly 94
decode-equivalence classes of size 94. A boundary whose only observable is the
decoded opcode may retain $s(m,c)$ instead of the pair $(m,c\bmod94)$. This does
not identify whole machine states: raw cell or code-pointer identity must remain
when encryption, control flow, source location, or another downstream semantic
uses them separately.


\section{Self-encryption orbit canonicalization}

Let $G=\{33,\ldots,126\}$ be the graphical-cell domain and let
$E:G\to G$ be the Malbolge self-encryption permutation $E(m)=\EncryptOp(m)$.
The exact $X_2$ table partitions $G$ into six disjoint cycles whose lengths are
\[
  \{2,4,5,6,9,68\}.
\]
For a cell $m\in G$, write $\ell(m)$ for the length of its unique cycle. Then
for every visit count $k\in\mathbb{N}$,
\begin{equation}
  E^k(m)=E^{k\bmod\ell(m)}(m).
  \label{eq:optimization-encryption-orbit}
\end{equation}
Therefore a search state that tracks only repeated committed self-encryption of
one unchanged code-cell address may replace an unbounded encryption-visit count
by the canonical residue $k\bmod\ell(m)$. The reduction applies only when each
counted visit reaches the normative post-instruction encryption phase. Halt,
rejected transitions, writes that change the cell by another mechanism, and
control-flow/address equivalence remain outside this reduction.

\section{Classic rotate lookup}

The classic word domain is finite, so define a table $R$ by
\begin{equation}
  R[x]=\RotateN{10}{x}
  \qquad\text{for every }x\in\WordDomain{10}.
  \label{eq:optimization-rotate-table}
\end{equation}
Replacing scalar rotation by $R[x]$ is an exact finite-domain substitution, not
an approximation or heuristic.

\section{Classic rotate-history canonicalization}

Let $R$ now denote one normative right rotation of a classic ten-trit word. A
rotation permutes the ten trit positions cyclically, so applying ten rotations
restores every word. Therefore, for every $x\in\WordDomain{10}$ and visit count
$k\in\mathbb{N}$,
\begin{equation}
  R^k(x)=R^{k\bmod 10}(x).
  \label{eq:optimization-rotate-history}
\end{equation}
This gives a safe universal canonical bound, but the exact period can be
sharpened per word. Define
\[
  p(x)=\min\{d\in\{1,2,5,10\}:R^d(x)=x\}.
\]
Every rotation period divides ten, and exhaustive classification of the classic
word domain gives
\begin{equation}
  \left|\{x:p(x)=d\}\right|=
  \begin{cases}
    3, & d=1,\\
    6, & d=2,\\
    240, & d=5,\\
    58800, & d=10.
  \end{cases}
  \qquad
  R^k(x)=R^{k\bmod p(x)}(x).
  \label{eq:optimization-rotate-minimal-period}
\end{equation}
The counts partition all $3^{10}=59049$ classic words. Therefore a search state
tracking repeated rotate updates to one otherwise unchanged classic data cell
may use the word-specific residue modulo $p(x)$ instead of the universal residue
modulo ten. Intervening writes or a changed data-pointer identity fall outside
this reduction, and the result carries no runtime-speed claim.

\section{Verified candidate boundary}

Let $C$ be a candidate set, $V(c,p)\in\{0,1\}$ the independent verifier under
profile $p$, and $\CostVector{c}$ the measured cost vector. Search may rank or
generate candidates arbitrarily, but only
\[
  C_V=\{c\in C\mid V(c,p)=1\}
\]
is eligible for cost comparison. A speedup cannot repair failed semantic
correspondence.

\section{Measured implementation result}

The versioned CPU VM benchmark evidence at
\texttt{benchmarks/interpreter/evidence/2026-07-26-windows-x86\_64/} contains 15
raw
samples per scalar/table implementation and identical checksums. On that
identified host, the classic crazy factorization reduced the median from
77,456,700 ns to 7,423,600 ns (10.43x), while rotate lookup reduced the median
from 15,260,300 ns to 10,141,700 ns (1.50x). These measurements justify the
current CPU implementation on that recorded workload; they are not universal
hardware performance claims.

\end{document}
