# Interface

## Purpose

User-facing interaction surfaces.

## Ownership

This boundary is owned by `repository:malbolge`.

## Prohibitions

It must not collapse independent function or kind boundaries.

## Navigation

- [`command-line/`](command-line/): governed function `command-line`.
