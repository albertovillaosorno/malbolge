# Tooling

## Purpose

Developer and language-support tools.

## Ownership

This boundary is owned by `repository:malbolge`.

## Prohibitions

It must not collapse independent function or kind boundaries.

## Navigation

- [`decompiler/`](decompiler/): governed function `decompiler`.
- [`language-support/`](language-support/): governed function
  `language-support`.
- [`native-analysis/`](native-analysis/): governed function `native-analysis`.
