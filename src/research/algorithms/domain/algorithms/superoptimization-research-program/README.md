# Superoptimization research program

This directory is the executable mirror for research ID
`superoptimization-research-program`. The current slice owns only a versioned,
validator-enforced experiment plan. It does not claim an implemented search
strategy or a measured result.

Future executable comparisons belong here only when they preserve the declared
challenge identity, budget, baseline, and independent verifier boundary.
Regenerable run output belongs in `out/` and remains Git ignored.
